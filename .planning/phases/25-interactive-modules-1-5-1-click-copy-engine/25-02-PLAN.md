---
phase: 25-interactive-modules-1-5-1-click-copy-engine
plan: "02"
wave: 2
depends_on:
  - "25-01"
files_modified:
  - app/components/course/pretraining/PretrainingModulesSection.tsx
  - app/routes/course.ai.tsx
  - tests/pretraining-modules.test.js
autonomous: true
requirements:
  - PRE-MOD-01
  - PRE-MOD-02
  - PRE-MOD-03
  - PRE-MOD-04
  - PRE-MOD-05
  - PRE-MOD-06
must_haves:
  truths:
    - `PretrainingModulesSection.tsx` mounts `#dynamic-modules-container` with Master Controls `#btn-expand-all-modules` and `#btn-collapse-all-modules` that open/close all 5 modules simultaneously.
    - Individual module cards toggle independently and report accessible ARIA states (`role="button"`, `aria-expanded`, `aria-controls`).
    - `app/routes/course.ai.tsx` mounts `<PretrainingModulesSection />` immediately beneath `<PretrainingPowerShellSection />` within `#container-pretraining`.
    - Automated test suite `tests/pretraining-modules.test.js` verifies all requirements PRE-MOD-01 through PRE-MOD-06.
    - Full test suite passes 100% with zero regressions across all existing suites.
    - TypeScript compilation completes with 0 errors (`tsc --noEmit`).
  artifacts:
    - app/components/course/pretraining/PretrainingModulesSection.tsx
    - app/routes/course.ai.tsx
    - tests/pretraining-modules.test.js
  key_links:
    - `PretrainingModulesSection.tsx` imports module data and renders the collapsible module sequence with master accordion controls.
    - `app/routes/course.ai.tsx` hosts the complete pre-training curriculum when `mode === 'pretraining'`.
    - `tests/pretraining-modules.test.js` validates data integrity, DOM elements, copy engine patterns, and route mounting.
---

<objective>
Assemble the Master Accordion Section (`PretrainingModulesSection.tsx`) with master open/close controls, integrate it into `app/routes/course.ai.tsx`, create the comprehensive automated test suite (`tests/pretraining-modules.test.js`), and verify zero regressions across the entire application test suite.

Purpose: Complete requirements PRE-MOD-01 through PRE-MOD-06 and provide deterministic verification for Phase 25.
Output: Integrated master modules section, updated AI course route, and passing test suite.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-interactive-modules-1-5-1-click-copy-engine/25-01-PLAN.md
@.planning/phases/25-interactive-modules-1-5-1-click-copy-engine/25-RESEARCH.md
@.planning/phases/25-interactive-modules-1-5-1-click-copy-engine/25-PATTERNS.md
@.planning/phases/25-interactive-modules-1-5-1-click-copy-engine/25-VALIDATION.md
@app/routes/course.ai.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PretrainingModulesSection with Master Controls and integrate into course.ai.tsx</name>
  <files>
    app/components/course/pretraining/PretrainingModulesSection.tsx
    app/routes/course.ai.tsx
  </files>
  <read_first>
    index.html
    app/data/pretrainingModules.ts
    app/components/course/pretraining/PretrainingModuleCard.tsx
    app/components/ui/Toast.tsx
    app/routes/course.ai.tsx
  </read_first>
  <action>
    1. Create `app/components/course/pretraining/PretrainingModulesSection.tsx`:
       - Import `PRETRAINING_MODULES` from `@/data/pretrainingModules`.
       - Import `PretrainingModuleCard` from `./PretrainingModuleCard`.
       - Import `ToastContainer` and `showToast` from `@/components/ui/Toast`.
       - Manage accordion state with `useState<Record<string, boolean>>`:
         - Default all 5 modules (`sec-module-1` through `sec-module-5`) to `true` (expanded).
       - Render container `<div id="dynamic-modules-container">`.
       - Render Master Controls:
         ```tsx
         <div className="module-controls">
           <button id="btn-expand-all-modules" className="btn btn-sm btn-outline" onClick={handleExpandAll}>
             <span>📖</span> Buka Semua Modul
           </button>
           <button id="btn-collapse-all-modules" className="btn btn-sm btn-outline" onClick={handleCollapseAll}>
             <span>📁</span> Tutup Semua Modul
           </button>
         </div>
         ```
       - `handleExpandAll`: sets all module keys to `true` and triggers `showToast('Semua modul dibuka 📖', 'info', 1800)`.
       - `handleCollapseAll`: sets all module keys to `false` and triggers `showToast('Semua modul disembunyikan 📁', 'info', 1800)`.
       - Render `<ToastContainer />` within the section.
       - Map through `PRETRAINING_MODULES` and wrap each in `<section id={mod.id} className="content-section">`, passing `module`, `isExpanded={!!expandedModules[mod.id]}`, and `onToggle={() => toggleModule(mod.id)}`.
    2. Modify `app/routes/course.ai.tsx`:
       - Import `PretrainingModulesSection` from `@/components/course/pretraining/PretrainingModulesSection`.
       - Within `{mode === 'pretraining' ? (...) : (...)}`, insert `<PretrainingModulesSection />` immediately following `<PretrainingPowerShellSection />` inside `<div id="container-pretraining" className="mode-container active">`.
  </action>
  <verify>
    <automated>pwsh -Command "& 'C:\nvm4w\nodejs\node.exe' ./node_modules/typescript/bin/tsc --noEmit"</automated>
    <fails_when>non-zero exit or TypeScript compilation error</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Create automated unit and integration test suite</name>
  <files>tests/pretraining-modules.test.js</files>
  <read_first>
    tests/pretraining-foundation.test.js
    app/data/pretrainingModules.ts
    app/components/course/pretraining/PretrainingModulesSection.tsx
    app/components/course/pretraining/CopyableCodeBlock.tsx
    app/routes/course.ai.tsx
  </read_first>
  <action>
    Create `tests/pretraining-modules.test.js` using Node.js native test runner (`node:test`, `node:assert`):
    1. **Suite 1: Data Model Completeness (PRE-MOD-01..05)**:
       - Verify `PRETRAINING_MODULES` exports exactly 5 modules with IDs `sec-module-1` through `sec-module-5`.
       - Modul 1 (Node.js): exactly 3 steps (`m1-check-node`, `m1-verify-lts`, `m1-check-npm`), contains `node --version` and `npm --version`, and Checkpoint 1 preview (Target Tahap 1).
       - Modul 2 (9Router): exactly 4 steps (`m2-install-pkg`, `m2-start-service`, `m2-open-dashboard`, `m2-verify-local`), contains `npm install -g 9router`, `9router`, `Ctrl + C`, curl test command, and Checkpoint 2 preview (Target Tahap 2).
       - Modul 3 (Telegram Bot): exactly 5 steps (`m3-start-botfather`, `m3-create-newbot`, `m3-save-token-secure`, `m3-start-chat`, `m3-get-userid`), contains `/newbot`, `/revoke`, BotFather link, userinfobot link, and Checkpoint 3 preview (Target Tahap 3).
       - Modul 4 (Google Cloud & Hermes): 2 steps (`m4-open-console`, `m4-verify-login`), Google Cloud Console URL, and Hermes CLI readiness guidance.
       - Modul 5 (Catatan & Batasan): contains Batasan 1 (9Router), Batasan 2 (Hermes Agent), 4-node architecture flow components, and completion banner.
    2. **Suite 2: 1-Click Copy Engine & Toast Parity (PRE-MOD-06)**:
       - Verify `CopyableCodeBlock.tsx` imports and provides 1-click copy logic with clipboard API and textarea fallback.
       - Verify copy state triggers `✓ Tersalin!` and 2000ms reset timer.
       - Verify multi-line commands preserve exact newlines and whitespace formatting.
       - Verify `Toast.tsx` exports `showToast` and `ToastContainer` with appropriate CSS classes (`toast-container`, `toast-success`).
    3. **Suite 3: Master Accordion & Component Architecture**:
       - Verify `PretrainingModulesSection.tsx` implements master control buttons `#btn-expand-all-modules` and `#btn-collapse-all-modules`.
       - Verify `PretrainingModuleCard.tsx` implements accessible ARIA attributes (`role="button"`, `aria-expanded`, `aria-controls`, `module-chevron`).
       - Verify `ModuleArchitectureFlow.tsx` renders all 4 flow nodes (`Telegram Ponsel`, `Hermes Agent`, `9Router`, `Model AI Cloud`).
       - Verify `CheckpointPreviewCard.tsx` renders milestone badge and requirement checklists.
    4. **Suite 4: Route Integration**:
       - Verify `app/routes/course.ai.tsx` imports and renders `<PretrainingModulesSection />` when `mode === 'pretraining'`.
  </action>
  <verify>
    <automated>& 'C:\nvm4w\nodejs\node.exe' --test tests/pretraining-modules.test.js</automated>
    <fails_when>non-zero exit or test assertion failure</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Run full suite zero-regression verification</name>
  <files>tests/pretraining-modules.test.js</files>
  <read_first>
    package.json
  </read_first>
  <action>
    1. Execute all test suites across the project (`tests/*.test.js`) to guarantee zero regression on Course 1, Course 2, live class modules, session security, and Phase 24 foundation sections.
    2. Execute TypeScript compiler (`tsc --noEmit`) to verify 100% strict type conformance with 0 errors.
  </action>
  <verify>
    <automated>& 'C:\nvm4w\nodejs\node.exe' --test tests/*.test.js</automated>
    <fails_when>non-zero exit or test failure</fails_when>
  </verify>
</task>

</tasks>
