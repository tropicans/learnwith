---
phase: 20-file-based-routes-validated-search-params
plan: "02"
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - app/routes/index.tsx
  - app/routes/course.ai.tsx
  - app/routes/course.word.tsx
autonomous: true
requirements:
  - ROUTE-02
  - ROUTE-03
  - ROUTE-04
must_haves:
  truths:
    - Index route (app/routes/index.tsx) validates filter parameter with homeSearchSchema and renders the Google NotebookLM Frontpage Hub.
    - Course AI route (app/routes/course.ai.tsx) validates mode parameter with courseAiSearchSchema and renders Agentic AI workspace shell with dual-mode tabs.
    - Course Word route (app/routes/course.word.tsx) validates query parameters with courseWordSearchSchema and renders Word Processing ASN workspace shell.
    - TanStack Router automatically compiles routes into app/routeTree.gen.ts without generation errors.
  artifacts:
    - app/routes/index.tsx
    - app/routes/course.ai.tsx
    - app/routes/course.word.tsx
    - app/routeTree.gen.ts
  key_links:
    - app/routes/index.tsx uses <Link to="/course/ai" search={{ mode: 'pretraining' }} /> with type-safe parameters.
    - app/routes/course.ai.tsx uses <Link to="/course/ai" search={{ mode: 'live-class' }} /> to switch between Pra-Training and Hari-H.
---

<objective>
Implement the Frontpage Hub route (`app/routes/index.tsx`), Agentic AI workspace route (`app/routes/course.ai.tsx`), and Word Processing workspace route (`app/routes/course.word.tsx`) with validated search parameters and type-safe navigation.

Purpose: Deliver the declarative file-based routes for ROUTE-02, ROUTE-03, and ROUTE-04.
Output: `app/routes/index.tsx`, `app/routes/course.ai.tsx`, `app/routes/course.word.tsx`, and generated `app/routeTree.gen.ts`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-file-based-routes-validated-search-params/20-RESEARCH.md
@.planning/phases/20-file-based-routes-validated-search-params/20-PATTERNS.md
@.planning/phases/20-file-based-routes-validated-search-params/20-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Frontpage Hub Route with Validated Filter Chips and Notebook Cards</name>
  <files>app/routes/index.tsx</files>
  <read_first>.planning/phases/20-file-based-routes-validated-search-params/20-PATTERNS.md</read_first>
  <action>
    1. Update `app/routes/index.tsx`:
       - Attach `validateSearch: (search) => homeSearchSchema.parse(search)`.
       - Read active filter via `Route.useSearch()`.
       - Render `#container-home` layout with hero greeting, filter chips (`all`, `ai`, `word`), and notebook cards.
       - Use `<Link>` with typed search parameters for filter switching and course entry.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>TypeScript type error or router route validation mismatch</fails_when>
  </verify>
  <done>Frontpage Hub route compiles with validated search params.</done>
</task>

<task type="auto">
  <name>Task 2: Implement File-Based Workspace Routes for Course AI and Course Word</name>
  <files>app/routes/course.ai.tsx, app/routes/course.word.tsx</files>
  <read_first>.planning/phases/20-file-based-routes-validated-search-params/20-PATTERNS.md</read_first>
  <action>
    1. Create `app/routes/course.ai.tsx`:
       - Validate search params via `courseAiSearchSchema`.
       - Provide route `head()` with page title.
       - Render dual-mode tabs (`pretraining` vs `live-class`) with `<Link>` navigation updating `search={{ mode: ... }}`.
       - Render `#container-course-ai` workspace outline.
    2. Create `app/routes/course.word.tsx`:
       - Validate search params via `courseWordSearchSchema`.
       - Provide route `head()` with page title.
       - Render `#container-course-word` workspace outline with BPSDM banner.
    3. Run `npm run build` or `npm run dev` to let `@tanstack/router-plugin` regenerate `app/routeTree.gen.ts`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
    <fails_when>Route tree generation failure or build error</fails_when>
  </verify>
  <done>Both course workspace routes created and router tree cleanly compiled.</done>
</task>

</tasks>

<threat_model>
| Threat Ref / Vector | Risk Description | Mitigation | Acceptance Gate |
|---|---|---|---|
| T-20-03 Broken Route Links | Typo in route path or invalid search parameters in Link components | Strict TypeScript checking with TanStack Router interface augmentation | `tsc --noEmit` verifies all `<Link>` routes exist |
</threat_model>

<verification>
- [ ] `app/routes/index.tsx` renders Frontpage Hub with typed filter params.
- [ ] `app/routes/course.ai.tsx` renders dual-mode workspace.
- [ ] `app/routes/course.word.tsx` renders Word processing workspace.
- [ ] `app/routeTree.gen.ts` contains registered routes `/`, `/course/ai`, `/course/word`.
- [ ] `npm run typecheck && npm run build` exits 0.
</verification>

<success_criteria>
- All tasks completed successfully.
- Production build succeeds with newly generated route tree.
</success_criteria>

<output>
Create `.planning/phases/20-file-based-routes-validated-search-params/20-02-SUMMARY.md` after execution.
</output>

