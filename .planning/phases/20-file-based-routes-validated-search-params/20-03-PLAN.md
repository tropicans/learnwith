---
phase: 20-file-based-routes-validated-search-params
plan: "03"
type: execute
wave: 3
depends_on:
  - "20-02"
files_modified: []
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
  - ROUTE-04
  - ROUTE-05
must_haves:
  truths:
    - Production Nitro server starts and listens on Port 3173.
    - HTTP GET requests to /, /course/ai?mode=pretraining, /course/ai?mode=live-class, and /course/word return HTTP 200 OK with text/html.
    - All 11 legacy Node.js test suites pass 100% with zero regressions.
  artifacts: []
  key_links:
    - Port 3173 serves all registered file-based routes via TanStack Start progressive SSR streaming.
---

<objective>
Verify end-to-end SSR response codes, query parameter handling, and route navigation on Port 3173, followed by a regression test run on the 11 legacy test suites.

Purpose: Guarantee production stability, routing integrity, and zero regression across the platform.
Output: Verification evidence confirming all ROUTE requirements.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-file-based-routes-validated-search-params/20-RESEARCH.md
@.planning/phases/20-file-based-routes-validated-search-params/20-PATTERNS.md
@.planning/phases/20-file-based-routes-validated-search-params/20-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Production Bundle and Probe SSR Route Endpoints on Port 3173</name>
  <files></files>
  <read_first>.planning/phases/20-file-based-routes-validated-search-params/20-RESEARCH.md</read_first>
  <action>
    1. Compile the production bundle: `npm run build`.
    2. Start the standalone Node server in background: `node .output/server/index.mjs` (with PORT=3173).
    3. Probe each route via HTTP GET:
       - `http://localhost:3173/` -> check HTTP 200 and presence of Frontpage Hub markup.
       - `http://localhost:3173/course/ai?mode=pretraining` -> check HTTP 200 and Agentic AI workspace markup.
       - `http://localhost:3173/course/ai?mode=live-class` -> check HTTP 200 and Hari-H session markup.
       - `http://localhost:3173/course/word` -> check HTTP 200 and Word processing markup.
    4. Terminate test server process cleanly.
  </action>
  <verify>
    <automated>npm run build</automated>
    <fails_when>Build error or failure in route compilation</fails_when>
  </verify>
  <done>All routes respond with HTTP 200 OK and valid HTML on Port 3173.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Legacy CommonJS Test Suite Integrity</name>
  <files></files>
  <read_first>.planning/phases/20-file-based-routes-validated-search-params/20-VALIDATION.md</read_first>
  <action>
    1. Run `npm test` (`node --test tests/*.test.js`).
    2. Verify 11 of 11 suites pass (0 failed, 0 regressions).
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>Any failed unit test suite</fails_when>
  </verify>
  <done>11/11 legacy test suites pass 100%.</done>
</task>

</tasks>

<threat_model>
| Threat Ref / Vector | Risk Description | Mitigation | Acceptance Gate |
|---|---|---|---|
| T-20-04 SSR Route Crashing | Uncaught exceptions during server rendering on specific routes | Try/catch error boundaries and Zod safe parsing for search params | All routes return HTTP 200 OK |
</threat_model>

<verification>
- [ ] `npm run build` succeeds.
- [ ] HTTP probe on Port 3173 returns 200 OK for `/`, `/course/ai`, and `/course/word`.
- [ ] `npm test` passes 11/11 test suites.
</verification>

<success_criteria>
- All tasks completed successfully.
- Full end-to-end SSR routing verified on Port 3173.
- Zero regressions on legacy tests.
</success_criteria>

<output>
Create `.planning/phases/20-file-based-routes-validated-search-params/20-03-SUMMARY.md` after execution.
</output>
