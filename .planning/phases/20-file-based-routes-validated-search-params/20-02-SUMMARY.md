---
phase: 20-file-based-routes-validated-search-params
plan: "02"
type: execute
wave: 2
status: complete
date: 2026-09-08
requirements:
  - ROUTE-02
  - ROUTE-03
  - ROUTE-04
artifacts:
  - app/routes/index.tsx
  - app/routes/course.ai.tsx
  - app/routes/course.word.tsx
  - app/routeTree.gen.ts
---

# Phase 20 Plan 02 Summary: File-Based Routes & Validated Search Params

## Accomplishments
1. **Frontpage Hub Route (`app/routes/index.tsx`)**:
   - Integrated `validateSearch: (search) => homeSearchSchema.parse(search)`.
   - Built NotebookLM-style hero greeting, responsive filter chips (`all`, `ai`, `word`), and interactive notebook cards for Hands-on Agentic AI and Pengolahan Kata Tingkat Lanjut.
   - Wired type-safe `<Link to="/course/ai" search={{ mode: 'pretraining' }} />` and `<Link to="/course/word" search={{}} />`.
2. **Agentic AI Workspace Route (`app/routes/course.ai.tsx`)**:
   - Integrated `validateSearch: (search) => courseAiSearchSchema.parse(search)`.
   - Provided route metadata and dual-mode tabs (`pretraining` vs `live-class`) with typed search navigation.
3. **Word Processing Workspace Route (`app/routes/course.word.tsx`)**:
   - Integrated `validateSearch: (search) => courseWordSearchSchema.parse(search)`.
   - Provided route metadata and BPSDM curriculum header outline.
4. **Route Tree Generation (`app/routeTree.gen.ts`)**:
   - Automatically regenerated by TanStack Router plugin via Vinxi build.
   - Cleanly registers `/`, `/course/ai`, and `/course/word` with full TypeScript interface augmentation.

## Verification
- Route tree generation succeeded in 370ms.
- Full TypeScript typecheck passed with 0 errors.
