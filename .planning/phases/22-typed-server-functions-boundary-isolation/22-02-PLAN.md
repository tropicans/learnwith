---
phase: 22-typed-server-functions-boundary-isolation
plan: "02"
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - app/server/auth.ts
  - app/server/diagnostics.ts
  - tests/server-functions.test.js
autonomous: true
requirements:
  - SRV-01
  - SRV-03
must_haves:
  truths:
    - verifyInstructorPasskeyFn is implemented via createServerFn with POST method and Zod validation.
    - getSystemDiagnosticsFn is implemented via createServerFn with GET method and Zod validation.
    - Server functions handle invalid or malicious inputs gracefully with typed error responses.
    - Automated tests (tests/server-functions.test.js) test both server functions under positive, negative, and edge-case inputs.
    - TypeScript compilation passes with zero errors (npm run typecheck).
  artifacts:
    - app/server/auth.ts
    - app/server/diagnostics.ts
    - tests/server-functions.test.js
  key_links:
    - app/server/auth.ts validates candidate passkeys against app/server/config.ts.
    - app/server/diagnostics.ts returns sanitized runtime health metrics without exposing secrets.
---

<objective>
Implement the typed server functions for instructor passkey verification (`app/server/auth.ts`) and internal system diagnostics/telemetry (`app/server/diagnostics.ts`) using TanStack Start's `createServerFn`, and create comprehensive automated test coverage (`tests/server-functions.test.js`).

Purpose: Fulfill requirements SRV-01 (typed server passkey authentication) and SRV-03 (typed telemetry RPC without exposing raw endpoints).
Output: `app/server/auth.ts`, `app/server/diagnostics.ts`, and `tests/server-functions.test.js`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-typed-server-functions-boundary-isolation/22-RESEARCH.md
@.planning/phases/22-typed-server-functions-boundary-isolation/22-PATTERNS.md
@.planning/phases/22-typed-server-functions-boundary-isolation/22-VALIDATION.md
@.planning/phases/22-typed-server-functions-boundary-isolation/22-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Instructor Passkey Verification Server Function</name>
  <files>app/server/auth.ts</files>
  <read_first>app/server/auth.ts, .planning/phases/22-typed-server-functions-boundary-isolation/22-PATTERNS.md, app/schemas/serverFn.ts, app/server/config.ts</read_first>
  <acceptance_criteria>
    - app/server/auth.ts defines and exports verifyInstructorPasskeyFn using createServerFn({ method: 'POST' })
    - Contains validator that runs verifyPasskeyInputSchema.parse(data)
    - Returns success: true and numeric unlockedAt timestamp when verifyPasskeyWithHash matches
    - Returns success: false when candidate passkey does not match
    - npm run typecheck passes with 0 errors
  </acceptance_criteria>
  <action>
    1. Create app/server/auth.ts.
    2. Import createServerFn from @tanstack/react-start.
    3. Import verifyPasskeyInputSchema, VerifyPasskeyResult from @/schemas/serverFn.
    4. Import verifyPasskeyWithHash from ./config.
    5. Export verifyInstructorPasskeyFn:
       - Method: 'POST'.
       - .validator((data: unknown) => verifyPasskeyInputSchema.parse(data)) for server-side Zod validation.
       - .handler(async ({ data }): Promise<VerifyPasskeyResult> => { ... }).
       - If valid, return { success: true, message: 'Sesi instruktur berhasil diverifikasi.', unlockedAt: Date.now() }.
       - If invalid, return { success: false, message: 'Passkey instruktur tidak valid atau salah.' }.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit code or TypeScript compilation error</fails_when>
  </verify>
  <done>verifyInstructorPasskeyFn implemented via createServerFn with full Zod validation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Internal Diagnostics and Health Telemetry Server Function</name>
  <files>app/server/diagnostics.ts</files>
  <read_first>app/server/diagnostics.ts, .planning/phases/22-typed-server-functions-boundary-isolation/22-PATTERNS.md, app/schemas/serverFn.ts, app/server/config.ts</read_first>
  <acceptance_criteria>
    - app/server/diagnostics.ts defines and exports getSystemDiagnosticsFn using createServerFn({ method: 'GET' })
    - Contains validator that runs diagnosticsInputSchema.parse(data)
    - Returns status: 'healthy', ISO timestamp string, numeric uptimeSeconds, and environment string
    - If includeMemory is true, returns memory object containing rssMb and heapUsedMb
    - Does not leak secret hashes or private environment keys
    - npm run typecheck passes with 0 errors
  </acceptance_criteria>
  <action>
    1. Create app/server/diagnostics.ts.
    2. Import createServerFn from @tanstack/react-start.
    3. Import diagnosticsInputSchema, DiagnosticsResult from @/schemas/serverFn.
    4. Import getServerConfig from ./config.
    5. Export getSystemDiagnosticsFn:
       - Method: 'GET'.
       - .validator((data: unknown) => diagnosticsInputSchema.parse(data || {})).
       - .handler(async ({ data }): Promise<DiagnosticsResult> => { ... }).
       - Compute uptime with Math.floor(process.uptime()).
       - Read memory if data.includeMemory is true (process.memoryUsage()).
       - Return sanitized telemetry metrics without exposing private secret keys or environment variables.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit code or TypeScript compilation error</fails_when>
  </verify>
  <done>getSystemDiagnosticsFn implemented via createServerFn returning sanitized telemetry.</done>
</task>

<task type="auto">
  <name>Task 3: Create Server Functions Unit and Integration Automated Tests</name>
  <files>tests/server-functions.test.js</files>
  <read_first>tests/server-functions.test.js, .planning/phases/22-typed-server-functions-boundary-isolation/22-RESEARCH.md, tests/server-boundary-audit.test.js</read_first>
  <acceptance_criteria>
    - tests/server-functions.test.js exists and runs under node --test
    - Tests positive authentication case for 'ai' ('buka-kelas') and 'word' ('buka-kata')
    - Tests negative authentication case returning success: false
    - Tests input validation rejection for missing or malformed inputs
    - Tests diagnostics telemetry output structure and memory reporting flag
    - node --test tests/server-functions.test.js passes cleanly
  </acceptance_criteria>
  <action>
    1. Create tests/server-functions.test.js using Node.js test runner (node:test, node:assert).
    2. Test suites for verifyInstructorPasskeyFn:
       - Valid passkey 'buka-kelas' for course 'ai' returns { success: true } and numeric unlockedAt.
       - Valid passkey 'buka-kata' for course 'word' returns { success: true }.
       - Invalid passkey 'wrong-code' returns { success: false }.
       - Empty or missing passkey triggers Zod validation rejection.
       - Invalid courseId ('invalid') triggers Zod validation rejection.
    3. Test suites for getSystemDiagnosticsFn:
       - Returns status: 'healthy', ISO timestamp, and numeric uptimeSeconds.
       - When includeMemory: true, returns memory.rssMb and memory.heapUsedMb.
       - Ensures no secrets or raw environment hashes appear anywhere in the returned diagnostic object.
  </action>
  <verify>
    <automated>node --test tests/server-functions.test.js</automated>
    <fails_when>non-zero exit code or test assertion failure</fails_when>
  </verify>
  <done>Automated test suite validates server functions under positive, negative, and validation-failure conditions.</done>
</task>

</tasks>