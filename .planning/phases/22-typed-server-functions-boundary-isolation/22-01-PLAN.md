---
phase: 22-typed-server-functions-boundary-isolation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas/serverFn.ts
  - app/server/config.ts
  - tests/server-boundary-audit.test.js
autonomous: true
requirements:
  - SRV-02
must_haves:
  truths:
    - Server-only environment variables and secret hash verifications are quarantined in app/server/config.ts.
    - crypto.timingSafeEqual is used to prevent timing side-channel attacks during passkey hash validation.
    - Shared Zod schemas (app/schemas/serverFn.ts) strictly define input and output contracts for RPC operations.
    - Automated audit test (tests/server-boundary-audit.test.js) verifies that server secrets, env keys, and native Node modules do not leak into client bundles.
    - TypeScript strict type checking passes with zero errors (npm run typecheck).
  artifacts:
    - app/schemas/serverFn.ts
    - app/server/config.ts
    - tests/server-boundary-audit.test.js
  key_links:
    - app/schemas/serverFn.ts exports schemas consumed by server functions and client callers.
    - app/server/config.ts encapsulates verifyPasskeyWithHash and getServerConfig away from client bundles.
---

<objective>
Establish the quarantined server-only configuration foundation (`app/server/config.ts`), the isomorphic Zod RPC schema contract (`app/schemas/serverFn.ts`), and the automated boundary isolation audit test (`tests/server-boundary-audit.test.js`).

Purpose: Fulfill requirement SRV-02 (isolation of server configuration and secrets from client bundle).
Output: `app/schemas/serverFn.ts`, `app/server/config.ts`, and `tests/server-boundary-audit.test.js`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-typed-server-functions-boundary-isolation/22-RESEARCH.md
@.planning/phases/22-typed-server-functions-boundary-isolation/22-PATTERNS.md
@.planning/phases/22-typed-server-functions-boundary-isolation/22-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Isomorphic Zod Schemas for Server Functions</name>
  <files>app/schemas/serverFn.ts</files>
  <read_first>app/schemas/serverFn.ts, .planning/phases/22-typed-server-functions-boundary-isolation/22-PATTERNS.md, app/schemas/searchParams.ts</read_first>
  <acceptance_criteria>
    - app/schemas/serverFn.ts exports verifyPasskeyInputSchema, verifyPasskeyResultSchema, diagnosticsInputSchema, and diagnosticsResultSchema
    - verifyPasskeyInputSchema validates courseId as enum 'ai' | 'word' and passkey as non-empty string
    - npm run typecheck passes with 0 errors
  </acceptance_criteria>
  <action>
    1. Create app/schemas/serverFn.ts.
    2. Define and export Zod schemas:
       - verifyPasskeyInputSchema: courseId enum ('ai' | 'word'), passkey trimmed string min 1, max 100.
       - verifyPasskeyResultSchema: success boolean, message string, unlockedAt optional number timestamp.
       - diagnosticsInputSchema: includeMemory boolean defaulting to false.
       - diagnosticsResultSchema: status literal ('healthy'), timestamp string, uptimeSeconds number, environment string, memory optional object with rssMb and heapUsedMb.
    3. Export TypeScript inferred types: VerifyPasskeyInput, VerifyPasskeyResult, DiagnosticsInput, DiagnosticsResult.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit code or TypeScript compilation error</fails_when>
  </verify>
  <done>Zod RPC schemas created with full type safety.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Quarantined Server Configuration and Timing-Safe Crypto Validation</name>
  <files>app/server/config.ts</files>
  <read_first>app/server/config.ts, .planning/phases/22-typed-server-functions-boundary-isolation/22-PATTERNS.md, .planning/phases/22-typed-server-functions-boundary-isolation/22-RESEARCH.md</read_first>
  <acceptance_criteria>
    - app/server/config.ts exports getServerConfig and verifyPasskeyWithHash
    - verifyPasskeyWithHash uses crypto.timingSafeEqual on Buffer instances derived from target and candidate SHA-256 hashes
    - Default hash for 'ai' matches SHA-256 of 'buka-kelas' (4452077e60e86b8ee876b509f61b09b52a9261a9953c8965a3c03565e33d26aa)
    - Default hash for 'word' matches SHA-256 of 'buka-kata' (ddf62f4013c59b111215312fb959629155a5b1dc5cf799f2053a8c2395c4511b)
    - npm run typecheck passes with 0 errors
  </acceptance_criteria>
  <action>
    1. Create app/server/config.ts.
    2. Import crypto from node:crypto.
    3. Define default SHA-256 hashes matching legacy test fixtures for backward compatibility:
       - DEFAULT_AI_PASSKEY_HASH for 'buka-kelas'
       - DEFAULT_WORD_PASSKEY_HASH for 'buka-kata'
    4. Implement getServerConfig() reading process.env.AI_PASSKEY_HASH, process.env.WORD_PASSKEY_HASH, process.env.NODE_ENV.
    5. Implement verifyPasskeyWithHash(courseId: 'ai' | 'word', candidate: string): boolean using crypto.timingSafeEqual.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit code or TypeScript compilation error</fails_when>
  </verify>
  <done>app/server/config.ts securely encapsulates passkey hash validation behind timing-safe comparisons.</done>
</task>

<task type="auto">
  <name>Task 3: Create Server Boundary Audit Automated Test</name>
  <files>tests/server-boundary-audit.test.js</files>
  <read_first>tests/server-boundary-audit.test.js, .planning/phases/22-typed-server-functions-boundary-isolation/22-RESEARCH.md, tests/session-security.test.js</read_first>
  <acceptance_criteria>
    - tests/server-boundary-audit.test.js exists and runs under node --test
    - Verifies app/server/config.ts is quarantined and not imported directly by app/routes/*
    - Verifies passkey candidate hash comparison succeeds for valid inputs and fails for invalid inputs
    - node --test tests/server-boundary-audit.test.js passes cleanly
  </acceptance_criteria>
  <action>
    1. Create tests/server-boundary-audit.test.js using Node.js test runner (node:test, node:assert).
    2. Implement test suites:
       - Assert app/server/config.ts exists and is isolated under app/server/.
       - Test verifyPasskeyWithHash with valid passkeys ('buka-kelas', 'buka-kata') returning true, and invalid passkeys returning false.
       - Assert that client source files (app/routes/*, app/components/*) DO NOT directly import app/server/config.ts.
       - Test that secret passkey hash constants are NOT found in client route files.
  </action>
  <verify>
    <automated>node --test tests/server-boundary-audit.test.js</automated>
    <fails_when>non-zero exit code or test assertion failure</fails_when>
  </verify>
  <done>Boundary audit test passes and confirms server quarantine integrity.</done>
</task>

</tasks>