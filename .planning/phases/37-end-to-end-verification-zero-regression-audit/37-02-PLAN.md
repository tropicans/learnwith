---
phase: "37"
plan: "02"
wave: 2
depends_on:
  - "37-01"
files_modified:
  - tests/course-lifecycle-e2e.test.js
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - COURSE-TEST-02
---

# Phase 37 — Plan 02: Platform-Wide Zero-Regression Audit & Milestone Completion Sign-Off

## Executive Summary
Execute Wave 2 of Phase 37 to fulfill requirement [`COURSE-TEST-02`](file:///c:/Users/yudhiar/Downloads/AgenticAI/.planning/REQUIREMENTS.md#L38). Expand [`tests/course-lifecycle-e2e.test.js`](file:///c:/Users/yudhiar/Downloads/AgenticAI/tests/course-lifecycle-e2e.test.js) with Suite 5 (Public Catalog & Reactive Discovery Invariants) and Suite 6 (Direct Workspace Route UX & Passkey Gate Preservation). Run the complete platform test suite across all 33 test suites (targeting ≥ 305 tests passing with 0 failures), verify TypeScript compilation type safety (`tsc --noEmit`), and verify CSS mirror parity between `assets/` and `public/assets/`. Finally, record completion of Phase 37 and sign off on **Milestone v3.3 (Admin Course Lifecycle & Visibility Management)** in project memory documentation ([`REQUIREMENTS.md`](file:///c:/Users/yudhiar/Downloads/AgenticAI/.planning/REQUIREMENTS.md), [`ROADMAP.md`](file:///c:/Users/yudhiar/Downloads/AgenticAI/.planning/ROADMAP.md), and [`STATE.md`](file:///c:/Users/yudhiar/Downloads/AgenticAI/.planning/STATE.md)).

---

<threat_model>
### ASVS Level 1 Threat Modeling

- **Threat Reference:** T-37-03 (Direct Route Passkey Bypass on Unlisted Courses)
  - **Threat Vector:** An unauthenticated visitor visits the direct URL `/course/word` for a hidden/unlisted course, expecting the unlisted status to bypass or skip the passkey gate.
  - **ASVS Control:** ASVS 4.0.3 v2.1.1 (Consistent authentication enforcement across direct and indirect routing).
  - **Mitigation:**
    - `app/routes/course.word.tsx` continues to enforce timing-safe passkey verification (`WordPasskeyModal`) regardless of whether lifecycle status is `active` or `hidden`.
    - Suite 6 explicitly asserts that `isUnlocked` verification remains required on `/course/word`.

- **Threat Reference:** T-37-04 (Platform-Wide Regression & Service Degradation)
  - **Threat Vector:** Changes made during course lifecycle milestone unintentionally break pre-training workflows, live class guides, telemetry ingestion, admin metrics, or file-based routing.
  - **ASVS Control:** ASVS 4.0.3 v1.14.1 (Automated regression testing and configuration integrity).
  - **Mitigation:**
    - Execute all 33 test suites covering 305+ unit and integration tests with zero failure tolerance.
    - Enforce full TypeScript typecheck (`tsc --noEmit`) with 0 errors.
</threat_model>

---

<must_haves>
### Truths
- `tests/course-lifecycle-e2e.test.js` must contain all 6 suites and pass completely.
- Direct route `/course/word` must preserve passkey requirement (`WordPasskeyModal`) when hidden.
- Direct routes for `archived` and `deleted` courses must render `CourseUnavailableNotice` with frontpage navigation CTA.
- The entire platform test suite (`node --test tests/*.test.js`) must pass 100% (33 suites, ≥ 305 tests, 0 failures, 0 skipped, 0 cancelled).
- TypeScript compilation (`npm run typecheck`) must complete with 0 errors.
- `assets/css/components.css` and `public/assets/css/components.css` must remain 100% byte-for-byte identical.
- `.planning/REQUIREMENTS.md` must have all requirements (`COURSE-STATUS-01..03`, `COURSE-ADMIN-01..04`, `COURSE-MUTATE-01..03`, `COURSE-SYNC-01..03`, `COURSE-TEST-01..02`) marked complete `[x]`.
- `.planning/ROADMAP.md` must show Phase 36 and Phase 37 complete, and Milestone v3.3 achieved.
- `.planning/STATE.md` must record Phase 37 and Milestone v3.3 completion.

### Artifacts
- Modified: [`tests/course-lifecycle-e2e.test.js`](file:///c:/Users/yudhiar/Downloads/AgenticAI/tests/course-lifecycle-e2e.test.js) (Suites 5–6 added).
- Modified: [`.planning/REQUIREMENTS.md`](file:///c:/Users/yudhiar/Downloads/AgenticAI/.planning/REQUIREMENTS.md) (All v3.3 requirements marked `[x]`).
- Modified: [`.planning/ROADMAP.md`](file:///c:/Users/yudhiar/Downloads/AgenticAI/.planning/ROADMAP.md) (Phases 34–37 checked off, Milestone v3.3 complete).
- Modified: [`.planning/STATE.md`](file:///c:/Users/yudhiar/Downloads/AgenticAI/.planning/STATE.md) (Milestone v3.3 marked complete).

### Key Links
- [`tests/course-lifecycle-e2e.test.js`](file:///c:/Users/yudhiar/Downloads/AgenticAI/tests/course-lifecycle-e2e.test.js) validates all 6 lifecycle layers.
- [`app/routes/course.ai.tsx`](file:///c:/Users/yudhiar/Downloads/AgenticAI/app/routes/course.ai.tsx) and [`app/routes/course.word.tsx`](file:///c:/Users/yudhiar/Downloads/AgenticAI/app/routes/course.word.tsx) implement direct route notice/banner/passkey contracts.
- [`assets/css/components.css`](file:///c:/Users/yudhiar/Downloads/AgenticAI/assets/css/components.css) and [`public/assets/css/components.css`](file:///c:/Users/yudhiar/Downloads/AgenticAI/public/assets/css/components.css) mirror styles.

### Prohibitions
- DO NOT mark requirements or roadmap complete until full test suite passes with 0 failures.
- DO NOT ignore TypeScript compilation warnings or errors.
- DO NOT edit `assets/css/components.css` without mirroring to `public/assets/css/components.css`.
</must_haves>

---

## Artifacts this phase produces
- [`tests/course-lifecycle-e2e.test.js`](file:///c:/Users/yudhiar/Downloads/AgenticAI/tests/course-lifecycle-e2e.test.js) — Full 6-suite E2E test file.
- [`.planning/REQUIREMENTS.md`](file:///c:/Users/yudhiar/Downloads/AgenticAI/.planning/REQUIREMENTS.md) — 100% completed requirements matrix for v3.3.
- [`.planning/ROADMAP.md`](file:///c:/Users/yudhiar/Downloads/AgenticAI/.planning/ROADMAP.md) — Milestone v3.3 complete roadmap.
- [`.planning/STATE.md`](file:///c:/Users/yudhiar/Downloads/AgenticAI/.planning/STATE.md) — Updated project state reflecting milestone completion.

---

## Tasks

<task id="37-02-01">
  <read_first>
    tests/course-lifecycle-e2e.test.js
    app/server/courseLifecycle.ts
    app/routes/course.ai.tsx
    app/routes/course.word.tsx
    tests/course-lifecycle-sync.test.js
  </read_first>
  <action>
    Append Suite 5 and Suite 6 to `tests/course-lifecycle-e2e.test.js`:
    1. Implement **Suite 5: Public Catalog & Reactive Discovery Invariants**:
       - `it('COURSE-TEST-02: verifies getPublicCoursesListFn and getPublicCourseStatusesFn data projection filtering')`:
         - Test initial state: `getPublicCoursesListFn` returns 2 courses; `getPublicCourseStatusesFn` returns `{ ai: 'active', word: 'active' }`.
         - Transition `ai` to `hidden`: `getPublicCoursesListFn` returns only `['word']`; statuses returns `{ ai: 'hidden', word: 'active' }`.
         - Transition `word` to `archived`: `getPublicCoursesListFn` returns empty array `[]`.
         - Transition `ai` to `deleted`: `getPublicCoursesListFn` remains `[]`.
         - Restore `ai` to `active`: `getPublicCoursesListFn` returns `['ai']`; statuses returns `{ ai: 'active', word: 'archived' }`.
       - `it('COURSE-TEST-02: verifies catalog empty-state and category-empty DOM contract')`:
         - Check that `WorkshopCatalog.tsx` includes `catalog-empty-state` and `catalog-category-empty` markers.
    2. Implement **Suite 6: Direct Workspace Route UX & Passkey Gate Preservation**:
       - `it('COURSE-TEST-02: verifies direct route SSR contracts for unlisted banner and unavailable notices')`:
         - Verify `app/routes/course.ai.tsx` and `app/routes/course.word.tsx` import `CourseUnavailableNotice` and `UnlistedCourseBanner`.
         - Assert that when `status === 'hidden'`, `UnlistedCourseBanner` (id: `unlisted-course-banner`) is rendered above workspace.
         - Assert that when `status === 'archived'` or `status === 'deleted'`, `CourseUnavailableNotice` (id: `course-unavailable-notice`) is rendered.
       - `it('COURSE-TEST-02: guarantees passkey gate preservation on /course/word when hidden')`:
         - Verify that `app/routes/course.word.tsx` checks `isUnlocked` regardless of course lifecycle status.
         - Assert that unlisted courses never bypass the passkey modal (`WordPasskeyModal`).
       - `it('COURSE-TEST-02: verifies byte-for-byte CSS mirror parity')`:
         - Assert `fs.readFileSync('assets/css/components.css').equals(fs.readFileSync('public/assets/css/components.css'))`.
  </action>
  <verify>
    <automated>$env:PATH = "C:\nvm4w\nodejs;$env:PATH"; node --test tests/course-lifecycle-e2e.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/course-lifecycle-e2e.test.js` runs all 6 suites cleanly.
    - Public catalog filtering, empty states, direct route unavailable notices, unlisted banners, and passkey preservation are fully asserted.
    - CSS mirror byte parity assertion passes.
  </acceptance_criteria>
  <done>Suites 5 and 6 added to `tests/course-lifecycle-e2e.test.js`, completing the comprehensive E2E audit suite.</done>
</task>

<task id="37-02-02">
  <read_first>
    package.json
    tests/course-lifecycle-e2e.test.js
  </read_first>
  <action>
    Execute full platform verification and static analysis:
    1. Run all 33 test files:
       `$env:PATH = "C:\nvm4w\nodejs;$env:PATH"; node --test tests/*.test.js`
       Confirm:
       - 33/33 test suites pass.
       - ≥ 305 tests pass.
       - 0 failures, 0 skipped, 0 cancelled.
    2. Run TypeScript compilation:
       `$env:PATH = "C:\nvm4w\nodejs;$env:PATH"; npm run typecheck`
       Confirm:
       - 0 errors (`tsc --noEmit`).
    3. Verify CSS mirror integrity:
       Compare `assets/css/components.css` and `public/assets/css/components.css` using node or powershell file hash / byte comparison.
  </action>
  <verify>
    <automated>$env:PATH = "C:\nvm4w\nodejs;$env:PATH"; node --test tests/*.test.js; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 33 test suites pass with 0 errors.
    - Total test count is ≥ 305 passing tests.
    - `npm run typecheck` passes with exit code 0 and 0 errors.
    - Zero regression confirmed across public workshop workflows, passkey gates, and participant telemetry.
  </acceptance_criteria>
  <done>Full platform verification and static analysis passed with zero regressions.</done>
</task>

<task id="37-02-03">
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </read_first>
  <action>
    Update project memory documentation to mark Phase 37 and Milestone v3.3 complete:
    1. In `.planning/REQUIREMENTS.md`:
       - Mark `COURSE-SYNC-01`, `COURSE-SYNC-02`, `COURSE-SYNC-03`, `COURSE-MUTATE-03` as `[x]`.
       - Mark `COURSE-TEST-01`, `COURSE-TEST-02` as `[x]`.
       - Update Traceability Matrix status column: set Phase 36 and Phase 37 requirements to `Complete`.
    2. In `.planning/ROADMAP.md`:
       - Mark Phase 36 as `[x]` (Complete: 2026-09-10).
       - Mark Phase 37 as `[x]` (Complete: 2026-09-10).
       - Update success criteria checkmarks for Phase 36 (3/3 ✓) and Phase 37 (2/2 ✓).
       - Add Milestone v3.3 to completed milestones list.
    3. In `.planning/STATE.md`:
       - Update frontmatter: `current_phase: 37`, `status: completed`, `completed_phases: 4`, `percent: 100`.
       - Update Milestone Summary: Record completion of Phase 36, Phase 37, and Milestone v3.3 achievement (15/15 requirements delivered).
       - Note final verification numbers (33 test suites, ≥ 305 passing tests, 0 errors).
  </action>
  <verify>
    <automated>$env:PATH = "C:\nvm4w\nodejs;$env:PATH"; node --test tests/course-lifecycle-e2e.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` reflects 15/15 requirements completed.
    - `.planning/ROADMAP.md` reflects 4/4 phases in Milestone v3.3 completed.
    - `.planning/STATE.md` reflects 100% milestone progress.
  </acceptance_criteria>
  <done>Documentation synchronized and Milestone v3.3 signed off as complete.</done>
</task>
