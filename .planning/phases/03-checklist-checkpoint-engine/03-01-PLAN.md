---
phase: 03-checklist-checkpoint-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html, assets/css/components.css, assets/js/app.js, assets/js/state.js]
autonomous: true
requirements: [CHK-01, CHK-04]
must_haves:
  truths:
    - Every practical step across Module 1, 2, 3, and 4 has an interactive checkbox reflecting task completion.
    - Toggling step checkboxes immediately saves state into localStorage and persists on page reload.
    - Sidebar module progress badges (badge-nav-m1, badge-nav-m2, badge-nav-m3, badge-nav-m4) update dynamically in real time (e.g., 2/3, 4/4).
    - Hero completion counter reflects active count of all checked tasks vs total tasks.
  artifacts:
    - index.html
    - assets/css/components.css
    - assets/js/app.js
    - assets/js/state.js
  key_links:
    - Checkbox elements with data-task-id attributes sync automatically with AppState.checklists.
    - updateProgressUI in app.js updates module counter badges and hero stats upon stateChange events.
---

<objective>
Implement interactive step-level checkboxes across all guide modules (Node.js, 9Router, Telegram Bot, Google Cloud), connect them to AppState localStorage synchronization, and display dynamic progress badges for each module in the sidebar.

Purpose: Allow users to track their step-by-step progress through each practical activity with immediate visual reinforcement and persistent browser storage.
Output: Updated `index.html`, `assets/css/components.css`, `assets/js/app.js`, and `assets/js/state.js`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-checklist-checkpoint-engine/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step Checkbox Elements to Modules 1, 2, 3, and 4 in index.html</name>
  <files>index.html</files>
  <read_first>index.html</read_first>
  <action>
    Update `index.html` to embed interactive checklist toggles inside each step section of Modules 1 to 4:
    1. **Module 1 (Node.js)**:
       - Langkah A: Add checkbox with `data-task-id="m1-check-node"` ("Periksa output node --version").
       - Langkah B: Add checkbox with `data-task-id="m1-verify-lts"` ("Instal Node.js LTS .msi 64-bit dan Add to PATH").
       - Langkah C: Add checkbox with `data-task-id="m1-check-npm"` ("Periksa output npm --version").
    2. **Module 2 (9Router)**:
       - Langkah A: Add checkbox with `data-task-id="m2-install-pkg"` ("Jalankan npm install -g 9router").
       - Langkah B: Add checkbox with `data-task-id="m2-start-service"` ("Jalankan 9router di terminal").
       - Langkah C: Add checkbox with `data-task-id="m2-open-dashboard"` ("Buka http://localhost:20128 di browser").
       - Langkah D: Add checkbox with `data-task-id="m2-verify-local"` ("Uji coba login sandi 123456 dan kendali Ctrl+C").
    3. **Module 3 (Telegram Bot)**:
       - Langkah A: Add checkbox with `data-task-id="m3-start-botfather"` ("Buka @BotFather resmi terverifikasi").
       - Langkah B: Add checkbox with `data-task-id="m3-create-newbot"` ("Kirim /newbot dan tentukan nama & username bot").
       - Langkah C: Add checkbox with `data-task-id="m3-save-token-secure"` ("Simpan bot token rahasia secara aman").
       - Langkah E: Add checkbox with `data-task-id="m3-get-userid"` ("Dapatkan nomor Telegram User ID dari @userinfobot").
    4. **Module 4 (Google Cloud)**:
       - Langkah A: Add checkbox with `data-task-id="m4-open-console"` ("Buka https://console.cloud.google.com").
       - Langkah B: Add checkbox with `data-task-id="m4-verify-login"` ("Verifikasi login akun Google latihan").
  </action>
  <verify>Verify `index.html` contains checkboxes with `data-task-id` attributes for `m1-*`, `m2-*`, `m3-*`, and `m4-*`.</verify>
  <acceptance_criteria>
    - All 13 module step checkboxes exist in `index.html` with correct `data-task-id` attributes matching `state.js`.
  </acceptance_criteria>
  <done>Interactive step checkboxes added to Modules 1-4 in index.html.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance CSS for Step Checklist Cards & Module Nav Badges</name>
  <files>assets/css/components.css</files>
  <read_first>assets/css/components.css</read_first>
  <action>
    In `assets/css/components.css`:
    1. Style `.step-checklist-action`: A dedicated, clean micro-card or action row at the bottom of each step section containing:
       - Custom toggle checkbox with `.checklist-checkbox`.
       - Clear label "Tandai langkah ini selesai" / "Langkah selesai".
       - Visual highlight when checked (border changes to subtle green, subtle green background tint).
    2. Style module badge states in sidebar (`.nav-link .badge`):
       - Badge turns `.badge-success` when all steps in that module are completed (e.g. 3/3, 4/4).
       - Badge displays `.badge-primary` when partially completed (e.g. 1/3, 2/4).
       - Badge remains `.badge-neutral` when 0 steps completed.
  </action>
  <verify>Check `assets/css/components.css` for `.step-checklist-action` rules and module badge state transitions.</verify>
  <acceptance_criteria>
    - `.step-checklist-action` styles exist and render cleanly on desktop and mobile.
  </acceptance_criteria>
  <done>CSS styles for step checklist toggles and reactive module badges are implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Reactive Module Badge Counters in JavaScript</name>
  <files>assets/js/app.js, assets/js/state.js</files>
  <read_first>assets/js/app.js</read_first>
  <action>
    1. In `assets/js/state.js`:
       - Add helper method `getModuleProgress(modulePrefix)`: Returns `{ completed, total, percentage }` for tasks starting with given prefix (e.g., `m1-`, `m2-`, `m3-`, `m4-`, `prereq-`).
    2. In `assets/js/app.js`:
       - Update `updateProgressUI()` to calculate and update:
         - `badge-nav-m1`: e.g. `${m1.completed}/${m1.total}` and switch class between `badge-neutral`, `badge-primary`, `badge-success`.
         - `badge-nav-m2`: e.g. `${m2.completed}/${m2.total}`.
         - `badge-nav-m3`: e.g. `${m3.completed}/${m3.total}`.
         - `badge-nav-m4`: e.g. `${m4.completed}/${m4.total}`.
       - Ensure `setupChecklistListeners()` binds to all new step checkboxes, updating visual completed state on the step container and firing `updateProgressUI()`.
  </action>
  <verify>Test checklist toggles update sidebar module badges and persist on refresh.</verify>
  <acceptance_criteria>
    - Checking any step checkbox updates its module badge in the sidebar and overall progress percentage.
    - Reloading the page restores all checked checkboxes.
  </acceptance_criteria>
  <done>JavaScript reactive module badge counters and step checklist listeners are fully connected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify each step in Modules 1-4 has a functional checkbox linked to `AppState`.
- [ ] Verify sidebar badges `badge-nav-m1` through `badge-nav-m4` reactively display completed/total counts.
- [ ] Verify checking/unchecking items preserves state in `localStorage` across page reload.
</verification>

<success_criteria>
- Requirements CHK-01 and CHK-04 fulfilled for step-level module checklists.
- Real-time reactive updates from step checklists to navigation badges and overall progress.
</success_criteria>

<output>
After completion, create `.planning/phases/03-checklist-checkpoint-engine/03-01-SUMMARY.md`
</output>
