---
phase: 03-checklist-checkpoint-engine
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified: [index.html, assets/css/components.css, assets/js/app.js, assets/js/state.js, tests/checkpoint-engine.test.js]
autonomous: true
requirements: [CHK-03, CHK-04]
must_haves:
  truths:
    - Overall readiness evaluates dynamically to either 'SIAP MENGIKUTI WORKSHOP' or 'PERLU TECHNICAL CLINIC' based on checkpoint and checklist states.
    - Prominent readiness summary card displays actionable guidance for both ready participants and those needing technical clinic help.
    - Reset progress button triggers an accessible confirmation modal preventing accidental data loss.
    - Confirming reset clears localStorage, unchecks all checkboxes, sets checkpoints back to pending, resets input fields, and shows feedback toast.
    - Automated tests verify localStorage persistence, readiness calculation rules, and reset behavior.
  artifacts:
    - index.html
    - assets/css/components.css
    - assets/js/app.js
    - assets/js/state.js
    - tests/checkpoint-engine.test.js
  key_links:
    - calculateReadinessStatus() in state.js determines readiness classification.
    - Modal controller in app.js traps focus and handles Escape key dismissals.
    - Automated test suite exercises state.js and app logic headlessly.
---

<objective>
Implement the dynamic Readiness Evaluation Engine ('SIAP MENGIKUTI WORKSHOP' vs 'PERLU TECHNICAL CLINIC'), build the accessible Reset Progress confirmation modal, and create automated tests validating checkpoint state management and calculations.

Purpose: Provide a definitive readiness verdict for workshop participants, protect against accidental data loss with an accessible reset confirmation, and verify correctness with automated test coverage.
Output: Updated `index.html`, `assets/css/components.css`, `assets/js/app.js`, `assets/js/state.js`, and new test suite `tests/checkpoint-engine.test.js`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-checklist-checkpoint-engine/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Dynamic Readiness Status Card & Reset Modal to index.html</name>
  <files>index.html</files>
  <read_first>index.html</read_first>
  <action>
    In `index.html`:
    1. Add **Readiness Summary Card** (`#sec-readiness-summary`) placed strategically after the Checkpoints:
       - Header with title: "Status Kelayakan Pre-Training Peserta".
       - Dynamic Readiness Badge (`#readiness-status-badge`): Large visual pill displaying either:
         - `🎉 SIAP MENGIKUTI WORKSHOP` (Green glowing state)
         - `⚠️ PERLU TECHNICAL CLINIC` (Orange/red alert state)
         - `⏳ MENUNGGU PENYELESAIAN LANGKAH` (Neutral/pending state)
       - Actionable description text (`#readiness-status-desc`) explaining what the participant should do next.
       - Quick action buttons: "Salin Laporan Kesiapan" (links to `#sec-readiness-report`) and "Reset Semua Progres" (`#btn-open-reset-modal`).
    2. Add **Reset Confirmation Modal Dialog** (`#modal-reset-confirm`):
       - Backdrop overlay with blur.
       - Dialog box with warning icon ⚠️, clear heading "Atur Ulang Semua Progres?".
       - Warning message: "Tindakan ini akan mengosongkan semua tanda centang, hasil verifikasi checkpoint, dan informasi peserta yang tersimpan di browser ini."
       - Action buttons: "Batal" (`#btn-cancel-reset`) and "Ya, Reset Progres" (`#btn-confirm-reset`, with `.btn-danger`).
  </action>
  <verify>Check `index.html` contains `#sec-readiness-summary` and `#modal-reset-confirm` with all required IDs.</verify>
  <acceptance_criteria>
    - Readiness summary card and reset modal markup exist in `index.html`.
  </acceptance_criteria>
  <done>Readiness summary card and reset modal markup added to index.html.</done>
</task>

<task type="auto">
  <name>Task 2: Add Readiness Badge & Modal Animation Styles to CSS</name>
  <files>assets/css/components.css</files>
  <read_first>assets/css/components.css</read_first>
  <action>
    In `assets/css/components.css`:
    1. Style `.readiness-status-card`:
       - Large badge styling for `#readiness-status-badge`: prominent typography, padding, pill shape, pulse animation on success.
       - State styles: `.status-ready` (green theme), `.status-clinic` (amber/red theme), `.status-pending` (neutral theme).
    2. Enhance `.modal-backdrop` and `.modal-content`:
       - Smooth fade and scale transitions.
       - Focus outline styles for accessible keyboard navigation.
       - Danger button style `.btn-danger` with hover and active states.
  </action>
  <verify>Check `assets/css/components.css` for readiness badge states and modal transition rules.</verify>
  <acceptance_criteria>
    - CSS classes for readiness badge states and modal interactions are fully styled.
  </acceptance_criteria>
  <done>Readiness status card and modal CSS styling implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Dynamic Readiness Engine & Modal Controller in JavaScript</name>
  <files>assets/js/app.js, assets/js/state.js</files>
  <read_first>assets/js/app.js</read_first>
  <action>
    1. In `assets/js/state.js`:
       - Add `calculateReadiness()` method to `StateManager`:
         - Returns `{ status: 'ready' | 'clinic' | 'pending', label: string, description: string, checkpointsPassed: number, totalCheckpoints: number, tasksPercent: number }`.
         - 'ready': All 3 checkpoints passed (`cp-1`, `cp-2`, `cp-3`) and total tasks completed >= 85%.
         - 'clinic': Any checkpoint is `failed`, OR (checkpoint pending and some failure condition reported).
         - 'pending': Still in progress without failures.
    2. In `assets/js/app.js`:
       - In `updateProgressUI()`: Update `#readiness-status-badge` and `#readiness-status-desc` with output from `AppState.calculateReadiness()`.
       - Add `setupResetModal()`:
         - Open modal when clicking `#btn-open-reset-modal`.
         - Close on `#btn-cancel-reset` or clicking backdrop.
         - Close on Escape key press.
         - On `#btn-confirm-reset`: Call `AppState.resetState()`, reset all DOM checkboxes, reset form inputs, close modal, update UI, and display toast "Semua progres berhasil diatur ulang 🔄".
  </action>
  <verify>Test readiness badge evaluation under all scenarios and verify reset modal clears localStorage and UI.</verify>
  <acceptance_criteria>
    - Marking all checkpoints passed updates badge to 'SIAP MENGIKUTI WORKSHOP'.
    - Marking any checkpoint failed updates badge to 'PERLU TECHNICAL CLINIC'.
    - Reset modal resets all state and updates UI.
  </acceptance_criteria>
  <done>Dynamic readiness calculation and accessible reset modal implemented in JavaScript.</done>
</task>

<task type="auto">
  <name>Task 4: Create Automated Test Suite for State & Checkpoint Engine</name>
  <files>tests/checkpoint-engine.test.js</files>
  <action>
    Create `tests/checkpoint-engine.test.js` containing automated tests for:
    1. LocalStorage state persistence and recovery across simulated restarts.
    2. Checklist task toggle updates and percentage calculations.
    3. Checkpoint status updates ('pending', 'passed', 'failed').
    4. Readiness status rules:
       - All passed + high tasks -> 'ready' (`SIAP MENGIKUTI WORKSHOP`).
       - Any failed -> 'clinic' (`PERLU TECHNICAL CLINIC`).
       - Fresh state -> 'pending'.
    5. State reset restoring all default properties and clearing modified data.
    Provide a lightweight self-contained test runner that runs headlessly via browser or node-compatible assertions.
  </action>
  <verify>Run the test suite script to verify all tests pass.</verify>
  <acceptance_criteria>
    - Automated tests cover all CHK-01, CHK-02, CHK-03, CHK-04 state transitions and pass without failures.
  </acceptance_criteria>
  <done>Automated test suite implemented and passing.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify dynamic readiness badge updates immediately when checkboxes and checkpoints change.
- [ ] Verify 'SIAP MENGIKUTI WORKSHOP' appears when all checkpoints pass.
- [ ] Verify 'PERLU TECHNICAL CLINIC' appears if any checkpoint fails.
- [ ] Verify Reset Modal opens, dismisses with Escape, and resets all progress when confirmed.
- [ ] Verify all automated tests in `tests/checkpoint-engine.test.js` pass.
</verification>

<success_criteria>
- Requirements CHK-03 and CHK-04 fulfilled.
- Real-time readiness verdict with actionable feedback.
- Accessible, robust reset functionality with automated test coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/03-checklist-checkpoint-engine/03-03-SUMMARY.md`
</output>
