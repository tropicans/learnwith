---
phase: 03-checklist-checkpoint-engine
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [index.html, assets/css/components.css, assets/js/app.js, assets/js/state.js]
autonomous: true
requirements: [CHK-02, CHK-04]
must_haves:
  truths:
    - Dedicated interactive Checkpoint sections (#sec-checkpoint-1, #sec-checkpoint-2, #sec-checkpoint-3) exist and anchor cleanly from sidebar navigation.
    - Each checkpoint features interactive Pass ("Lolos Verifikasi ✓") and Fail ("Ada Kendala ✕") verification controls with visual active states.
    - Checkpoint 3 includes interactive form inputs for Telegram Bot Username (@...bot) and Telegram User ID (numbers-only validation) that save to AppState.participantInfo.
    - Checkpoint status badges in both sidebar (status-nav-cp1, status-nav-cp2, status-nav-cp3) and card headers reflect 'pending', 'passed', or 'failed' reactively.
  artifacts:
    - index.html
    - assets/css/components.css
    - assets/js/app.js
    - assets/js/state.js
  key_links:
    - Sidebar checkpoint nav links (#sec-checkpoint-1, #sec-checkpoint-2, #sec-checkpoint-3) scroll smoothly to the respective checkpoint cards.
    - Pass/Fail button clicks trigger AppState.updateCheckpoint() which emits checkpointUpdate events to update badges and calculate readiness.
---

<objective>
Build the dedicated interactive Checkpoint Gates for Checkpoint 1 (Node.js & npm), Checkpoint 2 (9Router Dashboard), and Checkpoint 3 (Telegram Bot & User ID) with pass/fail verification controls, input validation helpers, and reactive badge synchronizations.

Purpose: Provide formal, verifiable milestone gates ensuring participants validate critical installation requirements before proceeding or identifying blockers for technical clinic assistance.
Output: Updated `index.html`, `assets/css/components.css`, `assets/js/app.js`, and `assets/js/state.js`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-checklist-checkpoint-engine/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Interactive Checkpoint Gate Sections to index.html</name>
  <files>index.html</files>
  <read_first>index.html</read_first>
  <action>
    In `index.html`, add full-featured, dedicated interactive sections for Checkpoints 1, 2, and 3:
    1. **Checkpoint 1 Section (`#sec-checkpoint-1`)**:
       - Title: "Gerbang Checkpoint 1: Node.js & npm Siap".
       - Verification criteria checklist: `node --version` output and `npm --version` output.
       - Quick input: Input field for recorded Node.js version (`data-participant-field="nodeVersion"`).
       - Verification actions: Button `btn-cp-pass` ("Lolos Verifikasi ✓") and `btn-cp-fail` ("Ada Kendala ✕") targeting `data-checkpoint="cp-1"`.
       - Direct link to `#sec-troubleshooting` if user encounters an issue.
    2. **Checkpoint 2 Section (`#sec-checkpoint-2`)**:
       - Title: "Gerbang Checkpoint 2: 9Router Dashboard Berjalan".
       - Verification criteria: Terminal 9Router active and browser can open `http://localhost:20128`.
       - Status verification toggles: Button `btn-cp-pass` and `btn-cp-fail` targeting `data-checkpoint="cp-2"`.
    3. **Checkpoint 3 Section (`#sec-checkpoint-3`)**:
       - Title: "Gerbang Checkpoint 3: Bot Telegram & User ID Terverifikasi".
       - Verification criteria: Bot created via BotFather, Start pressed, token saved securely, Telegram User ID recorded.
       - Interactive participant inputs:
         - Telegram Bot Username: Input with validation hint (must end with `bot`, e.g., `@jadwal_budi_bot`) with `data-participant-field="telegramUsername"`.
         - Telegram User ID: Input with numbers-only validation (e.g., `123456789`) with `data-participant-field="telegramUserId"`.
       - Status verification toggles: Button `btn-cp-pass` and `btn-cp-fail` targeting `data-checkpoint="cp-3"`.
  </action>
  <verify>Check `index.html` contains sections with IDs `sec-checkpoint-1`, `sec-checkpoint-2`, and `sec-checkpoint-3`.</verify>
  <acceptance_criteria>
    - Sections `#sec-checkpoint-1`, `#sec-checkpoint-2`, `#sec-checkpoint-3` exist with verification controls and participant input fields.
  </acceptance_criteria>
  <done>Dedicated Checkpoint sections 1, 2, and 3 added to index.html.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Checkpoint Gate & Input Validation Styling in CSS</name>
  <files>assets/css/components.css</files>
  <read_first>assets/css/components.css</read_first>
  <action>
    In `assets/css/components.css`:
    1. Checkpoint Gate Card states:
       - `.checkpoint-gate-card`: Premium glassmorphic border with checkpoint icon badge.
       - `.checkpoint-gate-card.passed`: Glowing green border (`box-shadow: 0 0 20px rgba(16, 185, 129, 0.2)`), green badge.
       - `.checkpoint-gate-card.failed`: Warning/danger border (`box-shadow: 0 0 20px rgba(239, 68, 68, 0.2)`), red badge, shows troubleshooting recommendation.
    2. Checkpoint action buttons (`.cp-action-btn-group`):
       - Pass button: Green accent, active state with checkmark animation.
       - Fail button: Red/warning outline, active state styling.
    3. Form inputs & validators (`.form-group`, `.form-input`, `.form-validation-msg`):
       - Clean dark/light mode form inputs.
       - Visual validation styles (`.is-valid`, `.is-invalid`).
  </action>
  <verify>Verify `assets/css/components.css` contains `.checkpoint-gate-card` state modifiers and form input styles.</verify>
  <acceptance_criteria>
    - CSS styles for checkpoint states, action buttons, and form inputs are defined without errors.
  </acceptance_criteria>
  <done>Checkpoint gate styles and validation UI states implemented in components.css.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Checkpoint Actions & Participant Input Sync in JavaScript</name>
  <files>assets/js/app.js, assets/js/state.js</files>
  <read_first>assets/js/app.js</read_first>
  <action>
    1. In `assets/js/app.js`:
       - Add `setupCheckpointGates()`:
         - Bind click events for pass/fail buttons across all checkpoints (`cp-1`, `cp-2`, `cp-3`).
         - Update `AppState.updateCheckpoint(checkpointId, 'passed' | 'failed')`.
         - Update visual card classes (`.passed`, `.failed`) and button active states.
         - Show toast confirmation when checkpoint status changes.
       - Add `setupParticipantInputs()`:
         - Listen to `input` / `change` on elements with `data-participant-field`.
         - Validate Telegram User ID (numbers only regex: `/^\d+$/`). Show helpful inline feedback if user types non-digits or @.
         - Validate Telegram Bot Username (must end with `bot` or `_bot`, case-insensitive).
         - Save values to `AppState.updateParticipantInfo(field, value)` so they persist in `localStorage`.
         - Restore values on initial load from `AppState.getState().participantInfo`.
    2. Ensure `updateProgressUI()` updates the card-level checkpoint status badges as well as the sidebar badges.
  </action>
  <verify>Test setting checkpoints to passed/failed and saving participant inputs; verify persistence across reload.</verify>
  <acceptance_criteria>
    - Clicking Pass/Fail updates checkpoint status and updates sidebar and card badges.
    - Entering Telegram User ID or Bot Username persists in localStorage.
  </acceptance_criteria>
  <done>Checkpoint gate logic and participant info synchronization wired up in app.js.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify clicking sidebar links for Checkpoints 1, 2, 3 scrolls to their respective interactive cards.
- [ ] Verify Pass/Fail buttons toggle checkpoint state in `AppState` and reflect visually.
- [ ] Verify Telegram User ID numbers-only validator works and saves to `participantInfo`.
- [ ] Verify page reload restores both checkpoint statuses and input values from `localStorage`.
</verification>

<success_criteria>
- Requirements CHK-02 and CHK-04 fulfilled.
- Checkpoints 1, 2, and 3 provide clear validation feedback and data persistence.
</success_criteria>

<output>
After completion, create `.planning/phases/03-checklist-checkpoint-engine/03-02-SUMMARY.md`
</output>
