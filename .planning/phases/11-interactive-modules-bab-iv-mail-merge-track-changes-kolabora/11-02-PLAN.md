---
phase: "11-interactive-modules-bab-iv-mail-merge-track-changes-kolabora"
plan: "02"
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - index.html
  - assets/js/search.js
  - assets/js/app.js
autonomous: true
requirements:
  - WORD-04
  - WORD-05
estimate:
  tokens: 36000
  raw_tokens: 18000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Course 2 container (#container-course-word) provides complete interactive guides for Bab IV with zero placeholder text"
    - "All 8 checklist tasks in Bab IV (word-b4-prepare-source to word-b4-compare-combine) are present with exact data-task-id attributes"
    - "Checkpoint 3 features a functional gate card (#card-word-cp-3) with 5 verification criteria and action buttons (btn-word-cp-3-passed, btn-word-cp-3-failed, btn-word-cp-3-pending)"
    - "Sidebar navigation group (#nav-group-word) includes Bab IV and Checkpoint 3 links with live badges (#badge-nav-word-b4, #status-nav-word-cp3)"
    - "Real-time search indexes Bab IV and Checkpoint 3 content when Course 2 is active"
    - "Mail Merge conditional rules and visual keyboard shortcuts support 1-click clipboard copying"
  artifacts:
    - index.html
    - assets/js/search.js
    - assets/js/app.js
  key_links:
    - "index.html #container-course-word hosts #sec-word-module-4 and #sec-word-cp-3"
    - "index.html #nav-group-word integrates Bab IV links into sidebar navigation and scroll spy"
    - "assets/js/search.js indexes visible Bab IV and Checkpoint 3 DOM elements"
---

<objective>
Implement the production-grade interactive markup, shortcuts, Checkpoint 3 gate card, sidebar navigation, and search indexing for Course 2 Bab IV (Mail Merge, Track Changes & Kolaborasi Dokumen).

This plan delivers:
1. Bab IV (Otomatisasi Dokumen & Kolaborasi) interactive accordion guide (`#sec-word-module-4`) with 8 steps (A to H), 8 checklist tasks (`word-b4-prepare-source` through `word-b4-compare-combine`), Mail Merge Excel schema cards, conditional rule formula cards with 1-click copying, label printing instructions (Tom & Jerry 103), Track Changes walkthroughs, comment threading guidance, Compare/Combine workflow, and Document Inspector sanitization (**WORD-04**, **WORD-05**).
2. Gerbang Checkpoint 3 card (`#sec-word-cp-3` / `#card-word-cp-3`) with 5 verification criteria and status actions (`passed`, `failed`, `pending`) (**WORD-04**).
3. Dedicated sidebar navigation sub-group in `#nav-group-word` with real-time badges (`#badge-nav-word-b4`, `#status-nav-word-cp3`), course-scoped real-time search indexing, and smooth scroll spy integration.

Purpose: Complete the interactive learning experience for Course 2 Bab IV with authentic civil service curriculum content and 100% test verification.
Output: Fully populated `#sec-word-module-4`, `#sec-word-cp-3`, and sidebar links in `index.html`, search synchronization in `assets/js/search.js`, and scroll spy observation in `assets/js/app.js`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-interactive-modules-bab-iv-mail-merge-track-changes-kolabora/11-RESEARCH.md
@.planning/phases/11-interactive-modules-bab-iv-mail-merge-track-changes-kolabora/11-VALIDATION.md
@.planning/phases/11-interactive-modules-bab-iv-mail-merge-track-changes-kolabora/11-PATTERNS.md
@index.html
@assets/js/app.js
@assets/js/search.js
@tests/word-modules.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Markup for Bab IV Accordion Guide (Mail Merge, Track Changes, Compare/Combine & Rules)</name>
  <files>index.html</files>
  <read_first>
    index.html
    .planning/phases/11-interactive-modules-bab-iv-mail-merge-track-changes-kolabora/11-RESEARCH.md
  </read_first>
  <action>
    Add section `#sec-word-module-4` inside `#container-course-word` directly before `#sec-word-diagnosis` (or after `#sec-word-cp-2`):
    1. **Module Header**:
       - Icon badge: `✉️`
       - Title: "Bab IV: Otomatisasi Dokumen & Kolaborasi"
       - Subtitle: "Mail Merge massal, aturan IF-THEN-ELSE, Track Changes, resolusi komentar, dan perbandingan naskah dinas ASN"
       - Meta: Badge "8 Langkah", chevron `▼`
    2. **8 Interactive Steps**:
       - **Langkah A (Persiapan Database Excel & Pembersihan Karakter)**:
         - Standard tabel Excel: Baris 1 sebagai header kolom (`Nama`, `NIP`, `Jabatan`, `Unit_Kerja`, `Alamat`, `Status_Kepegawaian`).
         - Wajib format kolom NIP/NIK sebagai Text (`@`) agar angka 0 di depan tidak terpotong.
         - Larangan merged cells atau baris kosong di dalam tabel data.
         - Checklist: `data-task-id="word-b4-prepare-source"`
       - **Langkah B (Penautan Sumber Data Mailings & Recipients)**:
         - Navigasi menu: `Mailings > Select Recipients > Use an Existing List...`.
         - Memilih file Excel dan memilih sheet data yang sesuai.
         - Verifikasi daftar penerima melalui `Edit Recipient List`.
         - Checklist: `data-task-id="word-b4-setup-mailmerge"`
       - **Langkah C (Penyisipan Merge Fields & Format Tampilan)**:
         - Menyisipkan field: `Mailings > Insert Merge Field` (`<<Nama>>`, `<<NIP>>`, dsb).
         - Format sakelar numerik/tanggal: `\# "000000000000000000"` untuk NIP dan `\@ "dd MMMM yyyy"` untuk tanggal naskah.
         - Tombol 1-klik untuk menyalin contoh format switch.
         - Checklist: `data-task-id="word-b4-insert-fields"`
       - **Langkah D (Penerapan Aturan Kondisional If...Then...Else)**:
         - Menu: `Mailings > Rules > If...Then...Else...`.
         - Contoh aturan: Jika `Status_Kepegawaian` = "PNS", maka "Pegawai Negeri Sipil", jika tidak maka "PPPK".
         - Peringatan krusial: Penulisan kurung kurawal field code wajib menggunakan `Ctrl+F9`, bukan mengetik tanda `{` dan `}` manual.
         - Shortcut toggle tampilan field codes: `Alt+F9`.
         - 1-klik tombol salin sintaks aturan.
         - Checklist: `data-task-id="word-b4-merge-rules"`
       - **Langkah E (Pencetakan Label Massal & Aturan Next Record)**:
         - Format label Tom & Jerry No. 103 (ukuran kertas, margin, tabel kisi).
         - Menyisipkan rule `{ NEXT }` / `Rules > Next Record` pada setiap sel label setelah record pertama agar tidak terjadi duplikasi penerima yang sama.
         - Menjalankan `Update Labels` untuk mereplikasi format ke seluruh halaman.
         - Preview Results dan `Finish & Merge > Edit Individual Documents`.
         - Checklist: `data-task-id="word-b4-preview-finish"`
       - **Langkah F (Pelacakan Perubahan Dokumen / Track Changes)**:
         - Mengaktifkan pelacakan via menu `Review > Track Changes` atau pintasan <kbd>Ctrl</kbd> + <kbd>Shift</kbd> + <kbd>E</kbd>.
         - Menjelaskan 4 mode tampilan markup: *Simple Markup*, *All Markup*, *No Markup*, dan *Original*.
         - Mengunci pelacakan dengan sandi: `Review > Tracking > Lock Tracking` untuk mencegah peserta lain menonaktifkan pelacakan tanpa izin.
         - Checklist: `data-task-id="word-b4-track-changes"`
       - **Langkah G (Manajemen Komentar Review & Thread Diskusi ASN)**:
         - Menyisipkan komentar baru (<kbd>Ctrl</kbd> + <kbd>Alt</kbd> + <kbd>M</kbd>).
         - Penggunaan Modern Comments: Menanggapi komentar (Reply thread), mengarahkan ke kolega (@mention), dan menandai *Resolve* setelah revisi dilakukan.
         - Larangan menghapus komentar instruktur/atasan secara sepihak sebelum ditelaah.
         - Checklist: `data-task-id="word-b4-comments-resolve"`
       - **Langkah H (Perbandingan Dokumen & Pembersihan Metadata)**:
         - Menu `Review > Compare > Compare Documents` untuk membandingkan *Original Document* vs *Revised Document*.
         - Menggunakan *Reviewing Pane* (Vertical/Horizontal) untuk merekapitulasi seluruh perbedaan revisi.
         - Menggabungkan beberapa masukan reviewer via `Review > Compare > Combine`.
         - **Pembersihan Metadata Wajib**: `File > Info > Check for Issues > Inspect Document` untuk menghapus komentar lama, riwayat revisi, dan informasi pribadi sebelum naskah dinas resmi ditandatangani atau diunggah ke publik.
         - Checklist: `data-task-id="word-b4-compare-combine"`
    3. **Civil Service Governance & Privacy Callout**:
       - Ringkasan kepatuhan Pergub DKI No. 14/2020: Larangan membagikan berkas draft yang memuat data pribadi ASN (NIK, NIP, Gaji, Rekening) melalui kanal publik tanpa enkripsi.
  </action>
  <acceptance_criteria>
    - `index.html` contains section `#sec-word-module-4`
    - `#sec-word-module-4` contains 8 checkbox inputs with `data-task-id="word-b4-*"`
    - Step D includes copyable formula for `If...Then...Else...` rule and `Ctrl+F9` warning
    - Step E details `{ NEXT }` rule for label 103 printing
    - Step F includes `Ctrl+Shift+E` Track Changes and Lock Tracking instructions
    - Step H includes Document Inspector metadata scrubbing guidance
  </acceptance_criteria>
  <verify>
    <automated>cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-modules.test.js"</automated>
    <fails_when>non-zero exit code or failing assertions in tests/word-modules.test.js</fails_when>
  </verify>
  <done>
    Section `#sec-word-module-4` is present in `index.html` with all 8 steps, checklists, formula cards, and governance callouts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Markup for Checkpoint 3 Gate Card (Otomatisasi & Kolaborasi Dokumen)</name>
  <files>index.html</files>
  <read_first>
    index.html
    .planning/phases/11-interactive-modules-bab-iv-mail-merge-track-changes-kolabora/11-PATTERNS.md
  </read_first>
  <action>
    Add section `#sec-word-cp-3` directly following `#sec-word-module-4` inside `#container-course-word`:
    1. Card `#card-word-cp-3` (class `checkpoint-gate-card`).
    2. Header:
       - Icon: `📍`
       - Title: "Gerbang Checkpoint 3: Otomatisasi & Kolaborasi Dokumen"
       - Subtitle: "Verifikasi Kelayakan Mail Merge, Logika Kondisional, Track Changes, dan Perbandingan Naskah Dinas ASN"
       - Status badge: `#status-card-word-cp3` (class `badge badge-pill badge-warning`, default text `Pending`)
    3. 5 Verification Criteria:
       - **Kriteria 1**: Database sumber Excel telah disiapkan dengan header baris pertama, format teks untuk NIP/NIK, dan bebas dari baris kosong atau merged cells.
       - **Kriteria 2**: Field Mail Merge (`<<Nama>>`, `<<NIP>>`, `<<Jabatan>>`) dan aturan logika kondisional (`If...Then...Else`) berfungsi dinamis menghasilkan output surat/undangan yang akurat.
       - **Kriteria 3**: Fitur Track Changes diaktifkan dan dikunci dengan sandi (*Lock Tracking*) saat naskah dinas diedit bersama tim penelaah.
       - **Kriteria 4**: Seluruh komentar telaah (*Modern Comments*) telah ditanggapi dan ditandai *Resolve* tanpa ada isu yang menggantung.
       - **Kriteria 5**: Dokumen telah diverifikasi perbedaannya menggunakan fitur *Compare/Combine*, dan seluruh metadata rahasia telah dibersihkan melalui *Document Inspector*.
    4. Checkpoint Actions Container:
       - Button "Tandai Lulus Verifikasi" (`#btn-word-cp-3-passed`, class `btn btn-success btn-sm btn-cp-action`, `data-status="passed"`)
       - Button "Tandai Ada Kendala" (`#btn-word-cp-3-failed`, class `btn btn-outline-danger btn-sm btn-cp-action`, `data-status="failed"`)
       - Button "Reset Status" (`#btn-word-cp-3-pending`, class `btn btn-outline-secondary btn-sm btn-cp-action`, `data-status="pending"`)
  </action>
  <acceptance_criteria>
    - `index.html` contains section `#sec-word-cp-3` and card `#card-word-cp-3`
    - `#card-word-cp-3` contains `#status-card-word-cp3`
    - `#card-word-cp-3` contains 5 verification criteria
    - Buttons `#btn-word-cp-3-passed`, `#btn-word-cp-3-failed`, and `#btn-word-cp-3-pending` exist with `btn-cp-action` class
  </acceptance_criteria>
  <verify>
    <automated>cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-modules.test.js"</automated>
    <fails_when>non-zero exit code or failing assertions in tests/word-modules.test.js</fails_when>
  </verify>
  <done>
    Section `#sec-word-cp-3` and card `#card-word-cp-3` are present with all 5 criteria and functional action buttons.
  </done>
</task>

<task type="auto">
  <name>Task 3: Sidebar Navigation Integration, Search Engine Sync & Full Verification</name>
  <files>index.html, assets/js/search.js, assets/js/app.js</files>
  <read_first>
    index.html
    assets/js/search.js
    assets/js/app.js
  </read_first>
  <action>
    1. In `index.html`, inside `<div id="nav-group-word">`, add the Bab IV sub-group directly before `<nav class="nav-group" aria-label="Navigasi Solusi Kendala Dokumen">`:
       ```html
       <!-- Sub-Group: Bab IV Otomatisasi & Checkpoint 3 -->
       <nav class="nav-group" aria-label="Navigasi Otomatisasi & Kolaborasi">
         <div class="nav-group-title">Bab IV: Mail Merge & Kolaborasi</div>
         <a href="#sec-word-module-4" class="nav-link">
           <div class="nav-link-content">
             <span class="nav-link-icon">✉️</span>
             <span>Modul Bab IV: Merge & Review</span>
           </div>
           <span class="badge badge-pill badge-neutral" id="badge-nav-word-b4">0/8</span>
         </a>
         <a href="#sec-word-cp-3" class="nav-link">
           <div class="nav-link-content">
             <span class="nav-link-icon">📍</span>
             <span>Checkpoint 3: Bab IV</span>
           </div>
           <span class="badge badge-pill badge-warning" id="status-nav-word-cp3">Pending</span>
         </a>
       </nav>
       ```
    2. In `assets/js/search.js`, verify that the search indexer automatically processes all headings, subtitles, and checklist descriptions within `#container-course-word` when Course 2 is active, covering `#sec-word-module-4` and `#sec-word-cp-3`.
    3. In `assets/js/app.js`, verify that `updateActiveNavOnScroll()` includes `#sec-word-module-4` and `#sec-word-cp-3` for smooth scroll spy tracking.
    4. Run full project test suite to verify 100% compliance across all suites:
       ```powershell
       cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-modules.test.js && node tests/multi-course.test.js && node tests/checkpoint-engine.test.js"
       ```
  </action>
  <acceptance_criteria>
    - `#nav-group-word` includes links to `#sec-word-module-4` and `#sec-word-cp-3`
    - `#badge-nav-word-b4` and `#status-nav-word-cp3` exist in sidebar
    - `tests/word-modules.test.js` passes with 0 failures
    - `tests/multi-course.test.js` passes with 0 failures
    - `tests/checkpoint-engine.test.js` passes with 0 failures
  </acceptance_criteria>
  <verify>
    <automated>cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-modules.test.js && node tests/multi-course.test.js && node tests/checkpoint-engine.test.js"</automated>
    <fails_when>non-zero exit code or failing assertions across any of the 3 test suites</fails_when>
  </verify>
  <done>
    Full multi-course automated test suites pass with zero failures. Sidebar navigation, search indexing, and Checkpoint 3 gate interactions operate smoothly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| DOM Static Content Injection | Statically embedded Mail Merge formula syntax, code blocks, and instruction cards in `index.html` |
| Navigation & Scroll Spy | Event tracking and hash navigation linking sidebar anchors to document sections |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-04 | Tampering | DOM Code Callouts | low | mitigate | All Mail Merge field code snippets and IF-THEN-ELSE syntax examples in `index.html` are statically hardcoded text inside `<code>` and `<pre>` elements with strict HTML entity escaping, eliminating runtime execution vectors |
| T-11-05 | Information Disclosure | Practice Data Guidance | medium | mitigate | All practice examples use synthetic placeholder data (e.g. "Nama Peserta", "Unit Kerja BPSDM", "198501012010011001") and explicitly forbid real NIP/NIK/salary records in practice files |
| T-11-06 | Denial of Service | Responsive Viewport Layout | low | mitigate | All tables, diff review callouts, and checkpoint action buttons employ fluid grid/flex layouts with mobile-safe padding and touch-friendly button targets (>= 44px) |
</threat_model>

<verification>
Run the automated test suite:
`cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-modules.test.js && node tests/multi-course.test.js && node tests/checkpoint-engine.test.js"`
</verification>

<success_criteria>
- Section `#sec-word-module-4` contains 8 interactive steps and checkboxes.
- Section `#sec-word-cp-3` contains `#card-word-cp-3` with 5 verification criteria and action buttons.
- `#nav-group-word` includes Bab IV and Checkpoint 3 navigation links and live badges.
- All unit and integration test suites pass with 0 failures.
</success_criteria>

## Artifacts this phase produces
- `#sec-word-module-4`
- `#sec-word-cp-3`
- `#card-word-cp-3`
- `#status-card-word-cp3`
- `#badge-nav-word-b4`
- `#status-nav-word-cp3`
