---
phase: 24-pre-training-foundation-sections-data-extraction
plan: "02"
type: execute
wave: 2
depends_on:
  - "24-01"
files_modified:
  - app/routes/course.ai.tsx
  - test/pretraining-foundation.test.js
autonomous: true
requirements:
  - PRE-BASE-01
  - PRE-BASE-02
  - PRE-BASE-03
  - PRE-BASE-04
  - PRE-BASE-05
must_haves:
  truths:
    - Route `/course/ai?mode=pretraining` renders the full foundation sections (Hero, Target & Alur, Glosarium, Security Rules, Alat & Persiapan, and PowerShell Guide) in correct sequence above the curriculum modules.
    - All 5 section IDs (`sec-target`, `sec-glosarium`, `sec-security`, `sec-prerequisites`, `sec-powershell`) are present and accessible in the DOM.
    - Automated unit test suite `test/pretraining-foundation.test.js` passes 100% with assertions verifying IDs, headers, and essential copy.
    - Existing test suite (16 test suites) passes with zero regression.
  artifacts:
    - app/routes/course.ai.tsx
    - test/pretraining-foundation.test.js
  key_links:
    - `app/routes/course.ai.tsx` imports and renders foundation components conditionally when `mode === 'pretraining'`.
    - `test/pretraining-foundation.test.js` tests the foundation data and server rendering output.
---

<objective>
Integrate the extracted foundation components into `app/routes/course.ai.tsx` for `mode === 'pretraining'`, verify visual styling and DOM structure parity with `index.html`, create a dedicated test suite `test/pretraining-foundation.test.js`, and verify 100% test pass rate with zero regression across all existing suites.

Purpose: Complete Phase 24 deliverables and validate that all 5 foundation requirements are fully operational in TanStack Start.
Output: Updated `course.ai.tsx` and passing test suite in `test/pretraining-foundation.test.js`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-pre-training-foundation-sections-data-extraction/24-01-PLAN.md
@app/routes/course.ai.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Foundation Components into course.ai.tsx</name>
  <files>app/routes/course.ai.tsx</files>
  <read_first>
    app/routes/course.ai.tsx
    app/components/course/pretraining/PretrainingHero.tsx
    app/components/course/pretraining/PretrainingTargetSection.tsx
    app/components/course/pretraining/PretrainingGlossarySection.tsx
    app/components/course/pretraining/PretrainingSecuritySection.tsx
    app/components/course/pretraining/PretrainingPrerequisitesSection.tsx
    app/components/course/pretraining/PretrainingPowerShellSection.tsx
  </read_first>
  <action>
    In `app/routes/course.ai.tsx`:
    1. Import the foundation components from `@/components/course/pretraining/`.
    2. When `mode === 'pretraining'`, render:
       - `<PretrainingHero />`
       - `<PretrainingTargetSection />`
       - `<PretrainingGlossarySection />`
       - `<PretrainingSecuritySection />`
       - `<PretrainingPrerequisitesSection />`
       - `<PretrainingPowerShellSection />`
       above the modules syllabus / streaming stats.
    3. Ensure all container classes (`content-section`, `card`, `card-glass`, `step-container`, `checklist-group`, `alert-box`) seamlessly pick up styles from `assets/css/main.css` and `assets/css/components.css`.
  </action>
  <verify>
    <automated>pwsh -Command "& 'C:\nvm4w\nodejs\node.exe' -e \"require('child_process').execSync('npx tsc --noEmit', { stdio: 'inherit' })\""</automated>
    <fails_when>non-zero exit or TypeScript type error</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Create Automated Unit & Regression Tests</name>
  <files>test/pretraining-foundation.test.js</files>
  <read_first>
    app/data/pretrainingFoundation.ts
    test/route-loaders.test.js
  </read_first>
  <action>
    1. Create `test/pretraining-foundation.test.js` using Node.js built-in `node:test` and `node:assert`.
    2. Verify `pretrainingFoundation.ts` data exports contain:
       - 3 target criteria with non-empty titles and descriptions.
       - At least 8 glossary terms including 'Browser', 'PowerShell / Terminal', 'Node.js & npm', '9Router', 'Token / API Key'.
       - 4 security protocols with strict warning copy.
       - 7 prerequisites checklist items with unique task IDs.
       - 3 PowerShell setup steps.
    3. Verify that `app/routes/course.ai.tsx` imports and references all 5 section IDs (`sec-target`, `sec-glosarium`, `sec-security`, `sec-prerequisites`, `sec-powershell`).
    4. Run all unit test suites to verify 0 regressions.
  </action>
  <verify>
    <automated>& 'C:\nvm4w\nodejs\node.exe' test/pretraining-foundation.test.js</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Full Test Suite Zero-Regression Verification</name>
  <files>test/</files>
  <read_first>package.json</read_first>
  <action>
    Run the complete testing suite (`npm test` or `node --test test/*.test.js`) to guarantee that all 17+ test suites pass 100% with zero regressions.
  </action>
  <verify>
    <automated>pwsh -Command "& 'C:\nvm4w\nodejs\node.exe' --test (Get-ChildItem test/*.test.js | Select-Object -ExpandProperty FullName)"</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
</task>

</tasks>

<verification>
Automated verification commands check that `tsc --noEmit` passes, `test/pretraining-foundation.test.js` passes, and the entire test directory runs with 100% success.
</verification>

<must_haves>
  <truths>
    - All 5 foundation sections are rendered in `app/routes/course.ai.tsx` during `mode === 'pretraining'`.
    - No SSR hydration mismatch errors occur.
    - Zero regressions across existing test suites.
  </truths>
  <artifacts>
    - app/routes/course.ai.tsx
    - test/pretraining-foundation.test.js
  </artifacts>
</must_haves>
