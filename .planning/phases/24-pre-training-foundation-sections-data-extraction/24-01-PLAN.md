---
phase: 24-pre-training-foundation-sections-data-extraction
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/data/pretrainingFoundation.ts
  - app/components/course/pretraining/PretrainingHero.tsx
  - app/components/course/pretraining/PretrainingTargetSection.tsx
  - app/components/course/pretraining/PretrainingGlossarySection.tsx
  - app/components/course/pretraining/PretrainingSecuritySection.tsx
  - app/components/course/pretraining/PretrainingPrerequisitesSection.tsx
  - app/components/course/pretraining/PretrainingPowerShellSection.tsx
autonomous: true
requirements:
  - PRE-BASE-01
  - PRE-BASE-02
  - PRE-BASE-03
  - PRE-BASE-04
  - PRE-BASE-05
must_haves:
  truths:
    - Structured typed dataset in `app/data/pretrainingFoundation.ts` extracts and defines all pre-training target criteria, glossary items, security rules, prerequisites, and PowerShell instructions with 100% fidelity to `index.html`.
    - Modular React 19 components render each foundation section with matching HTML ids (`sec-target`, `sec-glosarium`, `sec-security`, `sec-prerequisites`, `sec-powershell`) and design elements (alert boxes, step badges, cards).
    - TypeScript strict type checking passes with 0 errors.
  artifacts:
    - app/data/pretrainingFoundation.ts
    - app/components/course/pretraining/PretrainingHero.tsx
    - app/components/course/pretraining/PretrainingTargetSection.tsx
    - app/components/course/pretraining/PretrainingGlossarySection.tsx
    - app/components/course/pretraining/PretrainingSecuritySection.tsx
    - app/components/course/pretraining/PretrainingPrerequisitesSection.tsx
    - app/components/course/pretraining/PretrainingPowerShellSection.tsx
  key_links:
    - `app/data/pretrainingFoundation.ts` provides immutable content objects consumed by the pre-training presentation components.
    - `app/components/course/pretraining/` components isolate the foundation visual blocks ready for composition inside `course.ai.tsx`.
---

<objective>
Extract the curriculum text, glossary terms, security rules, prerequisites checklist, and PowerShell guidance from `index.html` into a strongly-typed data layer (`app/data/pretrainingFoundation.ts`), and build modular React 19 presentation components for each section.

Purpose: Provide full-fidelity foundation sections matching `index.html` lines 793–1082 for requirements PRE-BASE-01 through PRE-BASE-05.
Output: Strongly typed data module and 6 presentation components in `app/components/course/pretraining/`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract and define strongly typed pre-training foundation dataset</name>
  <files>app/data/pretrainingFoundation.ts</files>
  <read_first>index.html</read_first>
  <action>
    Create `app/data/pretrainingFoundation.ts` extracting:
    1. Hero stats: 45–75 mnt (Estimasi Waktu), 3 Gerbang (Checkpoint Mandiri), 0 Coding (Pemula Friendly), 0/13 or 0/20 (Langkah Selesai).
    2. Target criteria: 3 steps (9Router Terpasang, Dashboard Lokal Terbuka di port 20128, Bot Telegram Dibuat & User ID Dicatat) + alert box reminder regarding Hermes Agent in-class configuration.
    3. Glosarium items: 8+ definitions (Browser, PowerShell/Terminal, Node.js & npm, 9Router, Token/API Key, BotFather, Telegram User ID, Checkpoint).
    4. Security rules: 4 mandatory rules (dilarang kirim token ke grup, dilarang screenshot token terbuka, dilarang ubah proxy/firewall kantor, wajib sensor error) + sample token masking (`sk-ant-api03-****-rahasia`).
    5. Prerequisites items: 7 checklist items with IDs (`prereq-laptop`, `prereq-charger`, `prereq-internet`, `prereq-browser`, `prereq-admin`, `prereq-telegram`, `prereq-google`).
    6. PowerShell guide: 3 steps (Buka Start Menu, Ketik PowerShell, Memahami Prompt `PS C:\Users\NamaAnda>`) + copy/paste mouse guide alert.
    Export interfaces and data constants with TypeScript strict typing.
  </action>
  <verify>
    <automated>pwsh -Command "& 'C:\nvm4w\nodejs\node.exe' -e \"import('./app/data/pretrainingFoundation.ts').catch(() => { require('esbuild').buildSync({ entryPoints: ['app/data/pretrainingFoundation.ts'], write: false }); console.log('Foundation data compiled successfully'); })\""</automated>
    <fails_when>non-zero exit or syntax/type compile error</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Build PretrainingHero and Target & Alur Component</name>
  <files>
    app/components/course/pretraining/PretrainingHero.tsx
    app/components/course/pretraining/PretrainingTargetSection.tsx
  </files>
  <read_first>
    app/data/pretrainingFoundation.ts
    index.html
  </read_first>
  <action>
    1. Create `app/components/course/pretraining/PretrainingHero.tsx` rendering `.main-hero`, `.hero-tag`, `.hero-title`, `.hero-subtitle`, and `.hero-stats-grid` with stat icons and labels.
    2. Create `app/components/course/pretraining/PretrainingTargetSection.tsx` with `id="sec-target"`, section title, step badges 1-3, `.step-container`, and `.alert-box.alert-info` matching `index.html`.
  </action>
  <verify>
    <automated>pwsh -Command "Test-Path app/components/course/pretraining/PretrainingHero.tsx, app/components/course/pretraining/PretrainingTargetSection.tsx"</automated>
    <fails_when>returns False</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Build Glossary, Security, Prerequisites, and PowerShell Components</name>
  <files>
    app/components/course/pretraining/PretrainingGlossarySection.tsx
    app/components/course/pretraining/PretrainingSecuritySection.tsx
    app/components/course/pretraining/PretrainingPrerequisitesSection.tsx
    app/components/course/pretraining/PretrainingPowerShellSection.tsx
  </files>
  <read_first>
    app/data/pretrainingFoundation.ts
    index.html
  </read_first>
  <action>
    1. Create `PretrainingGlossarySection.tsx` with `id="sec-glosarium"`, rendering `.card-grid#glossary-container` with interactive term cards, icons, and definitions.
    2. Create `PretrainingSecuritySection.tsx` with `id="sec-security"`, checklist group of 4 security protocols with locked checked checkboxes, and `.alert-box.alert-warning` showing before/after token masking.
    3. Create `PretrainingPrerequisitesSection.tsx` with `id="sec-prerequisites"`, checklist items for the 7 hardware/software prerequisites using client-side interactive state with `localStorage` persistence under key `learnwith_ai_prereq`.
    4. Create `PretrainingPowerShellSection.tsx` with `id="sec-powershell"`, step badges 1-3, `.alert-box.alert-info` with copy/paste instructions, and clean code callouts.
  </action>
  <verify>
    <automated>pwsh -Command "Test-Path app/components/course/pretraining/PretrainingGlossarySection.tsx, app/components/course/pretraining/PretrainingSecuritySection.tsx, app/components/course/pretraining/PretrainingPrerequisitesSection.tsx, app/components/course/pretraining/PretrainingPowerShellSection.tsx"</automated>
    <fails_when>returns False</fails_when>
  </verify>
</task>

</tasks>

<verification>
Automated verification commands check that all 7 files exist and compile without syntax errors.
</verification>

<must_haves>
  <truths>
    - All 5 foundation sections render with exact IDs from index.html: sec-target, sec-glosarium, sec-security, sec-prerequisites, sec-powershell.
    - Prerequisite checkboxes persist state in localStorage without throwing SSR hydration mismatches (wrapped in ClientOnly or client-side useEffect).
  </truths>
  <artifacts>
    - app/data/pretrainingFoundation.ts
    - app/components/course/pretraining/PretrainingHero.tsx
    - app/components/course/pretraining/PretrainingTargetSection.tsx
    - app/components/course/pretraining/PretrainingGlossarySection.tsx
    - app/components/course/pretraining/PretrainingSecuritySection.tsx
    - app/components/course/pretraining/PretrainingPrerequisitesSection.tsx
    - app/components/course/pretraining/PretrainingPowerShellSection.tsx
  </artifacts>
</must_haves>
