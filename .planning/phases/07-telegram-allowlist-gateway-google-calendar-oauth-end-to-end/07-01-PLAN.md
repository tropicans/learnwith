---
phase: "07"
plan: "01"
title: "Modul 9 Telegram Gateway Setup, Allowlist Enforcement & Checkpoint 7 Gate"
wave: 1
dependencies: []
requirements:
  - CLASS-04
  - GATE-04
files_modified:
  - index.html
  - assets/js/state.js
  - assets/js/app.js
autonomous: true
must_haves:
  truths:
    - Modul 9 provides comprehensive step-by-step guidance for running `hermes gateway setup`, choosing Telegram, entering local bot token and numeric allowed user ID, and managing gateway lifecycle (`hermes gateway start`, `status`, `stop`).
    - Long-polling mode is clearly emphasized over webhooks for safe local workshop operation without public tunnels or domain setups.
    - Security callout warns against pasting bot tokens or allowed user IDs into Zoom chat or sharing screens with tokens visible.
    - Checkpoint 7 card is fully rendered in Modul 9 with Pass, Fail, and Reset buttons (`data-checkpoint="cp-7"`).
    - StateManager is extended with Modul 9 checklist items (`m9-run-setup`, `m9-select-telegram`, `m9-enter-token`, `m9-enter-allowed`, `m9-start-gateway`, `m9-check-status`, `m9-verify-dm`) and Checkpoint 7 (`cp-7`).
    - Checkpoint gates controller and sidebar badge counts in `app.js` seamlessly handle `cp-7`.
  artifacts:
    - path: index.html
      provides: Rich interactive section for #sec-module-9 and #card-cp-7 checkpoint gate
    - path: assets/js/state.js
      provides: Default state tracking for m9 checklist tasks and cp-7 checkpoint
    - path: assets/js/app.js
      provides: Updated setupCheckpointGates() and sidebar status indicators handling cp-7
  key_links:
    - from: index.html
      to: assets/js/app.js
      via: data-task-id attributes and data-checkpoint attributes
    - from: assets/js/app.js
      to: assets/js/state.js
      via: updateCheckpoint and getModuleProgress calls for Modul 9
---

<objective>
Implement interactive guide content for Modul 9 (Integrasi Telegram Gateway & Allowlist) and Checkpoint 7 Gate in `index.html`, extend `StateManager` to track Modul 9 checklists and Checkpoint 7, and update `app.js` checkpoint handling and sidebar counters.

Purpose: Guide participants through connecting their Telegram bot to Hermes with strict allowlist user ID filtering via long polling, and verify the connection with Checkpoint 7.
Output: Production markup in `index.html`, updated `state.js`, and updated `app.js`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@PANDUAN-PRAKTIK-KELAS.md
@index.html
@assets/js/state.js
@assets/js/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Modul 9 markup and Checkpoint 7 gate card</name>
  <files>index.html</files>
  <action>
    1. In `index.html`, locate `#sec-module-9` in `#container-liveclass`.
    2. Replace the placeholder with a full interactive module container featuring:
       - Module header: "Modul 9: Integrasi Telegram Gateway & Allowlist", icon 🤖, target duration (20 Menit).
       - Context callout: Explain how Hermes Gateway bridges Telegram messages to the autonomous agent, why local long-polling is used (no public webhook, no ngrok/tunnel needed), and why `TELEGRAM_ALLOWED_USERS` is mandatory to prevent unauthorized access.
       - Step 1: Running the interactive wizard with 1-click copy:
         `hermes gateway setup`
       - Step 2: Choosing Telegram as the messaging platform when prompted.
       - Step 3: Entering Telegram Bot Token (from BotFather) and numeric Telegram User ID (from @userinfobot) with security warning against sharing in Zoom chat.
       - Configuration reference box:
         ```text
         TELEGRAM_BOT_TOKEN=<token-bot-pribadi>
         TELEGRAM_ALLOWED_USERS=<user-id-numerik-peserta>
         ```
       - Step 4: Starting and inspecting the gateway service with 1-click copy buttons:
         `hermes gateway start`
         `hermes gateway status`
       - Step 5: Gateway control reference:
         `hermes gateway stop`
       - Step 6: Direct Message verification test: Send a message to the bot from the allowlisted personal Telegram account. Explain the expected reply, and provide immediate troubleshooting for the `unauthorized` error (re-verify numeric user ID via @userinfobot and update local config).
       - Interactive checklist items with unique `data-task-id` attributes:
         - `m9-run-setup`: Menjalankan wizard `hermes gateway setup` di terminal.
         - `m9-select-telegram`: Memilih platform Telegram pada prompt wizard.
         - `m9-enter-token`: Memasukkan Telegram Bot Token secara privat di terminal lokal.
         - `m9-enter-allowed`: Memasukkan User ID numerik peserta ke daftar `TELEGRAM_ALLOWED_USERS`.
         - `m9-start-gateway`: Menjalankan gateway lokal dengan `hermes gateway start`.
         - `m9-check-status`: Memeriksa status service dengan `hermes gateway status`.
         - `m9-verify-dm`: Mengirim pesan DM ke bot dan menerima balasan sukses.
       - Checkpoint 7 Card (`#card-cp-7`):
         - Header with badge icon "7" and status badge `#status-card-cp7` ("Pending").
         - Pass criteria list (Gateway berjalan aktif via long polling, allowlist user ID terpasang, bot berhasil membalas DM peserta).
         - Action button group: "Lolos Verifikasi" (`data-checkpoint="cp-7" data-status="passed"`), "Ada Kendala (Gagal)" (`data-checkpoint="cp-7" data-status="failed"`), "Reset Status" (`data-checkpoint="cp-7" data-status="pending"`).
  </action>
  <verify>
    Verify element IDs and markup structure in `index.html` ensuring `#sec-module-9`, `#card-cp-7`, checklist checkboxes, and copy buttons exist.
  </verify>
  <done>
    Modul 9 and Checkpoint 7 gate are completely rendered and aligned with PANDUAN-PRAKTIK-KELAS.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update StateManager and app.js for Modul 9 and Checkpoint 7</name>
  <files>assets/js/state.js, assets/js/app.js</files>
  <action>
    1. In `assets/js/state.js`:
       - Add Modul 9 checklist keys to `DEFAULT_STATE.checklists`:
         `'m9-run-setup': false`, `'m9-select-telegram': false`, `'m9-enter-token': false`, `'m9-enter-allowed': false`, `'m9-start-gateway': false`, `'m9-check-status': false`, `'m9-verify-dm': false`.
       - Add Checkpoint 7 to `DEFAULT_STATE.checkpoints`:
         `'cp-7': 'pending'`.
    2. In `assets/js/app.js`:
       - Update `setupCheckpointGates()` to include `'cp-7'` in the initialized checkpoints array.
       - Update sidebar live class checkpoint status badges loop to include `'cp-7'` if sidebar elements exist (`#badge-nav-cp7`).
       - Ensure `resetState()` handles `'cp-7'`.
  </action>
  <verify>
    Verify in node that StateManager correctly initializes Modul 9 tasks (`sm.getModuleProgress('m9-').total === 7`) and `cp-7` is recognized as 'pending'.
  </verify>
  <done>
    StateManager and app.js manage Modul 9 checklist progress and Checkpoint 7 status transitions.
  </done>
</task>

</tasks>
