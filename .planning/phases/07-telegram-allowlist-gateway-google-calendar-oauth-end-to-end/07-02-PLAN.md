---
phase: "07"
plan: "02"
title: "Modul 10 Google Calendar OAuth, Modul 11 E2E Scenarios, Checkpoints 8-9 & Tests"
wave: 2
dependencies:
  - "07-01"
requirements:
  - CLASS-05
  - CLASS-06
  - GATE-05
  - GATE-06
files_modified:
  - index.html
  - assets/js/state.js
  - assets/js/app.js
  - tests/live-class-modules.test.js
autonomous: true
must_haves:
  truths:
    - Modul 10 provides complete guidance for creating Google Cloud practice project, activating Google Calendar API, configuring OAuth consent screen with test users, creating Desktop app OAuth Client ID, downloading client secret JSON safely, and authenticating Hermes.
    - Modul 11 provides copyable prompt scenarios (`Tampilkan agenda saya hari ini`, `Buat agenda uji "Workshop Hermes" besok pukul 09.00 selama 15 menit di kalender latihan`), verifying in Google Calendar browser, deleting dummy events, and mastering the 5-step reboot sequence.
    - Checkpoint 8 card is fully rendered in Modul 10 with Pass, Fail, and Reset buttons (`data-checkpoint="cp-8"`).
    - Checkpoint 9 card is fully rendered in Modul 11 with Pass, Fail, and Reset buttons (`data-checkpoint="cp-9"`).
    - StateManager tracks checklists for Modul 10 (`m10-*`) and Modul 11 (`m11-*`), and checkpoints `cp-8` and `cp-9`.
    - Checkpoint gates controller and sidebar badges in `app.js` handle checkpoints `cp-1` through `cp-9`.
    - Automated tests in `tests/live-class-modules.test.js` verify all Phase 7 modules (9, 10, 11), Checkpoints 7, 8, 9, persistence, and achieve 100% pass rate.
  artifacts:
    - path: index.html
      provides: Rich interactive sections for #sec-module-10, #sec-module-11, #card-cp-8, and #card-cp-9
    - path: assets/js/state.js
      provides: Default state tracking for m10 and m11 checklists, and cp-8 and cp-9 checkpoints
    - path: assets/js/app.js
      provides: Full checkpoint gates and sidebar navigation support for cp-1 through cp-9
    - path: tests/live-class-modules.test.js
      provides: Comprehensive test suites for Phase 7 modules, checklists, and checkpoints
  key_links:
    - from: index.html
      to: assets/js/app.js
      via: data-task-id attributes and data-checkpoint attributes
    - from: assets/js/app.js
      to: assets/js/state.js
      via: updateCheckpoint and getModuleProgress calls for Modul 10 and 11
    - from: tests/live-class-modules.test.js
      to: assets/js/state.js
      via: StateManager testing with cp-7, cp-8, cp-9 and m9, m10, m11
---

<objective>
Implement interactive guide content for Modul 10 (Google Calendar & OAuth 2.0 Desktop) and Modul 11 (Skenario Uji End-to-End & Operasional) along with Checkpoint Gates 8 & 9 in `index.html`, extend `StateManager` and `app.js` to manage Checkpoints 8–9 and checklists for Modul 10 & 11, and expand `tests/live-class-modules.test.js` to verify all Phase 7 state tracking, UI controllers, and persistence.

Purpose: Allow participants to authorize Hermes with Google Calendar via Desktop OAuth, test managing events from Telegram, understand post-workshop restart procedures, and verify everything with automated tests.
Output: Production markup in `index.html`, updated `state.js` & `app.js`, and expanded test suite in `tests/live-class-modules.test.js`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@PANDUAN-PRAKTIK-KELAS.md
@index.html
@assets/js/state.js
@assets/js/app.js
@tests/live-class-modules.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Modul 10 and Modul 11 markup with Checkpoint 8 & 9 gate cards</name>
  <files>index.html</files>
  <action>
    1. In `index.html`, locate `#sec-module-10` in `#container-liveclass`.
    2. Replace the placeholder with a full interactive module container featuring:
       - Module header: "Modul 10: Google Calendar & OAuth 2.0 Desktop", icon 📅, target duration (25 Menit).
       - Critical note: Emphasize using a Google practice account and dedicated test calendar (avoid primary work/production account).
       - Step 1: Opening Google Cloud Console (`https://console.cloud.google.com`) and creating/selecting practice project.
       - Step 2: Enabling Google Calendar API for the project.
       - Step 3: Configuring OAuth Consent Screen (User Type: External, Publishing status: Testing, add practice email as Test User).
       - Step 4: Creating OAuth 2.0 Client ID with application type **Desktop app** (crucial: not Web application).
       - Step 5: Downloading client secret JSON file and saving it safely on local laptop with strict alert box warning against sharing or uploading the credentials JSON file.
       - Step 6: Hermes authorization flow: invoking Hermes Google Workspace skill / setup, opening the browser consent URL, authorizing the practice account, and completing redirect code/URL input.
       - Step 7: Performing initial calendar read test in Hermes CLI to ensure token authorization works before Telegram testing.
       - Interactive checklist items with unique `data-task-id` attributes:
         - `m10-open-cloud`: Membuka Google Cloud Console dan membuat/memilih project latihan.
         - `m10-enable-calendar`: Mengaktifkan Google Calendar API pada project latihan.
         - `m10-oauth-screen`: Mengatur OAuth consent screen dan mendaftarkan email latihan sebagai test user.
         - `m10-create-desktop-client`: Membuat OAuth 2.0 Client ID dengan tipe Desktop app.
         - `m10-download-secret`: Mengunduh client secret JSON dan menyimpannya secara privat di laptop.
         - `m10-auth-hermes`: Menyelesaikan otorisasi Google di browser dan mengonfirmasi izin ke Hermes.
       - Checkpoint 8 Card (`#card-cp-8`):
         - Header with badge icon "8" and status badge `#status-card-cp8` ("Pending").
         - Pass criteria list (Project & API aktif, OAuth Desktop app siap, client secret JSON tersimpan lokal, Hermes berhasil membaca kalender latihan).
         - Action button group: "Lolos Verifikasi" (`data-checkpoint="cp-8" data-status="passed"`), "Ada Kendala (Gagal)" (`data-checkpoint="cp-8" data-status="failed"`), "Reset Status" (`data-checkpoint="cp-8" data-status="pending"`).

    3. In `index.html`, locate `#sec-module-11` in `#container-liveclass`.
    4. Replace the placeholder with a full interactive module container featuring:
       - Module header: "Modul 11: Skenario Uji End-to-End & Operasional", icon 🚀, target duration (20 Menit).
       - Step 1: Reading agenda scenario with copyable prompt:
         `Tampilkan agenda saya hari ini.`
         Send prompt via Telegram bot, verify Hermes fetches events from Google Calendar and replies in Telegram chat.
       - Step 2: Creating dummy agenda scenario with copyable prompt:
         `Buat agenda uji "Workshop Hermes" besok pukul 09.00 selama 15 menit di kalender latihan.`
         Send prompt via Telegram bot, verify Hermes creates the event and confirms in Telegram.
       - Step 3: Checking Google Calendar web UI in browser to verify event presence, followed by deleting the test event.
       - Step 4: Daily operational reboot sequence (5 sequential steps for restarting after workshop):
         1. Jalankan `9router` dan pastikan dashboard lokal terbuka.
         2. Pastikan Hermes masih menggunakan endpoint `http://localhost:20128/v1` dan model yang benar.
         3. Jalankan `hermes gateway start`.
         4. Jalankan `hermes gateway status`.
         5. Kirim pesan uji ke bot Telegram.
       - Security incident protocol: What to do if bot token leaks (`/revoke` in @BotFather, update local config, restart gateway).
       - Interactive checklist items with unique `data-task-id` attributes:
         - `m11-prompt-read`: Mengirim prompt uji baca agenda melalui chat bot Telegram.
         - `m11-prompt-create`: Mengirim prompt pembuatan agenda dummy ke bot Telegram.
         - `m11-verify-calendar`: Memeriksa Google Calendar di browser dan memastikan agenda uji berhasil dibuat.
         - `m11-clean-dummy`: Menghapus agenda uji dummy setelah verifikasi selesai.
         - `m11-reboot-sequence`: Memahami 5 urutan menyalakan ulang sistem untuk operasional mandiri hari berikutnya.
       - Checkpoint 9 Card (`#card-cp-9`):
         - Header with badge icon "9" and status badge `#status-card-cp9` ("Pending").
         - Pass criteria list (Tindakan baca dan buat Google Calendar sukses via bot Telegram, peserta memahami urutan reboot dan penanganan token bocor).
         - Action button group: "Lolos Verifikasi" (`data-checkpoint="cp-9" data-status="passed"`), "Ada Kendala (Gagal)" (`data-checkpoint="cp-9" data-status="failed"`), "Reset Status" (`data-checkpoint="cp-9" data-status="pending"`).
  </action>
  <verify>
    Verify element IDs in `index.html`: `#sec-module-10`, `#sec-module-11`, `#card-cp-8`, `#card-cp-9`, checklist checkboxes, prompt copy blocks, and action buttons.
  </verify>
  <done>
    Modul 10, Modul 11, and Checkpoints 8 & 9 are fully rendered in index.html.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend StateManager and app.js for Modul 10-11 and Checkpoints 8-9</name>
  <files>assets/js/state.js, assets/js/app.js</files>
  <action>
    1. In `assets/js/state.js`:
       - Add Modul 10 checklist keys to `DEFAULT_STATE.checklists`:
         `'m10-open-cloud': false`, `'m10-enable-calendar': false`, `'m10-oauth-screen': false`, `'m10-create-desktop-client': false`, `'m10-download-secret': false`, `'m10-auth-hermes': false`.
       - Add Modul 11 checklist keys to `DEFAULT_STATE.checklists`:
         `'m11-prompt-read': false`, `'m11-prompt-create': false`, `'m11-verify-calendar': false`, `'m11-clean-dummy': false`, `'m11-reboot-sequence': false`.
       - Add Checkpoints 8 and 9 to `DEFAULT_STATE.checkpoints`:
         `'cp-8': 'pending'`, `'cp-9': 'pending'`.
    2. In `assets/js/app.js`:
       - Ensure `setupCheckpointGates()` includes `['cp-1', 'cp-2', 'cp-3', 'cp-4', 'cp-5', 'cp-6', 'cp-7', 'cp-8', 'cp-9']`.
       - Ensure live checkpoint sidebar badge updates track all live checkpoints (`cp-4` through `cp-9`).
       - Ensure `resetState()` resets cards for `cp-1` through `cp-9`.
  </action>
  <verify>
    Verify in node that StateManager correctly tracks `m10-` (6 tasks), `m11-` (5 tasks), and checkpoints `cp-8`, `cp-9`.
  </verify>
  <done>
    StateManager and app.js manage all Phase 7 checklists and checkpoints.
  </done>
</task>

<task type="auto">
  <name>Task 3: Expand automated test suite in tests/live-class-modules.test.js</name>
  <files>tests/live-class-modules.test.js</files>
  <action>
    1. In `tests/live-class-modules.test.js`:
       - Add test coverage for Modul 9 (7 tasks), Modul 10 (6 tasks), Modul 11 (5 tasks).
       - Add test coverage for Checkpoint Gates 7, 8, 9 (initial pending status, passed/failed transitions).
       - Add test coverage for full live class progress calculation and persistence across sessions.
       - Verify that pretraining readiness logic remains isolated and independent from live class checkpoints.
    2. Run the test suite via Node.js to ensure 100% pass rate with zero errors.
  </action>
  <verify>
    Run `node tests/live-class-modules.test.js` and ensure all test assertions pass.
  </verify>
  <done>
    Automated test suite fully covers Phase 7 requirements (CLASS-04..06, GATE-04..06) with 100% test pass rate.
  </done>
</task>

</tasks>
