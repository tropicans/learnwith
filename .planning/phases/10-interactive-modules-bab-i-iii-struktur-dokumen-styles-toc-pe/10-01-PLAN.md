---
phase: "10-interactive-modules-bab-i-iii-struktur-dokumen-styles-toc-pe"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/word-modules.test.js
  - tests/index.html
  - assets/js/state.js
  - assets/js/app.js
autonomous: true
requirements:
  - WORD-01
  - WORD-02
  - WORD-03
  - WORD-05
estimate:
  tokens: 28000
  raw_tokens: 14000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "StateManager initializes WORD_DEFAULT_STATE with exactly 20 checklist tasks and 2 checkpoints when activeCourse is 'word'"
    - "State updates in Course 2 persist strictly in learnwith_word_state_v1 without polluting or resetting learnwith_ai_state_v1"
    - "Course 2 progress formula calculates 60% weight for checklists (20 tasks) and 40% weight for checkpoints (word-cp-1 and word-cp-2)"
    - "switchCourse('word') displays #nav-group-word, hides Course 1 nav groups, synchronizes checkboxes and cards, and updates header progress"
  artifacts:
    - tests/word-modules.test.js
    - tests/index.html
    - assets/js/state.js
    - assets/js/app.js
  key_links:
    - "tests/word-modules.test.js verifies state isolation, checkpoints, progress calculation, and DOM controller interactions"
---

<objective>
Implement the Wave 0 automated test suite and core State/Controller engine for Course 2 (Pengolahan Kata Tingkat Lanjut). 

This plan establishes:
1. The comprehensive unit/regression test harness (`tests/word-modules.test.js`) and browser test runner fixtures (`tests/index.html`).
2. Scoped default state (`WORD_DEFAULT_STATE`), 20 checklist items (Bab I–III), 2 checkpoints (`word-cp-1`, `word-cp-2`), weighted progress math (60% tasks, 40% checkpoints), and readiness logic (`calculateWordReadiness()`) in `assets/js/state.js`.
3. Checkpoint gate and navigation synchronization for Course 2 in `assets/js/app.js`.

Purpose: Provide robust, regression-tested state management and controller backbones for the interactive Word processing modules without any cross-course pollution.
Output: `tests/word-modules.test.js`, updated `tests/index.html`, updated `assets/js/state.js`, and updated `assets/js/app.js`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-interactive-modules-bab-i-iii-struktur-dokumen-styles-toc-pe/10-RESEARCH.md
@.planning/phases/10-interactive-modules-bab-i-iii-struktur-dokumen-styles-toc-pe/10-VALIDATION.md
@.planning/phases/10-interactive-modules-bab-i-iii-struktur-dokumen-styles-toc-pe/10-PATTERNS.md
@assets/js/state.js
@assets/js/app.js
@tests/multi-course.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 Automated Test Suite for Word Processing Modules</name>
  <files>tests/word-modules.test.js, tests/index.html</files>
  <behavior>
    - Suite 1: StateManager Course 2 Isolation & Default State (20 checklist tasks: 4 for Bab I, 8 for Bab II, 8 for Bab III; 2 checkpoints: word-cp-1, word-cp-2; zero bleed into learnwith_ai_state_v1).
    - Suite 2: Checkpoint 1 & 2 Gates Workflow (transitions pending -> passed -> failed -> reset, persistence in localStorage).
    - Suite 3: Course 2 Weighted Progress & Readiness (0% on clean state; 20 tasks = 60%; 1 cp passed = 80%; 2 cps passed = 100%; calculateWordReadiness returns ready, clinic, or pending).
    - Suite 4: DOM Element & Attribute Integrity (verifies required sections #sec-word-intro, #sec-word-standards, #sec-word-shortcuts, #sec-word-module-2, #sec-word-cp-1, #sec-word-module-3, #sec-word-cp-2, #sec-word-diagnosis, buttons, and status badges).
    - Suite 5: Course Switcher & Navigation Synchronization (switchCourse('word') displays #nav-group-word, hides Course 1 groups, syncs checkboxes, and cleanly reverts to 'ai').
  </behavior>
  <action>
    1. Create `tests/word-modules.test.js` using Node.js built-in `assert` and mock DOM / localStorage utilities modeled after `tests/multi-course.test.js`.
    2. Define test suites 1 through 5 covering all behaviors above.
    3. Update `tests/index.html` to register `#container-course-word` and `#nav-group-word` in the `#test-dom-fixtures` container, and add `<script src="word-modules.test.js"></script>`.
  </action>
  <verify>
    <automated>cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-modules.test.js"</automated>
    <fails_when>non-zero exit code or failing assertions in tests/word-modules.test.js</fails_when>
  </verify>
  <done>
    `tests/word-modules.test.js` exists, executes in Node.js, and fails predictably only on unimplemented StateManager / Controller functions (red state) or passes once Tasks 2 and 3 are wired.
  </done>
</task>

<task type="auto">
  <name>Task 2: Course 2 Scoped State Defaults, Checklists, and Readiness in StateManager</name>
  <files>assets/js/state.js</files>
  <action>
    Refactor `assets/js/state.js` to natively support Course 2 (`activeCourse === 'word'`):
    1. Define `WORD_DEFAULT_STATE` with:
       - `theme`: 'light'
       - `checklists`: 4 Bab I keys (`word-b1-download-pkg`, `word-b1-setup-folder`, `word-b1-inspect-messy`, `word-b1-check-version`), 8 Bab II keys (`word-b2-apply-h1`, `word-b2-apply-h2-h3`, `word-b2-modify-styles`, `word-b2-nav-pane`, `word-b2-multilevel`, `word-b2-insert-toc`, `word-b2-update-toc`, `word-b2-captions-ref`), and 8 Bab III keys (`word-b3-section-breaks`, `word-b3-unlink-header`, `word-b3-page-num-roman`, `word-b3-page-num-arabic`, `word-b3-landscape-mix`, `word-b3-save-dotx`, `word-b3-content-controls`, `word-b3-doc-inspection`). All defaulting to `false`.
       - `checkpoints`: `word-cp-1`: 'pending', `word-cp-2`: 'pending'.
       - `participantInfo`: `name`, `nip`, `unitKerja`, `targetDoc` (all empty strings).
       - `activeSection`: 'sec-word-intro'.
    2. Update `loadState()` and `resetState()` to dynamically select `WORD_DEFAULT_STATE` when `this.activeCourse === 'word'`, and `DEFAULT_STATE` when `this.activeCourse === 'ai'`.
    3. Update `calculateProgress(mode)`:
       - When `this.activeCourse === 'word'`: filter taskKeys for `word-b1-`, `word-b2-`, `word-b3-` and checkpointKeys for `word-cp-`. Calculate 60% task weight + 40% checkpoint weight ((completedTasks/20 * 60) + (passedCheckpoints/2 * 40)).
       - When `this.activeCourse === 'ai'`: preserve existing Pre-Training and Live-Class progress logic.
    4. Implement `calculateWordReadiness()`:
       - Returns `status: 'clinic'` if any of `word-cp-1` or `word-cp-2` has value `'failed'`.
       - Returns `status: 'ready'` if all checkpoints are `'passed'` and progress percentage >= 80.
       - Returns `status: 'pending'` otherwise.
    5. Export `WORD_DEFAULT_STATE` in `module.exports` alongside `StateManager`, `DEFAULT_STATE`, and `COURSE_CONFIGS`.
  </action>
  <verify>
    <automated>cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-modules.test.js && node tests/multi-course.test.js"</automated>
    <fails_when>non-zero exit code or failing assertions in tests/word-modules.test.js or tests/multi-course.test.js</fails_when>
  </verify>
  <done>
    StateManager handles `WORD_DEFAULT_STATE` cleanly, calculates weighted progress correctly for Course 2, preserves isolation from Course 1, and passes Suites 1-3.
  </done>
</task>

<task type="auto">
  <name>Task 3: CourseManager, Checkpoint Gate & UI Progress Handlers for Course 2</name>
  <files>assets/js/app.js</files>
  <action>
    Enhance `assets/js/app.js` to orchestrate Course 2 navigation, checkpoint gates, and progress displays:
    1. In `switchCourse(courseId, updateUrl, showNotification)`:
       - Reference `navGroupWord = document.getElementById('nav-group-word')`.
       - When `validCourse === 'word'`:
         - Show `navGroupWord` (`style.display = 'block'`) and add `active` class.
         - Hide `navGroupPretraining` and `navGroupLiveclass` (`style.display = 'none'`).
         - Hide `#header-mode-switcher` and `#sidebar-mode-switcher-container`.
         - Re-sync Course 2 checklist checkboxes (`.checklist-checkbox`) and checkpoint cards (`card-word-cp-1`, `card-word-cp-2`) from active state.
         - Invoke `updateProgressUI()` to refresh header progress bar with Word course metrics.
       - When `validCourse === 'ai'`:
         - Hide `navGroupWord` (`style.display = 'none'`) and remove `active` class.
         - Restore Course 1 mode switcher and active mode navigation.
         - Re-sync Course 1 checkboxes and checkpoints.
         - Invoke `updateProgressUI()`.
    2. In `setupCheckpointGates()` and `updateCheckpointCardUI()`:
       - Support `word-cp-1` and `word-cp-2` in addition to `cp-1`..`cp-9`.
       - Implement robust badge resolver that handles `status-card-word-cp1`, `status-card-word-cp2`, and sidebar badges `status-nav-word-cp1`, `status-nav-word-cp2`.
       - Ensure clicking `.btn-cp-action` with `data-checkpoint="word-cp-1"` or `"word-cp-2"` updates state via `window.AppState.updateCheckpoint()`, updates card UI, updates sidebar badge, and displays toast notification.
    3. In `updateProgressUI()`:
       - When `window.AppState.getActiveCourse() === 'word'`:
         - Update header progress bar and percentage text with Word progress metrics.
         - Update `#badge-nav-word-b1` using `window.AppState.getModuleProgress('word-b1-')`.
         - Update `#badge-nav-word-b2` using `window.AppState.getModuleProgress('word-b2-')`.
         - Update `#badge-nav-word-b3` using `window.AppState.getModuleProgress('word-b3-')`.
         - Update `#status-nav-word-cp1` and `#status-nav-word-cp2`.
         - If `#card-word-readiness-status` and `#readiness-word-badge` exist, populate with `calculateWordReadiness()`.
  </action>
  <verify>
    <automated>cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-modules.test.js && node tests/multi-course.test.js"</automated>
    <fails_when>non-zero exit code or failing assertions in tests/word-modules.test.js or tests/multi-course.test.js</fails_when>
  </verify>
  <done>
    `tests/word-modules.test.js` passes completely across all 5 suites without regressions to `tests/multi-course.test.js`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage | State read/write boundary between courses and user sessions (`learnwith_ai_state_v1` vs `learnwith_word_state_v1`) |
| DOM Event Listeners | Client-side user input via checkbox clicks, checkpoint gate buttons, and course switcher controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | StateManager.loadState | medium | mitigate | Validate parsed JSON against `WORD_DEFAULT_STATE` schema; sanitize invalid checkpoint states to 'pending'; ensure keys from Course 1 cannot pollute Course 2 |
| T-10-02 | Tampering | updateCheckpointCardUI | low | mitigate | Allowlist valid checkpoint status values ('pending', 'passed', 'failed') to prevent DOM class injection |
| T-10-03 | Information Disclosure | Storage Namespacing | low | mitigate | Ensure separate storage keys (`learnwith_word_state_v1` and `learnwith_ai_state_v1`) so participant progress and notes never leak across courses |
</threat_model>

<verification>
Run the automated test suite:
`cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-modules.test.js && node tests/multi-course.test.js"`
</verification>

<success_criteria>
- `tests/word-modules.test.js` passes with 100% green assertions.
- StateManager cleanly isolates `learnwith_word_state_v1`.
- Course 2 checklists (20 items) and checkpoints (2 gates) calculate weighted progress accurately.
- Course switching toggles navigation groups and re-syncs DOM states seamlessly.
</success_criteria>

<output>
Create `.planning/phases/10-interactive-modules-bab-i-iii-struktur-dokumen-styles-toc-pe/10-01-PLAN.md`
</output>
