---
phase: "12-interactive-knowledge-quiz-bab-v-rubrik-evaluasi-laporan-kel"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/word-quiz-report.test.js
  - assets/js/state.js
  - assets/js/app.js
autonomous: true
requirements:
  - QUIZ-01
  - QUIZ-02
  - QUIZ-03
  - WORD-RPT-01
  - WORD-RPT-02
estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "StateManager initializes WORD_DEFAULT_STATE with quiz, reflections, and rubric sub-objects while strictly preserving 28 checklist tasks in state.checklists"
    - "WORD_QUIZ_QUESTIONS constant contains exactly 20 official Bab V multiple-choice questions with answer keys matching Lampiran 1 Table L1.1 and comprehensive pedagogical explanations"
    - "Quiz scoring calculates 5 points per correct answer (0-100 scale), sets passed=true when score >= 80, and resets cleanly on demand"
    - "calculateWordGraduation() accurately computes 70% Practice (Checkpoints + Portfolio Rubric) + 30% Quiz Knowledge score and assigns official BPSDM predicate and ASN competency status"
    - "generateWordReportText() formats live training progress for WhatsApp and Telegram Markdown without undefined fields or syntax corruption"
    - "All Course 2 Bab V evaluations and reports persist strictly in learnwith_word_state_v1 with zero pollution of Course 1 state"
  artifacts:
    - tests/word-quiz-report.test.js
    - assets/js/state.js
    - assets/js/app.js
  key_links:
    - "tests/word-quiz-report.test.js verifies question bank integrity, state persistence, scoring formulas, report generators, and state isolation"
    - "tests/word-modules.test.js verifies existing 28-task and 3-checkpoint progress invariant is preserved"
---

<objective>
Implement the Wave 0 automated test suite, StateManager extensions, official 20-question Bab V knowledge bank, and core multi-channel report exporter engine for Course 2 (Pengolahan Kata Tingkat Lanjut).

This plan establishes:
1. Wave 0 Automated Test Suite (`tests/word-quiz-report.test.js`) asserting question bank integrity (20 questions, choices A-D, official answer keys 1:B..20:B, explanations), immediate scoring logic, reflection/rubric state isolation, multi-channel report generation (WhatsApp & Telegram), and multi-course storage isolation.
2. State management extensions in `assets/js/state.js`: `WORD_DEFAULT_STATE` expansion (`quiz`, `reflections`, `rubric`), `WORD_QUIZ_QUESTIONS` constant, `updateQuizAnswer()`, `calculateQuizScore()`, `resetQuiz()`, `updateWordReflection()`, `updateWordRubric()`, and `calculateWordGraduation()`.
3. Report generator engine in `assets/js/app.js`: `generateWordReportText(format, overrides)` supporting `'whatsapp'` and `'telegram'` multi-channel export.

Purpose: Guarantee 100% test-driven data fidelity, deterministic scoring, and state isolation before building DOM interfaces.
Output: `tests/word-quiz-report.test.js`, updated `assets/js/state.js`, and updated `assets/js/app.js`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-interactive-knowledge-quiz-bab-v-rubrik-evaluasi-laporan-kel/12-RESEARCH.md
@.planning/phases/12-interactive-knowledge-quiz-bab-v-rubrik-evaluasi-laporan-kel/12-VALIDATION.md
@.planning/phases/12-interactive-knowledge-quiz-bab-v-rubrik-evaluasi-laporan-kel/12-PATTERNS.md
@assets/js/state.js
@assets/js/app.js
@tests/word-modules.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 Automated Test Suite for Bab V Quiz, Rubric & BPSDM Graduation Report</name>
  <files>tests/word-quiz-report.test.js</files>
  <read_first>
    tests/word-modules.test.js
    tests/troubleshooting-exporter.test.js
    assets/js/state.js
    assets/js/app.js
    .planning/phases/12-interactive-knowledge-quiz-bab-v-rubrik-evaluasi-laporan-kel/12-RESEARCH.md
  </read_first>
  <behavior>
    - Suite 1: Bab V Question Bank Integrity (verifies exactly 20 questions, IDs 1 to 20, 4 options A, B, C, D per question, official keys matching Lampiran 1 [1:B, 2:B, 3:C, 4:B, 5:C, 6:A, 7:B, 8:B, 9:C, 10:B, 11:A, 12:B, 13:A, 14:B, 15:B, 16:B, 17:A, 18:A, 19:B, 20:B], non-empty Indonesian explanations).
    - Suite 2: Interactive Scoring & State Transitions (verifies updateQuizAnswer records choices, calculateQuizScore awards 5 pts per correct answer, score >= 80 sets passed: true, score < 80 sets passed: false, resetQuiz clears answers and resets score to 0).
    - Suite 3: Self-Reflection & Competency Rubric Engine (verifies updating 5 reflection prompts, toggling 9 Lampiran 3 quality checks and 6 Lampiran 5 portfolio artifacts, calculateWordGraduation computing 70% Practice + 30% Quiz, predicate A/B/C/Remediasi, and ASN competency status).
    - Suite 4: BPSDM Graduation Report Generation (verifies generateWordReportText produces prefilled reports with participant info, Checkpoints 1-3 [X]/[ ], quiz score, rubric summary, and final competency verdict).
    - Suite 5: Multi-Channel Exporter & DOM Fixture Integrity (verifies valid WhatsApp emoji/bullet format, Telegram Markdown backtick format, and DOM mock fixtures for #sec-word-quiz, #sec-word-rubrik, #sec-word-report, #bpsdm-certificate-card, action buttons).
    - Suite 6: Multi-Course State Isolation & Progress Invariants (verifies quiz/rubric state persists in learnwith_word_state_v1 without touching learnwith_ai_state_v1, and calculateProgress continues to assert exactly 28 checklist tasks and 3 checkpoints).
  </behavior>
  <action>
    1. Create `tests/word-quiz-report.test.js` using Node.js assert, in-memory localStorage polyfill, and lightweight DOM mocks.
    2. Import or require `assets/js/state.js` and `assets/js/app.js`.
    3. Implement all 6 test suites with informative console output (`✓ PASS` / `✕ FAIL`).
    4. Assert all boundary conditions (clean state, partial quiz, perfect 100/100 quiz, failing quiz, passing vs failing checkpoints, multi-channel copy text escaping).
  </action>
  <acceptance_criteria>
    - File `tests/word-quiz-report.test.js` exists and executes under Node.js (`C:\nvm4w\nodejs\node.exe`).
    - Test asserts all 20 questions, answer keys, explanations, scoring formulas, reflection/rubric keys, and exporter text formats.
    - Test asserts strict state isolation from Course 1 and preservation of Course 2 28-task baseline.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-quiz-report.test.js"</automated>
    <fails_when>non-zero exit code or failing assertions in tests/word-quiz-report.test.js</fails_when>
  </verify>
  <done>
    Wave 0 test suite is written and runnable, providing immediate red/green feedback for Tasks 2 and 3.
  </done>
</task>

<task type="auto">
  <name>Task 2: StateManager Extensions for Course 2 Bab V (Quiz, Reflections, Rubric & Graduation)</name>
  <files>assets/js/state.js</files>
  <read_first>
    assets/js/state.js
    tests/word-quiz-report.test.js
    .planning/phases/12-interactive-knowledge-quiz-bab-v-rubrik-evaluasi-laporan-kel/12-PATTERNS.md
  </read_first>
  <behavior>
    - WORD_DEFAULT_STATE contains quiz { answers: {}, score: 0, submitted: false, passed: false }.
    - WORD_DEFAULT_STATE contains reflections { 'ref-repetitive': '', 'ref-challenging': '', 'ref-template': '', 'ref-risks': '', 'ref-collaboration': '' }.
    - WORD_DEFAULT_STATE contains rubric with 9 Lampiran 3 checklist keys ('word-chk-*') and 6 Lampiran 5 portfolio keys ('word-port-*').
    - StateManager implements updateQuizAnswer(questionId, selectedOption) that persists the option and recalculates quiz score.
    - StateManager implements calculateQuizScore() comparing answers with official keys in WORD_QUIZ_QUESTIONS (score = count * 5, passed = score >= 80).
    - StateManager implements resetQuiz() clearing quiz.answers, quiz.score = 0, quiz.submitted = false, quiz.passed = false.
    - StateManager implements updateWordReflection(key, text) persisting reflection responses.
    - StateManager implements updateWordRubric(key, checked) persisting quality/portfolio checks.
    - StateManager implements calculateWordGraduation() computing Nilai Akhir = (Praktik * 0.7) + (Pengetahuan * 0.3), determining Predikat (Sangat Memuaskan >= 90, Memuaskan 80-89.9, Cukup 75-79.9, Belum Memenuhi Syarat < 75 or failed checkpoint), and ASN status.
    - calculateProgress() for Course 2 continues to filter strictly word-b1- through word-b4- (totalTasks === 28).
  </behavior>
  <action>
    1. In `assets/js/state.js`, expand `WORD_DEFAULT_STATE` with `quiz`, `reflections`, and `rubric` sub-objects.
    2. Add `updateQuizAnswer(questionId, selectedOption)` method to `StateManager`.
    3. Add `calculateQuizScore()` method to `StateManager`.
    4. Add `resetQuiz()` method to `StateManager`.
    5. Add `updateWordReflection(key, text)` method to `StateManager`.
    6. Add `updateWordRubric(key, checked)` method to `StateManager`.
    7. Add `calculateWordGraduation()` method to `StateManager`.
    8. In `StateManager.resetState()`, ensure `quiz`, `reflections`, and `rubric` reset cleanly when Course 2 is active.
    9. Ensure all methods emit appropriate events (`quizUpdated`, `reflectionUpdated`, `rubricUpdated`, `stateChanged`).
    10. Ensure `calculateProgress()` logic does not count rubric keys into the 28-task baseline.
  </action>
  <acceptance_criteria>
    - `WORD_DEFAULT_STATE.quiz`, `WORD_DEFAULT_STATE.reflections`, and `WORD_DEFAULT_STATE.rubric` are defined.
    - `updateQuizAnswer`, `calculateQuizScore`, `resetQuiz`, `updateWordReflection`, `updateWordRubric`, and `calculateWordGraduation` are callable methods on `AppState`.
    - `calculateProgress()` for Course 2 returns `totalTasks === 28`.
    - Suites 1, 2, 3, and 6 pass in `tests/word-quiz-report.test.js`.
    - `tests/word-modules.test.js` passes with 0 regressions.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-quiz-report.test.js && node tests/word-modules.test.js"</automated>
    <fails_when>non-zero exit code or failing assertions</fails_when>
  </verify>
  <done>
    StateManager handles all Bab V data, scoring, and graduation evaluations with complete multi-course isolation and regression safety.
  </done>
</task>

<task type="auto">
  <name>Task 3: Official 20-Question Knowledge Bank & Core Multi-Channel Exporter Engine</name>
  <files>assets/js/state.js, assets/js/app.js</files>
  <read_first>
    assets/js/state.js
    assets/js/app.js
    tests/word-quiz-report.test.js
    .planning/phases/12-interactive-knowledge-quiz-bab-v-rubrik-evaluasi-laporan-kel/12-RESEARCH.md
    .planning/phases/12-interactive-knowledge-quiz-bab-v-rubrik-evaluasi-laporan-kel/12-PATTERNS.md
  </read_first>
  <behavior>
    - Define WORD_QUIZ_QUESTIONS constant with all 20 official questions from Bab V / Lampiran 1 BPSDM 2026, including id (1-20), question, options (A, B, C, D), correct key, and pedagogical explanation.
    - Export WORD_QUIZ_QUESTIONS in assets/js/state.js and bind to window.WORD_QUIZ_QUESTIONS.
    - Implement generateWordReportText(format, overrides) in assets/js/app.js.
    - 'whatsapp' format produces clean text with emojis (✅, 📋, 🏆, ⚠️), asterisks for bolding, bullet points, and participant details.
    - 'telegram' format produces clean Markdown with backtick checkboxes (`[X]`, `[ ]`), clean headers, bold/italic markup, and participant details.
    - Both formats include: Participant Name, NIP, Unit Kerja, Target Naskah Dinas, Tanggal, Checkpoint 1-3 statuses, Nilai Kuis Pengetahuan (0-100), Portofolio Praktik, Nilai Akhir Terpadu, and Status Kompetensi ASN.
    - Export generateWordReportText in module.exports and bind to window.generateWordReportText.
  </behavior>
  <action>
    1. In `assets/js/state.js`, define `WORD_QUIZ_QUESTIONS` array with exactly 20 questions, options A-D, correct keys [1:B, 2:B, 3:C, 4:B, 5:C, 6:A, 7:B, 8:B, 9:C, 10:B, 11:A, 12:B, 13:A, 14:B, 15:B, 16:B, 17:A, 18:A, 19:B, 20:B], and official explanations. Export it in `module.exports` and `window`.
    2. In `assets/js/app.js`, implement `generateWordReportText(format = 'whatsapp', overrides = {})`.
    3. Aggregate live data from `AppState.getState()` or fallback to `WORD_DEFAULT_STATE`.
    4. Format text for WhatsApp and Telegram cleanly, escaping any problematic characters.
    5. Export `generateWordReportText` and `WORD_QUIZ_QUESTIONS` in `assets/js/app.js` module.exports and `window`.
    6. Run the complete test suite.
  </action>
  <acceptance_criteria>
    - `WORD_QUIZ_QUESTIONS` has length 20, valid IDs, 4 options each, and official keys.
    - `generateWordReportText('whatsapp')` and `generateWordReportText('telegram')` generate non-empty, well-formatted strings.
    - All 6 suites in `tests/word-quiz-report.test.js` pass with 0 failures.
    - `tests/word-modules.test.js`, `tests/multi-course.test.js`, and `tests/checkpoint-engine.test.js` all pass.
  </acceptance_criteria>
  <verify>
    <automated>cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-quiz-report.test.js && node tests/word-modules.test.js && node tests/multi-course.test.js && node tests/checkpoint-engine.test.js"</automated>
    <fails_when>non-zero exit code or failing tests</fails_when>
  </verify>
  <done>
    Question bank and report generator are fully implemented and verified against all automated test suites.
  </done>
</task>

</tasks>

<threat_model>
- **T-12-01 (State Pollution & Baseline Corruption):** Adding quiz, reflection, and rubric items could pollute Course 1 or corrupt Course 2 28-task baseline in calculateProgress(). Mitigation: Store quiz, reflections, and rubric in dedicated sub-objects; maintain strict filtering in calculateProgress() for word-b1- through word-b4- keys only.
- **T-12-02 (Answer Key Mismatch):** Incorrect question keys or randomized IDs could penalize learners falsely. Mitigation: Verbatim keys matching Lampiran 1 Table L1.1 enforced by unit test Suite 1.
- **T-12-03 (Format Syntax Injection in Multi-Channel Exporters):** Unescaped user inputs could break Telegram markdown or WhatsApp styling. Mitigation: Clean sanitization and fallback strings in generateWordReportText().
</threat_model>
