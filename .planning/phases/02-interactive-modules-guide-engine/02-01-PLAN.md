---
phase: 02-interactive-modules-guide-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [assets/css/components.css, assets/js/app.js, assets/js/search.js]
autonomous: true
requirements: [GUIDE-01, GUIDE-03, GUIDE-05]
must_haves:
  truths:
    - Interactive accordion/collapsible module styles allow smooth expanding and collapsing of guide sections.
    - Glossary terms throughout the guide feature hover and click popovers/tooltips with plain Indonesian explanations.
    - External links have secure attributes (target="_blank" rel="noopener noreferrer") and visual external indicator badges.
    - Search engine seamlessly indexes collapsible module content and auto-expands collapsed modules when matching search queries are found.
  artifacts:
    - assets/css/components.css
    - assets/js/app.js
    - assets/js/search.js
  key_links:
    - Accordion triggers in HTML invoke expand/collapse handlers in app.js.
    - SearchEngine in search.js auto-opens relevant accordions when user searches.
---

<objective>
Build the interactive guide framework, collapsible module UI, glossary tooltip/popover engine, and external link security layer.

Purpose: Provide the foundational UI components and micro-interactions required for rendering rich, structured guide modules with collapsible steps, contextual tooltips, and search-responsive accordion expansion.
Output: Updated `assets/css/components.css`, `assets/js/app.js`, and `assets/js/search.js`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-interactive-modules-guide-engine/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Accordion, Module Card, and Glossary Tooltip Styles to CSS</name>
  <files>assets/css/components.css</files>
  <read_first>assets/css/components.css</read_first>
  <action>
    Update `assets/css/components.css` to add:
    1. **Collapsible Module Card**:
       - `.module-card`: Container with glassmorphic background, border accent, and rounded corners.
       - `.module-header`: Clickable accordion header with title, icon, badges, step counter, and smooth rotating chevron indicator.
       - `.module-body`: Animated expandable container using CSS grid/max-height transitions (`transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1)`).
       - `.module-card.collapsed .module-body`: Hidden/collapsed state with `display: none` or height transition.
    2. **Glossary Popover & Inline Tooltip Styles**:
       - `.glossary-term`: Dotted underline, accent color, cursor help, relative positioning.
       - `.glossary-tooltip`: Floating popover card with title, explanation, arrow pointer, shadow, and fade-in animation.
    3. **External Link Badges & Visual Cues**:
       - `.external-link`: Styling for official external documentation links with inline SVG arrow ↗ and safe hover states.
       - `.command-badge`: Expected output badges (`v24.x.x`, `http://localhost:20128`).
  </action>
  <verify>Check `assets/css/components.css` contains `.module-card`, `.module-header`, `.glossary-term`, and `.external-link` styles.</verify>
  <acceptance_criteria>
    - CSS rules for collapsible modules and glossary tooltips exist and have valid syntax.
  </acceptance_criteria>
  <done>CSS styles for modules, accordions, and glossary popovers are implemented in assets/css/components.css</done>
</task>

<task type="auto">
  <name>Task 2: Implement Accordion and Glossary Popover Logic in JavaScript</name>
  <files>assets/js/app.js, assets/js/search.js</files>
  <read_first>assets/js/app.js</read_first>
  <action>
    1. In `assets/js/app.js`:
       - Add `setupModuleAccordions()`: Add click listeners to `.module-header` and `.accordion-toggle` buttons to toggle `.collapsed` class on parent `.module-card`, updating `aria-expanded` attributes.
       - Support URL hash navigation: Auto-expand the targeted module if the page loads or hash changes to `#sec-module-X`.
       - Add `setupGlossaryTooltips()`: Wire up interactive click/hover events for `.glossary-term` elements with positioning and backdrop click dismiss for mobile devices.
       - Add Expand All / Collapse All controls support.
    2. In `assets/js/search.js`:
       - Enhance search indexing to index all module steps and collapsible content.
       - When a search query matches text inside a collapsed module, automatically remove `.collapsed` so the user immediately sees the highlighted result.
  </action>
  <verify>Check `assets/js/app.js` and `assets/js/search.js` for `setupModuleAccordions` and auto-expansion search integration.</verify>
  <acceptance_criteria>
    - `setupModuleAccordions()` toggles module open/close states.
    - Searching for a term inside a module automatically reveals the module content.
  </acceptance_criteria>
  <done>JavaScript logic for module accordions, glossary popovers, and search auto-expansion is wired up.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify `assets/css/components.css` contains accordion and tooltip classes.
- [ ] Verify `assets/js/app.js` initializes accordion and tooltip handlers on DOMContentLoaded.
- [ ] Verify `assets/js/search.js` expands collapsed parent containers when highlighting matches.
</verification>

<success_criteria>
- All tasks completed
- Responsive accordion and tooltip UI foundation ready for Modules 1–5
- Requirements GUIDE-01, GUIDE-03, GUIDE-05 foundation met
</success_criteria>

<output>
After completion, create `.planning/phases/02-interactive-modules-guide-engine/02-01-SUMMARY.md`
</output>
