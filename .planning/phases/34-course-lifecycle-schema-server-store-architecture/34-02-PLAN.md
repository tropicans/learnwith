---
phase: 34-course-lifecycle-schema-server-store-architecture
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/server/courseLifecycle.ts
  - tests/course-lifecycle-rpc.test.js
autonomous: true
requirements:
  - COURSE-MUTATE-01
  - COURSE-MUTATE-02
must_haves:
  truths:
    - Server functions in app/server/courseLifecycle.ts validate Master Admin authorization via assertAdminAuthorized().
    - adminGetCoursesLifecycleFn returns complete course status list and audit log for authorized Master Admin.
    - adminUpdateCourseStatusFn validates inputs using updateCourseStatusInputSchema, executes mutation, and returns updated record.
    - getPublicCourseStatusesFn exposes safe public projection without leaking audit logs or admin tokens.
    - Automated tests in tests/course-lifecycle-rpc.test.js verify RPC authorization, mutation success, and unauthorized rejection.
  artifacts:
    - app/server/courseLifecycle.ts
    - tests/course-lifecycle-rpc.test.js
  key_links:
    - app/server/courseLifecycle.ts consumes courseLifecycleStore.ts and asserts admin session using session.ts / platform.ts assertAdminAuthorized.
---

<objective>
Implement typed Server Functions (RPCs) using TanStack Starts createServerFn for querying and mutating course lifecycle states with Master Admin session verification.

Purpose: Expose secure server endpoints for the admin command center to query status, change visibility, and soft-delete/restore courses while providing public projection for client components.
Output: app/server/courseLifecycle.ts and tests/course-lifecycle-rpc.test.js.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@app/schemas/courseLifecycle.ts
@app/server/courseLifecycleStore.ts
@app/server/platform.ts
@app/server/session.ts
</context>

<tasks>

<task type="auto" id="34-02-01">
<title>Implement Master Admin and Public Server Functions for Course Lifecycle</title>
<instruction>
Create app/server/courseLifecycle.ts:
- Import createServerFn from @tanstack/react-start.
- Import assertAdminAuthorized from ./platform.ts.
- Import schemas from ../schemas/courseLifecycle.ts.
- Import store methods from ./courseLifecycleStore.ts.
- Implement adminGetCoursesLifecycleFn:
  - Method: GET
  - Validator: optional sessionToken in query/input.
  - Handler: assertAdminAuthorized(sessionToken), returns records and audit history.
- Implement adminUpdateCourseStatusFn:
  - Method: POST
  - Validator: updateCourseStatusInputSchema.
  - Handler: assertAdminAuthorized(data.sessionToken), calls updateCourseLifecycleStatus(data.courseId, data.targetStatus, user.role, data.reason), returns updated record and message.
- Implement getPublicCourseStatusesFn:
  - Method: GET
  - Public endpoint without auth check.
  - Handler: returns getPublicCourseStatusProjections().
</instruction>
<files>app/server/courseLifecycle.ts</files>
</task>

<task type="auto" id="34-02-02">
<title>Create Automated Test Suite for Course Lifecycle RPCs</title>
<instruction>
Create tests/course-lifecycle-rpc.test.js:
- Test getPublicCourseStatusesFn executes without credentials and returns projected items.
- Test adminGetCoursesLifecycleFn rejects unauthorized requests (missing/invalid session token) with 401/UNAUTHORIZED error.
- Test adminGetCoursesLifecycleFn succeeds with valid Master Admin session token.
- Test adminUpdateCourseStatusFn rejects unauthorized callers.
- Test adminUpdateCourseStatusFn successfully updates status (active -> hidden -> archived -> deleted -> active) when authenticated.
- Test adminUpdateCourseStatusFn rejects invalid status transitions with descriptive error message.
- Run npm test to verify full suite passes.
</instruction>
<files>tests/course-lifecycle-rpc.test.js</files>
</task>

</tasks>