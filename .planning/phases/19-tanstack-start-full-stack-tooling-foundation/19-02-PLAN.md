---
phase: 19-tanstack-start-full-stack-tooling-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - tsconfig.json
  - app.config.ts
  - app/router.tsx
  - app/client.tsx
  - app/ssr.tsx
  - app/routes/__root.tsx
  - app/routes/index.tsx
  - app/server/.gitkeep
  - public/favicon.svg
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
  - FOUND-04
must_haves:
  truths:
    - tsconfig.json enforces full strict mode with Bundler moduleResolution and @/* alias mapped to ./app/*.
    - app.config.ts configures Vinxi with node-server preset and vite-tsconfig-paths plugin.
    - app/client.tsx hydrates the document using React 19 hydrateRoot and StartClient.
    - app/ssr.tsx handles incoming server requests via progressive HTML streaming with defaultStreamHandler.
    - app/router.tsx creates the router instance and augments Register interface for type-safe routing.
    - app/server/ is established as an isolated backend boundary anchored with .gitkeep.
    - npm run typecheck and npm run build succeed with zero errors.
  artifacts:
    - tsconfig.json
    - app.config.ts
    - app/router.tsx
    - app/client.tsx
    - app/ssr.tsx
    - app/routes/__root.tsx
    - app/routes/index.tsx
    - app/server/.gitkeep
    - public/favicon.svg
  key_links:
    - app.config.ts reads tsconfig.json via vite-tsconfig-paths to resolve @/* imports across client and SSR bundles.
    - app/client.tsx and app/ssr.tsx share the router instance created by app/router.tsx.
    - app/routes/__root.tsx provides the outer HTML document shell, meta tags, and asset link pipeline.
---

<objective>
Configure strict TypeScript settings (`tsconfig.json`), Vinxi application bundler (`app.config.ts`), backend isolation boundary (`app/server/`), static asset root (`public/`), and complete TanStack Start entry points (`app/router.tsx`, `app/client.tsx`, `app/ssr.tsx`, `app/routes/__root.tsx`, `app/routes/index.tsx`).

Purpose: Deliver the full-document SSR streaming and client hydration architecture required by FOUND-02, FOUND-03, and FOUND-04, verifying type safety and successful Vinxi bundling.
Output: `tsconfig.json`, `app.config.ts`, `app/server/.gitkeep`, `public/favicon.svg`, `app/router.tsx`, `app/client.tsx`, `app/ssr.tsx`, `app/routes/__root.tsx`, `app/routes/index.tsx`, and generated `app/routeTree.gen.ts`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-tanstack-start-full-stack-tooling-foundation/19-CONTEXT.md
@.planning/phases/19-tanstack-start-full-stack-tooling-foundation/19-RESEARCH.md
@.planning/phases/19-tanstack-start-full-stack-tooling-foundation/19-PATTERNS.md
@.planning/phases/19-tanstack-start-full-stack-tooling-foundation/19-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Strict tsconfig.json, app.config.ts, Backend Boundary Anchor, and Public Asset Directory</name>
  <files>tsconfig.json, app.config.ts, app/server/.gitkeep, public/favicon.svg</files>
  <read_first>.planning/phases/19-tanstack-start-full-stack-tooling-foundation/19-RESEARCH.md, .planning/phases/19-tanstack-start-full-stack-tooling-foundation/19-PATTERNS.md</read_first>
  <action>
    1. Create `tsconfig.json` honoring locked decisions D-02 and D-15:
       - `target`: `"ES2022"`, `lib`: `["DOM", "DOM.Iterable", "ES2022"]`, `module`: `"ESNext"`
       - `moduleResolution`: `"Bundler"`
       - `resolveJsonModule`: `true`, `allowJs`: `true`
       - `strict`: `true`, `noEmit`: `true`, `jsx`: `"react-jsx"`
       - `esModuleInterop`: `true`, `isolatedModules`: `true`, `skipLibCheck`: `true`
       - `baseUrl`: `"."`, `paths`: `{ "@/*": ["./app/*"] }`
       - `include`: `["app", "app.config.ts"]`
       - `exclude`: `["node_modules", ".output", ".vinxi", "tests"]`
    2. Create `app.config.ts` honoring locked decisions D-02 and D-11:
       - Import `defineConfig` from `@tanstack/start/config`
       - Import `tsConfigPaths` from `vite-tsconfig-paths`
       - Configure `server: { preset: 'node-server' }` for standalone Node.js production deployment
       - Configure `vite: { plugins: [tsConfigPaths({ projects: ['./tsconfig.json'] })] }`
    3. Create directory `app/server/` and add `app/server/.gitkeep` honoring locked decision D-12 to anchor the isolated backend boundary.
    4. Create directory `public/` and copy `favicon.svg` into `public/favicon.svg` honoring locked decision D-06 for standard static asset serving at root `/`.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const check = ['tsconfig.json', 'app.config.ts', 'app/server/.gitkeep', 'public/favicon.svg'].every(f => fs.existsSync(f)); if (!check) { console.error('Missing required files'); process.exit(1); }"</automated>
    <fails_when>non-zero exit code or any required config/asset file is missing</fails_when>
  </verify>
  <done>`tsconfig.json`, `app.config.ts`, `app/server/.gitkeep`, and `public/favicon.svg` created and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Router Factory, Document Route Shell, SSR Streaming Handler, and Client Hydration Entry Points</name>
  <files>app/routes/__root.tsx, app/routes/index.tsx, app/router.tsx, app/client.tsx, app/ssr.tsx</files>
  <read_first>.planning/phases/19-tanstack-start-full-stack-tooling-foundation/19-RESEARCH.md, .planning/phases/19-tanstack-start-full-stack-tooling-foundation/19-PATTERNS.md</read_first>
  <action>
    1. Create `app/routes/__root.tsx` honoring locked decisions D-04 and D-07:
       - Import `createRootRoute`, `Outlet`, `ScrollRestoration` from `@tanstack/react-router`
       - Import `Meta`, `Scripts` from `@tanstack/react-start`
       - Define root route with `head()` emitting utf-8 charset, responsive viewport, page title, and favicon link (`/favicon.svg`)
       - Implement `RootDocument` rendering `<html>`, `<head>`, `<Meta />`, `<body>`, `{children}`, `<ScrollRestoration />`, `<Scripts />`
    2. Create `app/routes/index.tsx` as baseline placeholder route:
       - Import `createFileRoute` from `@tanstack/react-router`
       - Render baseline header and platform indicator confirming full-stack foundation active on Port 3173
    3. Create `app/router.tsx` honoring TanStack Start standards:
       - Import `createRouter as createTanStackRouter` from `@tanstack/react-router`
       - Import `routeTree` from `./routeTree.gen` (auto-generated by TanStack Router plugin)
       - Export `createRouter()` factory function with `scrollRestoration: true`
       - Augment module `'@tanstack/react-router'` with interface `Register { router: ReturnType<typeof createRouter> }`
    4. Create `app/client.tsx` honoring locked decision D-04:
       - Add directive `/// <reference types="vinxi/types/client" />`
       - Import `hydrateRoot` from `react-dom/client`
       - Import `StartClient` from `@tanstack/react-start`
       - Import `createRouter` from `./router`
       - Instantiate router and call `hydrateRoot(document, <StartClient router={router} />)`
    5. Create `app/ssr.tsx` honoring locked decision D-03:
       - Add directive `/// <reference types="vinxi/types/server" />`
       - Import `createStartHandler`, `defaultStreamHandler` from `@tanstack/react-start/server`
       - Import `getRouterManifest` from `@tanstack/react-start/router-manifest`
       - Import `createRouter` from `./router`
       - Export default `createStartHandler({ createRouter, getRouterManifest })(defaultStreamHandler)`
    6. Execute `npm run build` which runs `vinxi build` (generating `app/routeTree.gen.ts` and compiling production output in `.output/server` and `.output/public`).
    7. Execute `npm run typecheck` (`tsc --noEmit`) to verify zero TypeScript errors across all created files.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
    <fails_when>non-zero exit code, TypeScript compilation errors, or Vinxi build failures</fails_when>
  </verify>
  <done>`app/router.tsx`, `app/client.tsx`, `app/ssr.tsx`, `app/routes/__root.tsx`, and `app/routes/index.tsx` implemented and verified via clean `npm run typecheck` and `npm run build` output.</done>
</task>

</tasks>

<threat_model>
| Threat Ref / Vector | Risk Description | Mitigation | Acceptance Gate |
|---|---|---|---|
| Hydration Mismatch | Browser-specific code (e.g. `window`, `localStorage`) accessed during SSR render causes React 19 hydration mismatch warnings | Clean static HTML shell in `__root.tsx`, pure SSR streaming in `ssr.tsx`, isolated client entry in `client.tsx` | `npm run build` succeeds, server starts without React 19 hydration mismatch warnings |
| Server Code Leakage | Backend secrets or Node.js built-ins accidentally bundled into client JavaScript bundle | Rigid architectural boundary with `app/server/` isolated and no direct server imports in route components | TypeScript compiler enforces import boundaries, bundle analysis confirms `.output/public` contains no server secrets |
| Relative Import Fragility | Deep nested path traversals (`../../components/...`) causing broken builds upon restructuring | Enforce `@/*` path alias mapping via `tsconfig.json` and `vite-tsconfig-paths` in `app.config.ts` | All `@/*` imports resolve successfully in both client and SSR build outputs |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Verify `tsconfig.json` exists with strict mode and `@/*` path alias.
- [ ] Verify `app.config.ts` exists with `node-server` preset and `vite-tsconfig-paths`.
- [ ] Verify `app/server/.gitkeep` exists.
- [ ] Verify `public/favicon.svg` exists.
- [ ] Verify `app/routes/__root.tsx` and `app/routes/index.tsx` exist.
- [ ] Verify `app/router.tsx`, `app/client.tsx`, and `app/ssr.tsx` exist.
- [ ] Run `npm run typecheck` and confirm exit 0 with 0 errors.
- [ ] Run `npm run build` and confirm exit 0, creating `.output/server/index.mjs` and `.output/public/`.
- [ ] Run `npm test` and confirm all 11 legacy tests continue passing.
</verification>

<success_criteria>
- All tasks completed successfully.
- Requirements FOUND-02, FOUND-03, and FOUND-04 satisfied.
- Strict typecheck and Vinxi production build pass without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/19-tanstack-start-full-stack-tooling-foundation/19-02-SUMMARY.md`
</output>
