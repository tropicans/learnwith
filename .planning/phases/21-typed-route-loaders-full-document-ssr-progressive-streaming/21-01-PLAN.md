---
phase: 21-typed-route-loaders-full-document-ssr-progressive-streaming
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/data/courses.ts
  - app/routes/index.tsx
  - app/routes/course.ai.tsx
  - app/routes/course.word.tsx
autonomous: true
requirements:
  - SSR-01
  - SSR-03
must_haves:
  truths:
    - Strongly-typed course data store (app/data/courses.ts) exports curriculum models and preloading functions for Agentic AI, Word ASN, and Hub courses.
    - Routes (app/routes/index.tsx, app/routes/course.ai.tsx, app/routes/course.word.tsx) define typed loader functions preloading curriculum on the server.
    - Dynamic route metadata (head) outputs contextual page titles, meta descriptions, and OpenGraph tags per route based on loaderData and query parameters.
    - TypeScript strict type checking passes with zero errors (npm run typecheck).
  artifacts:
    - app/data/courses.ts
    - app/routes/index.tsx
    - app/routes/course.ai.tsx
    - app/routes/course.word.tsx
  key_links:
    - app/routes/index.tsx consumes getCoursesList() inside its loader and renders cards using loaderData.
    - app/routes/course.ai.tsx consumes getCourseAiData(mode) inside its loader and renders module curriculum with dynamic head title.
    - app/routes/course.word.tsx consumes getCourseWordData() inside its loader and renders bab syllabus with dynamic head title.
---

<objective>
Implement the strongly-typed curriculum data layer (`app/data/courses.ts`), typed route loaders across all routes, and dynamic document metadata (`head()`) for SEO, social graph, and instant tab labeling.

Purpose: Deliver SSR-01 (server preloading without layout shifts) and SSR-03 (dynamic route metadata).
Output: `app/data/courses.ts`, updated `app/routes/index.tsx`, `app/routes/course.ai.tsx`, and `app/routes/course.word.tsx`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-typed-route-loaders-full-document-ssr-progressive-streaming/21-RESEARCH.md
@.planning/phases/21-typed-route-loaders-full-document-ssr-progressive-streaming/21-PATTERNS.md
@.planning/phases/21-typed-route-loaders-full-document-ssr-progressive-streaming/21-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Strongly-Typed Course Data Layer and Syllabus Loader Functions</name>
  <files>app/data/courses.ts</files>
  <read_first>.planning/phases/21-typed-route-loaders-full-document-ssr-progressive-streaming/21-PATTERNS.md, assets/js/state.js</read_first>
  <action>
    1. Create `app/data/courses.ts`.
    2. Define TypeScript interfaces:
       - `CourseModule`: `id`, `num`, `title`, `subtitle`, `estimatedMinutes`, `checkpoints`
       - `CourseData`: `id`, `title`, `subtitle`, `description`, `category`, `badge`, `modulesCount`, `checkpointsCount`, `modules`, `sessionMode`
       - `CourseStats`: `activeParticipants`, `completionRate`, `lastUpdated`
    3. Export preloading data functions:
       - `getCoursesList()`: Returns summary array for Frontpage Hub (`ai` and `word`).
       - `getCourseAiData(mode: string)`: Returns Agentic AI curriculum tailored for `pretraining` (Modules 1-5, 3 Checkpoints) or `live-class` (Modules 6-10).
       - `getCourseWordData()`: Returns Pengolahan Kata Tingkat Lanjut curriculum (Bab I - Bab V, 28 checklist items, 3 checkpoints, Pergub DKI No. 14/2020 references).
       - `getCourseStatsAsync(courseId: string)`: Asynchronous function simulating stats calculation for progressive streaming.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>TypeScript type error or syntax error in app/data/courses.ts</fails_when>
  </verify>
  <done>Course data layer created with strict TypeScript types and preloading functions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Route Loaders and Dynamic Head Metadata on Hub and Course Workspaces</name>
  <files>app/routes/index.tsx, app/routes/course.ai.tsx, app/routes/course.word.tsx</files>
  <read_first>.planning/phases/21-typed-route-loaders-full-document-ssr-progressive-streaming/21-PATTERNS.md, app/data/courses.ts</read_first>
  <action>
    1. Update `app/routes/index.tsx`:
       - Add `loader: async () => { const courses = await getCoursesList(); return { courses }; }`.
       - Add `head: () => ({ meta: [{ title: "learnwith — Pusat Workshop & Ruang Belajar Terpadu" }, { name: "description", content: "Platform Pembelajaran Praktik Komputer & AI Interaktif untuk ASN & Profesional" }, { property: "og:title", content: "learnwith — Pusat Workshop & Ruang Belajar Terpadu" }, { property: "og:type", content: "website" }] })`.
       - Render course cards dynamically using `Route.useLoaderData()`.
    2. Update `app/routes/course.ai.tsx`:
       - Add `loader: async ({ search }) => { const course = await getCourseAiData(search.mode); return { course }; }`.
       - Add `head: ({ loaderData, params }) => { ... }` dynamically generating page title ("Hands-on Agentic AI (Pra-Training)" vs "Hands-on Agentic AI (Hari-H Praktik)") and OpenGraph tags.
       - Render course curriculum header and module cards from `loaderData.course`.
    3. Update `app/routes/course.word.tsx`:
       - Add `loader: async () => { const course = await getCourseWordData(); return { course }; }`.
       - Add `head: ({ loaderData }) => ({ meta: [{ title: "Pengolahan Kata Tingkat Lanjut (ASN) — learnwith Yudhi" }, { name: "description", content: "Standardisasi Dokumen Kedinasan Sesuai Pergub DKI No. 14/2020" }, { property: "og:title", content: "Pengolahan Kata Tingkat Lanjut (ASN)" }, { property: "og:type", content: "website" }] })`.
       - Render course curriculum overview and bab list from `loaderData.course`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
    <fails_when>TypeScript or Vinxi compilation errors</fails_when>
  </verify>
  <done>Typed route loaders and dynamic metadata implemented and compiling cleanly.</done>
</task>

</tasks>

<threat_model>
| Threat Ref / Vector | Risk Description | Mitigation | Acceptance Gate |
|---|---|---|---|
| T-21-01 Loader Data Pollution | Malformed or poisoned search parameters cause server loader crash or unhandled promise rejection | Validate all search params via Zod with .catch() before passing to data loaders; loaders handle missing values gracefully | Server never throws unhandled rejection during SSR route loading |
| T-21-02 Incomplete Meta SSR | Social crawler or browser tab displays blank or generic title due to missing loader data | Provide safe fallback titles and descriptions in head() function when loaderData is undefined | All rendered HTML contains descriptive <title> and OpenGraph tags |
</threat_model>

<verification>
- [ ] `app/data/courses.ts` exports `getCoursesList`, `getCourseAiData`, `getCourseWordData`.
- [ ] `app/routes/index.tsx` defines typed `loader` and `head`.
- [ ] `app/routes/course.ai.tsx` defines typed `loader` and dynamic `head`.
- [ ] `app/routes/course.word.tsx` defines typed `loader` and dynamic `head`.
- [ ] `npm run typecheck && npm run build` passes with 0 errors.
</verification>

<success_criteria>
- Typed route loaders eliminate client-side loading flashes for curriculum structures.
- Route head metadata renders server-side in the initial HTML document stream.
</success_criteria>

<output>
Create `.planning/phases/21-typed-route-loaders-full-document-ssr-progressive-streaming/21-01-SUMMARY.md` after execution.
</output>
