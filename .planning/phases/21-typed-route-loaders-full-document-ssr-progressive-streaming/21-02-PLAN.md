---
phase: 21-typed-route-loaders-full-document-ssr-progressive-streaming
plan: "02"
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - app/components/ui/Skeleton.tsx
  - app/components/course/ChecklistIsland.tsx
  - app/routes/course.ai.tsx
  - app/routes/course.word.tsx
autonomous: true
requirements:
  - SSR-02
  - SSR-04
must_haves:
  truths:
    - Reusable skeleton loader components (app/components/ui/Skeleton.tsx) render animated pulse placeholders matching the NotebookLM design system.
    - Route loaders integrate defer() and <Await> inside React Suspense boundaries to stream asynchronous stats and module details over progressive HTML chunks.
    - Client-only interactive widgets accessing localStorage or browser events are wrapped in <ClientOnly> boundaries, preventing React hydration mismatch errors.
    - Production build compiles cleanly with zero TypeScript errors.
  artifacts:
    - app/components/ui/Skeleton.tsx
    - app/components/course/ChecklistIsland.tsx
    - app/routes/course.ai.tsx
    - app/routes/course.word.tsx
  key_links:
    - app/routes/course.ai.tsx renders <Suspense fallback={<StatsSkeleton />}> with <Await promise={deferredStats}>.
    - app/components/course/ChecklistIsland.tsx wraps interactive checklist checkboxes with <ClientOnly fallback={<ChecklistSkeleton />}>.
---

<objective>
Implement progressive HTML streaming with React Suspense fallbacks using TanStack Router `defer` & `<Await>` (SSR-02), and encapsulate client-side state widgets within `<ClientOnly>` boundaries to guarantee zero hydration mismatch (SSR-04).

Purpose: Deliver SSR-02 (progressive streaming) and SSR-04 (hydration safety).
Output: `app/components/ui/Skeleton.tsx`, `app/components/course/ChecklistIsland.tsx`, and updated workspace routes.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-typed-route-loaders-full-document-ssr-progressive-streaming/21-RESEARCH.md
@.planning/phases/21-typed-route-loaders-full-document-ssr-progressive-streaming/21-PATTERNS.md
@.planning/phases/21-typed-route-loaders-full-document-ssr-progressive-streaming/21-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Skeleton Fallback Components and Progressive Streaming Integration</name>
  <files>app/components/ui/Skeleton.tsx, app/routes/course.ai.tsx, app/routes/course.word.tsx</files>
  <read_first>.planning/phases/21-typed-route-loaders-full-document-ssr-progressive-streaming/21-PATTERNS.md</read_first>
  <action>
    1. Create `app/components/ui/Skeleton.tsx`:
       - `SkeletonLine`: pulse text line placeholder with configurable width.
       - `CardSkeleton`: pulse container for notebook cards.
       - `StatsSkeleton`: pulse container for live workshop metrics.
       - `ChecklistSkeleton`: pulse container for interactive checklist tasks.
    2. Update `app/routes/course.ai.tsx` and `app/routes/course.word.tsx`:
       - Update loaders to return deferred promises via `defer(getCourseStatsAsync(course.id))`.
       - In route components, wrap the deferred stats block with:
         ```tsx
         <Suspense fallback={<StatsSkeleton />}>
           <Await promise={deferredStats}>
             {(stats) => <StatsDisplay stats={stats} />}
           </Await>
         </Suspense>
         ```
       - Ensure initial critical HTML renders immediately while secondary stats stream in.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
    <fails_when>TypeScript or bundle build errors</fails_when>
  </verify>
  <done>Skeleton components created and progressive streaming with Suspense & Await integrated.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Client-Only Hydration Boundary for Interactive Checklist Widgets</name>
  <files>app/components/course/ChecklistIsland.tsx, app/routes/course.ai.tsx, app/routes/course.word.tsx</files>
  <read_first>.planning/phases/21-typed-route-loaders-full-document-ssr-progressive-streaming/21-PATTERNS.md, assets/js/state.js</read_first>
  <action>
    1. Create `app/components/course/ChecklistIsland.tsx`:
       - Import `ClientOnly` from `@tanstack/react-router`.
       - Define `ChecklistIsland`: reads participant progress from `localStorage` safely on the client.
       - Include toggleable checklist items with immediate feedback and visual checkmarks.
       - Wrap with:
         ```tsx
         <ClientOnly fallback={<ChecklistSkeleton />}>
           <InteractiveChecklist courseId={courseId} modules={modules} />
         </ClientOnly>
         ```
    2. Embed `ChecklistIsland` into `app/routes/course.ai.tsx` and `app/routes/course.word.tsx`.
    3. Verify that the server render produces the `<ChecklistSkeleton />` without executing `localStorage` calls during SSR, and smoothly hydrates on the client.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
    <fails_when>TypeScript compilation or build error</fails_when>
  </verify>
  <done>Client-only checklist island implemented with zero hydration mismatch vulnerabilities.</done>
</task>

</tasks>

<threat_model>
| Threat Ref / Vector | Risk Description | Mitigation | Acceptance Gate |
|---|---|---|---|
| T-21-03 Unbounded Stream Hang | A stalled or rejected deferred promise halts the SSR stream and exhausts server connection pools | Provide finite timeout simulation and error handling on deferred promises; React Suspense boundaries gracefully handle rejections | Server response streams complete within timeout threshold; fallback renders if promise fails |
| T-21-04 Hydration Mismatch | Discrepancy between server HTML and client-rendered DOM caused by localStorage causes React warning #418/#423 and UI flicker | Enforce <ClientOnly fallback={<Skeleton />}> boundary for all components reading localStorage or browser navigator | Zero hydration warnings in client browser console |
</threat_model>

<verification>
- [ ] `app/components/ui/Skeleton.tsx` exports `StatsSkeleton`, `ChecklistSkeleton`, `CardSkeleton`.
- [ ] `app/components/course/ChecklistIsland.tsx` uses `ClientOnly` from `@tanstack/react-router`.
- [ ] Routes use `Suspense` and `<Await>` for deferred streaming.
- [ ] `npm run typecheck && npm run build` completes with 0 errors.
</verification>

<success_criteria>
- Progressive streaming renders server HTML progressively.
- Interactive widgets operate within `<ClientOnly>` with zero hydration mismatch.
</success_criteria>

<output>
Create `.planning/phases/21-typed-route-loaders-full-document-ssr-progressive-streaming/21-02-SUMMARY.md` after execution.
</output>
