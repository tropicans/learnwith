---
phase: 01-core-foundation-shell
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: [assets/js/state.js, assets/js/search.js, assets/js/app.js]
autonomous: true
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - Theme switch toggles data-theme="dark" / "light" and persists preference in localStorage.
    - Global search filters visible sections in real-time, displays matching count, and highlights query terms.
    - Progress controller calculates completion percentage from state and updates header & sidebar indicators.
    - Mobile sidebar drawer opens and closes smoothly with touch/click handlers.
  artifacts:
    - assets/js/state.js
    - assets/js/search.js
    - assets/js/app.js
  key_links:
    - index.html includes scripts for state.js, search.js, and app.js.
---

<objective>
Implement core client-side JavaScript controllers for theme management, responsive navigation drawer, real-time search filtering, and progress tracking calculation.

Purpose: Bring the UI shell to life with interactive navigation, theme persistence, search engine, and reactive progress metrics.
Output: `assets/js/state.js`, `assets/js/search.js`, and `assets/js/app.js`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-foundation-shell/01-01-PLAN.md
@.planning/phases/01-core-foundation-shell/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create State Management & Theme Controller</name>
  <files>assets/js/state.js</files>
  <read_first>.planning/phases/01-core-foundation-shell/01-RESEARCH.md</read_first>
  <action>
    Create `assets/js/state.js` with:
    1. `AppState` module with methods:
       - `init()`: Load stored state from `localStorage` (key: `pretraining_app_state_v1`).
       - `getState()`: Returns current state object (theme, checklist items, checkpoint states, active section, participant info).
       - `setTheme(theme)`: Updates theme attribute on `document.documentElement` and saves to `localStorage`.
       - `toggleTheme()`: Toggles between 'dark' and 'light'.
       - `updateChecklist(id, completed)`: Updates step completion state and dispatches custom `stateChange` event.
       - `updateCheckpoint(id, status)`: Updates checkpoint status ('pending', 'passed', 'failed').
       - `calculateProgress()`: Computes total items, completed items, and progress percentage.
       - `resetState()`: Clears stored progress and resets to default.
    2. Event dispatcher to notify listeners whenever state updates.
  </action>
  <verify>Check `assets/js/state.js` exports and methods</verify>
  <acceptance_criteria>
    - `assets/js/state.js` contains `AppState` with `setTheme`, `calculateProgress`, and `localStorage` persistence.
  </acceptance_criteria>
  <done>State management and theme controller implemented in assets/js/state.js</done>
</task>

<task type="auto">
  <name>Task 2: Create Real-Time Search & Highlighting Engine</name>
  <files>assets/js/search.js</files>
  <read_first>assets/js/state.js</read_first>
  <action>
    Create `assets/js/search.js` with:
    1. `SearchEngine` module:
       - `buildIndex()`: Scans all cards, guide steps, glossary terms, and troubleshooting items in the DOM.
       - `search(query)`: Filters searchable elements based on title, content, keywords, and code snippets.
       - Debounced input handler (150ms).
       - Visual highlight helper: wraps matching terms in `<mark class="search-highlight">`.
       - Empty state / no results notification.
       - Keyboard shortcut listener (`Ctrl+K`, `/`, `Escape` to focus/clear search).
  </action>
  <verify>Check `assets/js/search.js` search and highlighting functions</verify>
  <acceptance_criteria>
    - `assets/js/search.js` handles query input, element filtering, and keyword highlighting.
  </acceptance_criteria>
  <done>Real-time search engine implemented in assets/js/search.js</done>
</task>

<task type="auto">
  <name>Task 3: Create App Controller & Navigation Wireup</name>
  <files>assets/js/app.js</files>
  <read_first>index.html, assets/js/state.js, assets/js/search.js</read_first>
  <action>
    Create `assets/js/app.js` with:
    1. DOMContentLoaded initialization:
       - Initialize `AppState` and apply saved theme.
       - Initialize `SearchEngine`.
       - Setup mobile drawer toggle (hamburger button click, backdrop click, link navigation auto-close).
       - Setup smooth scroll & active link highlighting for sidebar navigation.
       - Setup reactive progress bar & readiness percentage updater listening to `stateChange`.
       - Global toast notification helper: `showToast(message, type = 'info', duration = 3000)`.
    2. Link script tags in `index.html` before `</body>`.
  </action>
  <verify>Check `assets/js/app.js` and verify integration in `index.html`</verify>
  <acceptance_criteria>
    - `assets/js/app.js` wires up theme toggle, drawer, navigation, progress UI, and toast alerts.
    - `index.html` includes script tags for `state.js`, `search.js`, and `app.js`.
  </acceptance_criteria>
  <done>App controller and navigation interactions wired up in assets/js/app.js and index.html</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify JavaScript files have clean ES6 syntax without errors.
- [ ] Verify theme switches cleanly between dark and light modes.
- [ ] Verify progress bar updates reactively.
- [ ] Verify search input filters items and responds to keyboard shortcuts (`Ctrl+K`, `/`).
</verification>

<success_criteria>
- All tasks completed
- Requirements UI-01, UI-02, and UI-03 fully supported in Phase 1 shell
- Zero build steps or external dependencies required
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation-shell/01-03-SUMMARY.md`
</output>
