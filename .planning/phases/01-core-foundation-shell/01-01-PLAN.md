---
phase: 01-core-foundation-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [assets/css/main.css, assets/css/components.css]
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - CSS custom properties define a harmonious dark and light color system, typography, shadows, borders, and spacing tokens.
    - Component styles support glassmorphic cards, badges, buttons, tooltips, alert callouts, and smooth animations.
    - Responsive layout media queries accommodate desktop (>1024px), tablet (768px-1023px), and mobile (<768px).
  artifacts:
    - assets/css/main.css
    - assets/css/components.css
  key_links:
    - CSS tokens in main.css are consumed by components in components.css.
---

<objective>
Build the design system and CSS styling layer for the Pre-Training Interactive Web App.

Purpose: Provide a modern, responsive, and aesthetic user interface with dark/light mode themes, accessible typography, glassmorphic card containers, buttons, and transition animations.
Output: `assets/css/main.css` and `assets/css/components.css`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-foundation-shell/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Main Design System & Theme CSS</name>
  <files>assets/css/main.css</files>
  <read_first>.planning/phases/01-core-foundation-shell/01-RESEARCH.md</read_first>
  <action>
    Create `assets/css/main.css` with:
    1. CSS Reset (box-sizing border-box, margin/padding reset, body font-smoothing).
    2. Theme variables (`:root` for light mode and `[data-theme="dark"]` for dark mode default):
       - Palette: Background base, surface, surface-border, text-primary, text-secondary, text-muted, accent-blue, accent-indigo, accent-emerald, accent-amber, accent-rose.
       - Typography tokens: font-family (Inter, system sans-serif), font-sizes (xs to 3xl), font-weights (400, 500, 600, 700), line-heights.
       - Shadows, border-radii, transitions, and z-index layers.
    3. Global layout system:
       - App container with CSS Grid layout: header, fixed/sticky sidebar, scrollable main content, and footer.
       - Responsive breakpoints for mobile drawer sidebar and compact header.
  </action>
  <verify>Check file existence and verify CSS token definitions in `assets/css/main.css`</verify>
  <acceptance_criteria>
    - `assets/css/main.css` contains `:root` and `[data-theme="dark"]` theme variables.
    - `assets/css/main.css` includes responsive grid and layout styles.
  </acceptance_criteria>
  <done>CSS design system and layout foundations are created in assets/css/main.css</done>
</task>

<task type="auto">
  <name>Task 2: Create UI Components & Micro-Interactions CSS</name>
  <files>assets/css/components.css</files>
  <read_first>assets/css/main.css</read_first>
  <action>
    Create `assets/css/components.css` with:
    1. **Cards & Panels**: Glassmorphism cards with subtle border highlight, backdrop-filter blur, and hover elevation.
    2. **Badges & Tags**: Status badges for checkpoints (Passed, Pending, Failed), version indicators, and pill tags.
    3. **Buttons**: Primary, secondary, outline, icon buttons with active/hover states and ripple/pulse effects.
    4. **Code Blocks & Copy Buttons**: Dark terminal-style code containers, monospaced font, inline copy button with tooltip and transition.
    5. **Progress Bars**: Smooth gradient progress track with percentage fill animations.
    6. **Alert Callouts**: Information, warning ("Jangan instal Hermes dulu", "Jangan pilih provider dulu"), caution, and tip callout boxes.
    7. **Toasts & Modals**: Floating notification toasts and backdrop modal dialogs for clean interactions.
  </action>
  <verify>Check file existence and verify component classes in `assets/css/components.css`</verify>
  <acceptance_criteria>
    - `assets/css/components.css` contains `.card`, `.badge`, `.btn`, `.code-block`, `.progress-bar`, `.alert-box`, and `.toast` styles.
  </acceptance_criteria>
  <done>UI component styles and animations are defined in assets/css/components.css</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify `assets/css/main.css` and `assets/css/components.css` exist and have valid CSS syntax.
- [ ] Verify color tokens for both dark and light themes are declared.
- [ ] Verify responsive media queries are defined for viewport widths.
</verification>

<success_criteria>
- All tasks completed
- CSS design system files created without external build dependencies
- Requirement UI-01 foundation met
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation-shell/01-01-SUMMARY.md`
</output>
