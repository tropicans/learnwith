---
phase: 01-core-foundation-shell
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [index.html]
autonomous: true
requirements: [UI-01, UI-03]
must_haves:
  truths:
    - HTML structure contains semantic markup (header, nav, main, sections, footer).
    - Top header displays app title, search bar, overall progress bar with percentage, and theme switch button.
    - Sidebar navigation provides links to all 5 guide modules, checkpoints, troubleshooting, and readiness report.
    - Mobile layout provides a slide-in drawer with toggle hamburger button and backdrop overlay.
  artifacts:
    - index.html
  key_links:
    - index.html links to assets/css/main.css and assets/css/components.css.
---

<objective>
Construct the main semantic HTML app shell and responsive navigation structure for the Pre-Training Interactive Web App.

Purpose: Provide the DOM structure for header, sidebar, main content views, search bar, and progress tracker.
Output: `index.html`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-foundation-shell/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Semantic HTML App Shell & Header</name>
  <files>index.html</files>
  <read_first>assets/css/main.css, assets/css/components.css</read_first>
  <action>
    Create `index.html` with:
    1. Meta tags (`viewport`, `description`, UTF-8 charset, title).
    2. Google Fonts (`Inter` font link).
    3. Stylesheet links (`assets/css/main.css`, `assets/css/components.css`).
    4. Top Header component:
       - Brand logo/icon and workshop title ("Pre-Training: Workshop Hermes Agent + 9Router").
       - Global Search Input with shortcut hint (`Ctrl+K` / `/`) and search icon.
       - Progress summary badge ("Kesiapan: 0%") and animated progress bar.
       - Dark/Light Theme toggle button with sun/moon SVG icons.
       - Mobile hamburger menu toggle button.
  </action>
  <verify>Check `index.html` structure and header markup</verify>
  <acceptance_criteria>
    - `index.html` contains `<header class="app-header">` with search input, theme toggle, and progress bar.
  </acceptance_criteria>
  <done>Semantic header and basic app shell constructed in index.html</done>
</task>

<task type="auto">
  <name>Task 2: Build Sidebar Navigation & Main Content Container</name>
  <files>index.html</files>
  <read_first>index.html</read_first>
  <action>
    Update `index.html` to add:
    1. Sidebar Navigation (`<aside class="app-sidebar">`):
       - Group 1: Pendahuluan & Ringkasan (Target Pre-Training, Aturan Keamanan, Alat & Persiapan).
       - Group 2: Modul Utama (Modul 1: Node.js, Modul 2: 9Router, Modul 3: Bot Telegram, Modul 4: Google Cloud).
       - Group 3: Checkpoints (Checkpoint 1, Checkpoint 2, Checkpoint 3).
       - Group 4: Bantuan & Laporan (Troubleshooting / Solusi Kendala, Redaction Helper, Form Laporan Kesiapan).
       - Section status badges (completed count pill).
    2. Main Content Area (`<main id="main-content" class="app-main">`):
       - Hero section welcoming participants and displaying quick start stats.
       - View container placeholders where interactive module sections, checkpoints, and troubleshooting cards will render.
    3. Toast Container and Modal Backdrops for dynamic notifications and dialogs.
  </action>
  <verify>Verify sidebar links and main container in `index.html`</verify>
  <acceptance_criteria>
    - `index.html` contains `<aside class="app-sidebar">` and `<main id="main-content">` with semantic section IDs.
  </acceptance_criteria>
  <done>Sidebar navigation and main layout structure completed in index.html</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify `index.html` links all CSS assets properly.
- [ ] Verify sidebar navigation contains entries for all 5 core modules and tools.
- [ ] Verify semantic accessibility tags (header, nav, main, aside, section, footer).
</verification>

<success_criteria>
- All tasks completed
- Responsive semantic DOM structure ready for interactive scripting
- Requirements UI-01 and UI-03 structural foundation fulfilled
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation-shell/01-02-SUMMARY.md`
</output>
