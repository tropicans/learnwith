---
phase: "09-multi-course-architecture-course-gate-protection"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/js/state.js
  - tests/multi-course.test.js
autonomous: true
requirements:
  - GATEWAY-01
  - GATEWAY-03
estimate:
  tokens: 25000
  raw_tokens: 12000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "StateManager supports active course context ('ai' or 'word') with namespaced storage keys"
    - "Existing pretraining state (pretraining_app_state_v1) is automatically and non-destructively migrated to learnwith_ai_state_v1"
    - "Changes in Course 1 never overwrite or mutate Course 2 state in localStorage"
  artifacts:
    - assets/js/state.js
    - tests/multi-course.test.js
  key_links:
    - "tests/multi-course.test.js verifies StateManager course switching, namespace isolation, and migration"
---

<objective>
Refactor StateManager to support multi-course namespacing (`learnwith_ai_*` vs `learnwith_word_*`) with an automated, non-destructive legacy migration layer for Course 1 (`Hands-on Agentic AI`), ensuring 100% backward compatibility with existing participant bookmarks and localStorage data.

Purpose: Guarantee zero cross-course interference and preserve existing public participant progress when introducing Course 2 (Pengolahan Kata Tingkat Lanjut).
Output: Refactored `assets/js/state.js` and automated unit test suite `tests/multi-course.test.js`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-multi-course-architecture-course-gate-protection/09-RESEARCH.md
@assets/js/state.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 Unit Test Suite for Multi-Course Namespacing & State Isolation</name>
  <files>tests/multi-course.test.js</files>
  <behavior>
    - Test default active course is 'ai'
    - Test legacy migration from pretraining_app_state_v1 to learnwith_ai_state_v1 preserves all completed checklists and checkpoints
    - Test switching to course 'word' loads learnwith_word_state_v1 with default word course state
    - Test updates to checklists/checkpoints in 'ai' do not bleed into 'word' state
    - Test resetState() only resets the currently active course state
  </behavior>
  <action>
    Create tests/multi-course.test.js establishing comprehensive assertions for StateManager multi-course operations:
    1. Define DOM/localStorage mock compatible with Node.js runner.
    2. Write test cases for default course ('ai'), active course getters/setters, namespace isolation, legacy migration without data loss, and scoped state resets.
    3. Ensure runner logs standard pass/fail counts and exits with code 0 when all tests pass.
  </action>
  <verify>
    <automated>node tests/multi-course.test.js</automated>
  </verify>
  <done>
    tests/multi-course.test.js exists, runs cleanly via node, and exercises course namespacing requirements GATEWAY-01 and GATEWAY-03.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Namespaced Storage & Migration Layer in StateManager</name>
  <files>assets/js/state.js</files>
  <action>
    Enhance StateManager in assets/js/state.js:
    1. Define COURSE_CONFIGS mapping course IDs ('ai', 'word') to their storage keys:
       - ai: 'learnwith_ai_state_v1' (with legacy fallback to 'pretraining_app_state_v1')
       - word: 'learnwith_word_state_v1'
    2. Add activeCourse property (defaulting to 'ai' or stored 'learnwith_active_course').
    3. Implement migrateLegacyStateIfNeeded() to copy data from 'pretraining_app_state_v1' to 'learnwith_ai_state_v1' without deleting the legacy key (ensuring legacy bookmarks and readers remain intact).
    4. Implement setCourse(courseId) and getActiveCourse() with 'courseChange' event emission.
    5. Update saveState(), loadState(), and resetState() to operate strictly within the active course's storageKey.
    6. Export COURSE_CONFIGS alongside StateManager for testing and UI consumption.
  </action>
  <verify>
    <automated>node tests/multi-course.test.js && node tests/mode-switcher.test.js</automated>
  </verify>
  <done>
    StateManager persists and retrieves course-scoped state seamlessly, passes all tests in tests/multi-course.test.js, and produces zero regressions in existing tests/mode-switcher.test.js.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage | State read/write boundary between courses and user sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | StateManager.loadState | medium | mitigate | Validate parsed JSON and sanitize courseId to known allowlist ('ai', 'word') to prevent arbitrary key overwrite |
| T-09-02 | Information Disclosure | State Migration | low | mitigate | Non-destructive read and copy only; do not serialize sensitive auth tokens in shared cross-course buffers |
</threat_model>

<verification>
Run both new and existing test suites:
- `node tests/multi-course.test.js`
- `node tests/mode-switcher.test.js`
- `node tests/live-class-modules.test.js`
</verification>

<success_criteria>
- StateManager isolates course data by key (`learnwith_ai_*` vs `learnwith_word_*`).
- Public Agentic AI participants experience zero loss of progress or broken states.
- All test suites pass with 0 failures.
</success_criteria>

<output>
Create `.planning/phases/09-multi-course-architecture-course-gate-protection/09-01-PLAN.md`
</output>
