---
phase: 02-interactive-modules-guide-engine
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [index.html, assets/js/search.js]
autonomous: true
requirements: [GUIDE-01, GUIDE-02, GUIDE-04, GUIDE-05]
must_haves:
  truths:
    - Module 1 (Node.js) contains Langkah A (cek versi), Langkah B (instalasi LTS), Langkah C (cek npm), and Checkpoint 1 intro.
    - Module 2 (9Router) contains Langkah A (instalasi global), Langkah B (menjalankan 9Router), Langkah C (login default 123456), and Langkah D (menghentikan Ctrl+C & restart).
    - Every PowerShell command has a 1-click copy button with instant feedback and toast.
    - Important caution callouts (e.g., initial password 123456, keep terminal open, Windows installer .msi 64-bit) are prominently displayed with custom alert designs.
    - Official external download links (https://nodejs.org) and localhost links (http://localhost:20128) are functional and safe.
  artifacts:
    - index.html
  key_links:
    - Copy buttons trigger setupCodeCopy in app.js.
    - Checkpoint 1 & 2 intros link forward to Checkpoint verification gates.
---

<objective>
Implement Module 1 (Node.js) and Module 2 (9Router) in `index.html` with full step-by-step guidance, copyable commands, terminal output badges, caution alerts, and external links.

Purpose: Provide participants with a clear, guided setup path for checking/installing Node.js and installing/running 9Router locally.
Output: Updated `index.html` with comprehensive Module 1 & Module 2 markup.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-interactive-modules-guide-engine/02-RESEARCH.md
@PANDUAN-PRE-TRAINING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Module 1 (Pemeriksaan & Instalasi Node.js) Markup</name>
  <files>index.html</files>
  <read_first>PANDUAN-PRE-TRAINING.md, index.html</read_first>
  <action>
    Add Section `#sec-module-1` to `index.html` inside `dynamic-modules-container`:
    1. **Module Header**: Badge "Modul 1", title "Pemeriksaan & Instalasi Node.js", step count indicator "3 Langkah", and collapse toggle.
    2. **Langkah A — Periksa apakah sudah terpasang**:
       - Command block with 1-click copy: `node --version`.
       - Expected output badge: `v24.x.x` (LTS yang disarankan) atau `v22.x.x`.
       - Guidance: Jika muncul `not recognized`, normal pada laptop baru; lanjutkan ke Langkah B.
    3. **Langkah B — Instal Node.js jika belum tersedia**:
       - Step-by-step list (1-10) based on `PANDUAN-PRE-TRAINING.md` Bagian 5.
       - Direct official link: `<a href="https://nodejs.org" target="_blank" rel="noopener noreferrer" class="external-link">https://nodejs.org ↗</a>`.
       - Caution alerts: Pilih versi **LTS**, pilih **Windows Installer (.msi) 64-bit/x64**, jangan ZIP/source code, pastikan opsi PATH & npm aktif, restart PowerShell setelah selesai.
    4. **Langkah C — Periksa npm**:
       - Command block with 1-click copy: `npm --version`.
       - Expected output badge: menampilkan nomor versi npm.
    5. **Checkpoint 1 Preview Banner**:
       - Summary card explaining that Checkpoint 1 is ready when both `node --version` and `npm --version` display version numbers.
  </action>
  <verify>Verify `index.html` contains `#sec-module-1` with all steps, code copy blocks, alerts, and external links.</verify>
  <acceptance_criteria>
    - `#sec-module-1` exists with Langkah A, B, C and interactive code blocks.
    - `node --version` and `npm --version` have copy buttons.
  </acceptance_criteria>
  <done>Module 1 is implemented in index.html with interactive steps and copyable commands.</done>
</task>

<task type="auto">
  <name>Task 2: Build Module 2 (Instalasi & Menjalankan 9Router) Markup</name>
  <files>index.html</files>
  <read_first>PANDUAN-PRE-TRAINING.md, index.html</read_first>
  <action>
    Add Section `#sec-module-2` to `index.html` inside `dynamic-modules-container`:
    1. **Module Header**: Badge "Modul 2", title "Instalasi & Menjalankan 9Router", step count indicator "4 Langkah", and collapse toggle.
    2. **Langkah A — Instalasi Global 9Router**:
       - Command block with 1-click copy: `npm install -g 9router`.
       - Visual badge: Menjelaskan proses instalasi membutuhkan beberapa menit, tulisan bergerak di layar normal, tunggu hingga kembali ke prompt `PS`.
    3. **Langkah B — Menjalankan 9Router**:
       - Command block with 1-click copy: `9router`.
       - Link to local dashboard: `<a href="http://localhost:20128" target="_blank" rel="noopener noreferrer" class="external-link">http://localhost:20128 ↗</a>` (and alternative `http://127.0.0.1:20128`).
       - Caution alerts: Jangan tutup jendela PowerShell/Terminal agar 9Router tetap berjalan. Jangan izinkan akses jaringan publik jika Windows Firewall memunculkan dialog.
    4. **Langkah C — Penanganan Login & Kata Sandi Awal**:
       - Plain text alert: Kata sandi awal default adalah `123456`.
       - Security callout: `123456` adalah kata sandi lokal 9Router, BUKAN kata sandi Google/Telegram. Jika diminta membuat kata sandi baru, simpan di catatan pribadi dan jangan dibagikan ke siapapun.
    5. **Langkah D — Menghentikan & Menjalankan Kembali**:
       - Command block for stop: `Ctrl+C` (lalu ketik `Y` jika diminta konfirmasi).
       - Command block for restart: `9router`.
    6. **Checkpoint 2 Preview Banner**:
       - Summary card showing Checkpoint 2 is passed when `http://localhost:20128` loads the 9Router dashboard in browser.
  </action>
  <verify>Verify `index.html` contains `#sec-module-2` with all steps, code copy blocks, password instructions, and alerts.</verify>
  <acceptance_criteria>
    - `#sec-module-2` exists with Langkah A, B, C, D and interactive code blocks.
    - `npm install -g 9router` and `9router` have copy buttons.
  </acceptance_criteria>
  <done>Module 2 is implemented in index.html with interactive steps and copyable commands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify `index.html` renders Module 1 and Module 2 cleanly with dark and light themes.
- [ ] Verify all code snippets in Modules 1 & 2 have functional 1-click copy buttons.
- [ ] Verify all external links have `target="_blank" rel="noopener noreferrer"`.
- [ ] Verify search indexes Module 1 & 2 contents properly.
</verification>

<success_criteria>
- All tasks completed
- Module 1 and Module 2 fully implemented and functional
- Requirements GUIDE-01, GUIDE-02, GUIDE-04, GUIDE-05 satisfied for Modules 1 & 2
</success_criteria>

<output>
After completion, create `.planning/phases/02-interactive-modules-guide-engine/02-02-SUMMARY.md`
</output>
