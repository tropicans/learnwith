# Milestone v1.1 Requirements Archive — Live Workshop Guide (Hari-H Praktik Kelas)

**Status:** ✅ ALL REQUIREMENTS SATISFIED (19/19)
**Shipped:** 2026-09-07
**Milestone:** v1.1

---

## Requirements Traceability & Outcome

### Mode Switcher & Navigation (MODE)
- [x] **MODE-01**: User can toggle between "Pra-Training (Persiapan)" and "Hari-H (Praktik Kelas)" via tab switchers in the header and sidebar.
  - *Outcome*: Validated in Phase 5 (`05-01`, `05-02`, unit tests). Header & sidebar segmented control tabs active.
- [x] **MODE-02**: Switching modes dynamically updates the sidebar navigation links, section visibility, and active progress counters while preserving state across modes.
  - *Outcome*: Validated in Phase 5 (`05-02`, unit tests). Dynamic visibility toggles, persistent mode storage, and non-destructive state preservation.

### In-Class Guide Modules (CLASS)
- [x] **CLASS-01**: User can follow collapsible interactive guides for Modul 6 (Menyamakan 9Router: provider, model, API key lokal, dan endpoint `http://localhost:20128/v1`).
  - *Outcome*: Validated in Phase 6 (`06-01`, unit tests). Interactive checklist and guidance card for 9Router local setup.
- [x] **CLASS-02**: User can follow Modul 7 with 1-click copy for Hermes Windows installation script (`iex (irm https://hermes-agent.nousresearch.com/install.ps1)`) and `hermes doctor` command.
  - *Outcome*: Validated in Phase 6 (`06-01`, unit tests). 1-click installer and doctor diagnostics guide with toast feedback.
- [x] **CLASS-03**: User can follow Modul 8 for `hermes setup` wizard (model, endpoint, API key) and testing Hermes response via 9Router.
  - *Outcome*: Validated in Phase 6 (`06-02`, unit tests). Complete wizard parameters walkthrough and test prompt verification.
- [x] **CLASS-04**: User can follow Modul 9 for Telegram Gateway interactive setup (`hermes gateway setup`, `hermes gateway start`, `status`, `stop`) with allowlist user ID enforcement.
  - *Outcome*: Validated in Phase 7 (`07-01`, unit tests). Long-polling gateway walkthrough with strict `TELEGRAM_ALLOWED_USERS` security.
- [x] **CLASS-05**: User can follow Modul 10 for Google Cloud OAuth 2.0 (Desktop app type), client secret JSON download, and Google Calendar skill authorization.
  - *Outcome*: Validated in Phase 7 (`07-02`, unit tests). Practice account guide, Desktop app Client ID configuration, and browser consent flow.
- [x] **CLASS-06**: User can follow Modul 11 for end-to-end testing prompts (read agenda, create dummy agenda) and post-workshop operational restart procedures.
  - *Outcome*: Validated in Phase 7 (`07-02`, unit tests). Copyable test prompts, dummy calendar verification, 5-step reboot SOP, and emergency token revocation.

### Live Workshop Checkpoints (GATE)
- [x] **GATE-01**: User can validate Checkpoint 4 (9Router active, model/provider synchronized, API key private) with Pass/Fail buttons.
  - *Outcome*: Validated in Phase 6 (`06-02`, unit tests). Checkpoint 4 card with reactive state sync.
- [x] **GATE-02**: User can validate Checkpoint 5 (`hermes doctor` passes and Hermes CLI is recognized) with Pass/Fail buttons.
  - *Outcome*: Validated in Phase 6 (`06-02`, unit tests). Checkpoint 5 card with reactive state sync.
- [x] **GATE-03**: User can validate Checkpoint 6 (Hermes test response received via 9Router) with Pass/Fail buttons.
  - *Outcome*: Validated in Phase 6 (`06-02`, unit tests). Checkpoint 6 card with reactive state sync.
- [x] **GATE-04**: User can validate Checkpoint 7 (Telegram bot replies to allowlisted User ID via long polling) with Pass/Fail buttons.
  - *Outcome*: Validated in Phase 7 (`07-01`, unit tests). Checkpoint 7 card with reactive state sync.
- [x] **GATE-05**: User can validate Checkpoint 8 (Google Calendar OAuth authorization active and calendar readable) with Pass/Fail buttons.
  - *Outcome*: Validated in Phase 7 (`07-02`, unit tests). Checkpoint 8 card with reactive state sync.
- [x] **GATE-06**: User can validate Checkpoint 9 (End-to-end calendar event action succeeded via Telegram) with Pass/Fail buttons.
  - *Outcome*: Validated in Phase 7 (`07-02`, unit tests). Checkpoint 9 card with reactive state sync.
- [x] **GATE-07**: State engine tracks in-class readiness and displays overall Live Workshop completion badge (`BELUM SIAP` -> `DALAM PRAKTIK` -> `SELESAI (SUKSES)`).
  - *Outcome*: Validated in Phase 8 (`08-02`, unit tests). `calculateLiveReadiness()` engine in `state.js` and dynamic status badge in `#sec-live-report`.

### In-Class Troubleshooting Hub (TRBL)
- [x] **TRBL-04**: User can browse and search 7 specific live workshop error solutions (`hermes` not recognized, 9Router endpoint connection, API key invalid, bot silence, `unauthorized` allowlist error, OAuth Desktop app mismatch, wrong Google Calendar).
  - *Outcome*: Validated in Phase 8 (`08-01`, unit tests). 7 SOP resolution cards in `#sec-live-troubleshooting`.
- [x] **TRBL-05**: User can filter troubleshooting cards specifically for in-class workshop categories (`9router-live`, `hermes-cli`, `telegram-gateway`, `google-oauth`).
  - *Outcome*: Validated in Phase 8 (`08-01`, unit tests). Dedicated category pill filter toolbar and real-time live search.

### Live Workshop Completion Exporter (RPT)
- [x] **RPT-04**: User can view a pre-filled Form Laporan Hasil Praktik Kelas summarizing Checkpoints 4–9 status, provider/model used, and end-to-end test results.
  - *Outcome*: Validated in Phase 8 (`08-02`, unit tests). Interactive form in `#sec-live-report` with real-time preview and character counter.
- [x] **RPT-05**: User can export the final workshop report via 1-click copy formatted for WhatsApp and Telegram instructor chats with sensitive credentials masked.
  - *Outcome*: Validated in Phase 8 (`08-02`, unit tests). Dual 1-click export buttons (WhatsApp & Telegram Markdown) integrated with regex credential redaction.

---

## Future Requirements (Carried Forward to v1.2+)
- **SAND-01**: In-browser interactive PowerShell terminal simulation sandbox for trying commands safely.
- **PING-01**: Live 9Router ping button using `fetch('http://localhost:20128/v1/models')` to test local connectivity directly from the browser.
- **I18N-01**: Multilingual toggle (Indonesian / English) across all guides and checklists.
- **PWA-01**: Service worker and Web App Manifest for complete offline PWA installation.
