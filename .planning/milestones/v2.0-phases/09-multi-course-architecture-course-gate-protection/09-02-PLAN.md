---
phase: "09-multi-course-architecture-course-gate-protection"
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - index.html
  - assets/css/components.css
  - assets/js/app.js
  - assets/js/search.js
  - tests/multi-course.test.js
autonomous: true
requirements:
  - GATEWAY-01
  - GATEWAY-02
estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "Top header and sidebar navigation contain accessible Course Switcher dropdown/controls"
    - "Course 1 (Agentic AI) is active and accessible by default without entering passcodes"
    - "Course 2 (Pengolahan Kata Tingkat Lanjut) is protected by a developer/instructor gate modal requiring passcode 'buka-kata' or URL parameter ?course=word&unlock=dev"
    - "Switching courses dynamically updates document title, brand subtitles, and toggles container-course-ai vs container-course-word without full page reload"
    - "Global search index rebuilds cleanly for the active course container"
  artifacts:
    - index.html
    - assets/css/components.css
    - assets/js/app.js
    - assets/js/search.js
    - tests/multi-course.test.js
  key_links:
    - "app.js wires CourseManager to StateManager.setCourse() and manages gate modal dialog"
    - "index.html wraps existing Course 1 views in container-course-ai and scaffolds container-course-word"
---

<objective>
Implement Course Switcher UI components in the application header and sidebar drawer, construct the Course 2 Developer Gate protection modal with passcode verification (`buka-kata`) and URL parameter bypass (`?course=word&unlock=dev`), and wire dynamic course container switching into the application controller.

Purpose: Allow instructors and developers to access Course 2 securely while keeping Course 1 immediately available to the public.
Output: Updated `index.html`, `assets/css/components.css`, `assets/js/app.js`, `assets/js/search.js`, and expanded test suite in `tests/multi-course.test.js`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-multi-course-architecture-course-gate-protection/09-RESEARCH.md
@.planning/phases/09-multi-course-architecture-course-gate-protection/09-01-PLAN.md
@index.html
@assets/js/app.js
@assets/js/search.js
@assets/css/components.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Multi-Course Containers and Course Switcher UI in index.html & CSS</name>
  <files>index.html, assets/css/components.css</files>
  <action>
    1. In index.html:
       - Wrap current Course 1 main contents (pretraining & live-class sections) inside a parent container `<div id="container-course-ai" class="course-container active">`.
       - Create placeholder container for Course 2: `<div id="container-course-word" class="course-container" style="display: none;">` with an introductory ASN workshop banner and outline ready for Phase 10 interactive modules.
       - Add Course Switcher component to `.header-brand` in the top header and inside the sidebar navigation drawer:
         - Displays current course icon and title.
         - Dropdown menu options: "Hands-on Agentic AI" (🤖) and "Pengolahan Kata Tingkat Lanjut (ASN)" (📝) with lock badge for Course 2.
       - Add modal markup `<div id="modal-wordcourse-locked" class="modal-backdrop" role="dialog">` with title "🔒 Modul Pengolahan Kata Tingkat Lanjut Terkunci", explanation for instructors/developers, passcode input (`input-word-unlock-code`), submit button, and unlock feedback element.
    2. In assets/css/components.css:
       - Style course dropdown menu, item hover states, active indicators, and lock badges with full dark/light theme support.
       - Style `.course-container` transitions and responsive sizing for mobile drawer.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const html=fs.readFileSync('index.html','utf8'); if(!html.includes('container-course-ai') || !html.includes('container-course-word') || !html.includes('modal-wordcourse-locked')) process.exit(1); console.log('Markup verified');"</automated>
  </verify>
  <done>
    Course containers, switcher dropdowns, and gate modal markup exist in index.html and are styled in components.css.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Course Controller, Passcode Gate & Search Re-Indexing in app.js & search.js</name>
  <files>assets/js/app.js, assets/js/search.js, tests/multi-course.test.js</files>
  <action>
    1. In assets/js/app.js:
       - Implement CourseManager functions:
         - isWordCourseUnlocked(): checks URL param (?course=word&unlock=dev or ?unlock=word) and localStorage key 'learnwith_word_unlocked'.
         - setWordCourseUnlocked(unlocked): persists key and updates lock badges in DOM.
         - switchCourse(courseId, updateUrl): switches active course, displays the corresponding container (#container-course-ai vs #container-course-word), toggles mode-switcher visibility (since mode switcher is specific to Course 1 dual-mode), updates header titles, and emits course changes.
         - setupCourseSwitcher(): binds click events on course dropdown options, handles gate modal presentation if attempting to access locked Course 2, and processes passcode input (validating against WORD_PASSCODES = ['buka-kata']).
       - Wire URL query param detection during DOMContentLoaded to auto-select and unlock course if params are present.
    2. In assets/js/search.js:
       - Scope buildIndex() to only index visible, searchable cards inside the active course container (`#container-course-ai` or `#container-course-word`), ensuring search results never bleed across courses.
    3. In tests/multi-course.test.js:
       - Add DOM integration tests verifying:
         - Default course is Course 1 ('ai') with container-course-ai visible and container-course-word hidden.
         - Attempting to switch to Course 2 when locked triggers the gate modal.
         - Submitting 'buka-kata' successfully unlocks Course 2 and switches container visibility.
         - URL query param '?course=word&unlock=dev' automatically unlocks and activates Course 2.
  </action>
  <verify>
    <automated>node tests/multi-course.test.js && node tests/mode-switcher.test.js && node tests/live-class-modules.test.js</automated>
  </verify>
  <done>
    Course switcher and developer gate are fully functional, interactive tests in tests/multi-course.test.js pass with 100% success rate, and all previous test suites remain green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client URL query string | Untrusted user query parameters entering unlock logic |
| Passcode input field | Text input evaluated for instructor/developer unlock |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03 | Elevation of Privilege | Course Unlock Gate | low | accept | Client-side static application; developer gate is an intentional soft barrier for stage rollout and instructor preview, not a financial/PII security boundary |
| T-09-04 | Tampering | DOM Search Indexing | medium | mitigate | Sanitize query inputs via textContent/innerText and restrict indexing to the active course DOM root |
</threat_model>

<verification>
Execute the full test suite across all modules:
- `node tests/multi-course.test.js`
- `node tests/mode-switcher.test.js`
- `node tests/live-class-modules.test.js`
- `node tests/search-security.test.js`
- `node tests/troubleshooting-exporter.test.js`
- `node tests/checkpoint-engine.test.js`
</verification>

<success_criteria>
- Course Switcher renders cleanly in header and sidebar.
- Course 1 remains the default public workshop without friction.
- Course 2 is gated behind passcode 'buka-kata' or '?course=word&unlock=dev'.
- Global search is properly scoped per active course.
- All automated tests pass without errors.
</success_criteria>

<output>
Create `.planning/phases/09-multi-course-architecture-course-gate-protection/09-02-PLAN.md`
</output>
