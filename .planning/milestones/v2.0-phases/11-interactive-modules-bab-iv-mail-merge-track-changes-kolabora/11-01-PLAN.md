---
phase: "11-interactive-modules-bab-iv-mail-merge-track-changes-kolabora"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/word-modules.test.js
  - assets/js/state.js
  - assets/js/app.js
autonomous: true
requirements:
  - WORD-04
  - WORD-05
estimate:
  tokens: 28000
  raw_tokens: 14000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "StateManager initializes WORD_DEFAULT_STATE with exactly 28 checklist tasks (Bab I: 4, Bab II: 8, Bab III: 8, Bab IV: 8) and 3 checkpoints (word-cp-1, word-cp-2, word-cp-3)"
    - "Bab IV checklist tasks (word-b4-prepare-source to word-b4-compare-combine) toggle and persist cleanly in learnwith_word_state_v1 without cross-course pollution"
    - "Checkpoint 3 (word-cp-3) transitions correctly between pending, passed, and failed with localStorage persistence"
    - "Course 2 progress calculation factors all 28 checklist tasks (60% weight) and all 3 checkpoints (40% weight)"
    - "calculateWordReadiness() requires all 3 checkpoints to be passed and >= 80% progress to achieve 'ready' status"
    - "app.js binds word-cp-3 action buttons and updates navigation badges for Bab IV and Checkpoint 3"
  artifacts:
    - tests/word-modules.test.js
    - assets/js/state.js
    - assets/js/app.js
  key_links:
    - "tests/word-modules.test.js verifies Bab IV state keys, Checkpoint 3 transitions, progress formulas, and DOM controller bindings"
---

<objective>
Implement the Wave 0 automated test suite extension and core State/Controller engine for Course 2 Bab IV (Mail Merge, Track Changes, Compare/Combine & Checkpoint 3).

This plan establishes:
1. Extended test suite (`tests/word-modules.test.js`) asserting Bab IV checklist tasks (8 tasks), Checkpoint 3 gate workflow, updated progress weighting (28 tasks + 3 checkpoints), and multi-checkpoint readiness logic.
2. State management extensions in `assets/js/state.js`: 8 Bab IV checklist tasks (`word-b4-*`), `word-cp-3` checkpoint in `WORD_DEFAULT_STATE`, progress calculation for all 4 chapters, and 3-checkpoint readiness evaluation.
3. Controller integration in `assets/js/app.js`: Checkpoint 3 event binding, action button handlers, and `#nav-group-word` badge updates for Bab IV and Checkpoint 3.

Purpose: Guarantee state isolation, deterministic checkpoint progression, and full automated test verification for Word Processing Bab IV before building interactive UI markup.
Output: Updated `tests/word-modules.test.js`, `assets/js/state.js`, and `assets/js/app.js`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-interactive-modules-bab-iv-mail-merge-track-changes-kolabora/11-RESEARCH.md
@.planning/phases/11-interactive-modules-bab-iv-mail-merge-track-changes-kolabora/11-VALIDATION.md
@.planning/phases/11-interactive-modules-bab-iv-mail-merge-track-changes-kolabora/11-PATTERNS.md
@assets/js/state.js
@assets/js/app.js
@tests/word-modules.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 Automated Test Suite Extension for Bab IV & Checkpoint 3</name>
  <files>tests/word-modules.test.js</files>
  <read_first>
    tests/word-modules.test.js
    assets/js/state.js
  </read_first>
  <behavior>
    - Suite 1: StateManager Course 2 Isolation & Default State (verifies 28 total checklist tasks across Bab I–IV, 8 Bab IV tasks: word-b4-prepare-source, word-b4-setup-mailmerge, word-b4-insert-fields, word-b4-merge-rules, word-b4-preview-finish, word-b4-track-changes, word-b4-comments-resolve, word-b4-compare-combine; 3 checkpoints: word-cp-1, word-cp-2, word-cp-3; zero bleed into Course 1).
    - Suite 2: Checkpoint 3 Gate Workflow (transitions pending -> passed -> failed -> reset, persistence in localStorage).
    - Suite 3: Course 2 Weighted Progress & Multi-Checkpoint Readiness (0% on clean state; 28 tasks = 60%; 3 cps = 40%; all 3 cps passed + >= 80% tasks = ready; any cp failed = clinic).
    - Suite 4: DOM Element & Attribute Integrity for Bab IV (verifies required sections #sec-word-module-4, #sec-word-cp-3, #card-word-cp-3, #status-card-word-cp3, buttons btn-word-cp-3-passed/failed/pending, #badge-nav-word-b4, #status-nav-word-cp3).
    - Suite 5: Course Switcher & Navigation Synchronization (verifies updateCourse2NavBadges correctly syncs #badge-nav-word-b4 and #status-nav-word-cp3).
  </behavior>
  <action>
    1. Update mock DOM definitions in `tests/word-modules.test.js` to include `#sec-word-module-4`, `#sec-word-cp-3`, `#card-word-cp-3`, `#status-card-word-cp3`, `#badge-nav-word-b4`, `#status-nav-word-cp3`, and action buttons for `word-cp-3`.
    2. Add unit assertions in Suite 1 verifying the 8 Bab IV keys exist and default to `false`, and `word-cp-3` defaults to `'pending'`.
    3. Add unit assertions in Suite 2 verifying `setCheckpointStatus('word-cp-3', ...)` updates state and persists.
    4. Add unit assertions in Suite 3 verifying `calculateProgress()` and `calculateWordReadiness()` with all 3 checkpoints.
    5. Add unit assertions in Suite 4 and 5 verifying DOM mocks and navigation badge synchronization for Bab IV.
  </action>
  <acceptance_criteria>
    - `tests/word-modules.test.js` contains assertions for `word-b4-prepare-source`, `word-b4-setup-mailmerge`, `word-b4-insert-fields`, `word-b4-merge-rules`, `word-b4-preview-finish`, `word-b4-track-changes`, `word-b4-comments-resolve`, and `word-b4-compare-combine`
    - `tests/word-modules.test.js` contains assertions for `word-cp-3` checkpoint status transitions ('pending' -> 'passed' -> 'failed' -> 'pending')
    - `tests/word-modules.test.js` asserts 28 total checklist tasks and 3 checkpoints in Course 2 default state
    - `cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-modules.test.js"` executes under Node.js
  </acceptance_criteria>
  <verify>
    <automated>cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-modules.test.js"</automated>
    <fails_when>non-zero exit code or failing assertions in tests/word-modules.test.js</fails_when>
  </verify>
  <done>
    `tests/word-modules.test.js` contains full test coverage for Bab IV and Checkpoint 3, and fails predictably only on unextended StateManager/app.js features or passes once Tasks 2 and 3 are implemented.
  </done>
</task>

<task type="auto">
  <name>Task 2: StateManager Extensions for Bab IV Checklists, Checkpoint 3 & Multi-Checkpoint Readiness</name>
  <files>assets/js/state.js</files>
  <read_first>
    assets/js/state.js
    tests/word-modules.test.js
  </read_first>
  <action>
    Modify `assets/js/state.js`:
    1. In `WORD_DEFAULT_STATE.checklists`, add the 8 Bab IV keys:
       - `'word-b4-prepare-source': false`
       - `'word-b4-setup-mailmerge': false`
       - `'word-b4-insert-fields': false`
       - `'word-b4-merge-rules': false`
       - `'word-b4-preview-finish': false`
       - `'word-b4-track-changes': false`
       - `'word-b4-comments-resolve': false`
       - `'word-b4-compare-combine': false`
    2. In `WORD_DEFAULT_STATE.checkpoints`, add:
       - `'word-cp-3': 'pending'`
    3. In `calculateProgress(mode)`:
       - Update the Course 2 task filter so `taskKeys` includes `k.startsWith('word-b4-')` alongside `word-b1-`, `word-b2-`, and `word-b3-`.
       - Keep checkpoint filtering `k.startsWith('word-cp-')` (which automatically picks up `word-cp-1`, `word-cp-2`, and `word-cp-3`).
       - Ensure 60% task weight and 40% checkpoint weight are calculated across all 28 tasks and 3 checkpoints.
    4. In `calculateWordReadiness()`:
       - Update `cpIds` to `['word-cp-1', 'word-cp-2', 'word-cp-3']`.
       - Update description messages to explicitly state "Bab I–IV" and "Checkpoint 1, 2, & 3".
       - Return `status: 'ready'` only when all 3 checkpoints are `'passed'` and `progress.percentage >= 80`.
  </action>
  <acceptance_criteria>
    - `assets/js/state.js` `WORD_DEFAULT_STATE.checklists` includes all 8 `word-b4-*` keys
    - `assets/js/state.js` `WORD_DEFAULT_STATE.checkpoints` includes `'word-cp-3': 'pending'`
    - `assets/js/state.js` `calculateProgress()` includes `word-b4-` in Course 2 task filter
    - `assets/js/state.js` `calculateWordReadiness()` evaluates `word-cp-1`, `word-cp-2`, and `word-cp-3`
    - Node test command `cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-modules.test.js"` exits with 0
  </acceptance_criteria>
  <verify>
    <automated>cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-modules.test.js"</automated>
    <fails_when>non-zero exit code or failing assertions in tests/word-modules.test.js</fails_when>
  </verify>
  <done>
    All StateManager unit tests in `tests/word-modules.test.js` for Bab IV default state, checklist toggling, Checkpoint 3 status, weighted progress math, and readiness evaluation pass cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Checkpoint 3 Event Binding & Navigation Badge Synchronization in Controller</name>
  <files>assets/js/app.js</files>
  <read_first>
    assets/js/app.js
    assets/js/state.js
  </read_first>
  <action>
    Modify `assets/js/app.js`:
    1. In `bindCheckpointEvents()`, add `'word-cp-3'` to the list of Course 2 checkpoints:
       `['word-cp-1', 'word-cp-2', 'word-cp-3'].forEach((cpId) => { ... });`
    2. In `updateCourse2NavBadges()`:
       - Add progress badge update for `#badge-nav-word-b4`:
         ```javascript
         const b4Badge = document.getElementById('badge-nav-word-b4');
         if (b4Badge) {
           const b4Prog = stateManager.getModuleProgress('word-b4-');
           b4Badge.textContent = `${b4Prog.completed}/${b4Prog.total}`;
           b4Badge.className = `badge badge-pill ${b4Prog.completed === b4Prog.total && b4Prog.total > 0 ? 'badge-success' : 'badge-neutral'}`;
         }
         ```
       - Add status badge update for `#status-nav-word-cp3`:
         ```javascript
         const cp3NavBadge = document.getElementById('status-nav-word-cp3');
         if (cp3NavBadge) {
           const st = stateManager.getCheckpointStatus('word-cp-3');
           cp3NavBadge.textContent = st.charAt(0).toUpperCase() + st.slice(1);
           cp3NavBadge.className = `badge badge-pill ${st === 'passed' ? 'badge-success' : st === 'failed' ? 'badge-danger' : 'badge-warning'}`;
         }
         ```
    3. Ensure `syncWordUI()` calls `updateCourse2NavBadges()` and updates `#card-word-readiness-status` and `#readiness-word-badge` with the new 3-checkpoint readiness data.
  </action>
  <acceptance_criteria>
    - `assets/js/app.js` handles `word-cp-3` click events on `.btn-cp-action`
    - `assets/js/app.js` updates `#badge-nav-word-b4` with `${completed}/${total}` format
    - `assets/js/app.js` updates `#status-nav-word-cp3` with 'Passed', 'Failed', or 'Pending'
    - Node test command `cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-modules.test.js && node tests/multi-course.test.js"` exits with 0
  </acceptance_criteria>
  <verify>
    <automated>cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-modules.test.js && node tests/multi-course.test.js"</automated>
    <fails_when>non-zero exit code or failing assertions in tests/word-modules.test.js or tests/multi-course.test.js</fails_when>
  </verify>
  <done>
    Automated test suites pass with zero failures, confirming Checkpoint 3 button binding, status badge updates, and navigation badge synchronization work properly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser localStorage | State read/write boundary between courses and user sessions (`learnwith_ai_state_v1` vs `learnwith_word_state_v1`) |
| DOM Event Listeners | Client-side user input via checkbox clicks, checkpoint gate buttons, and course switcher controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-01 | Tampering | StateManager.loadState | medium | mitigate | Validate parsed JSON against extended `WORD_DEFAULT_STATE` schema; sanitize invalid checkpoint states to 'pending'; ensure keys from Course 1 cannot pollute Course 2 |
| T-11-02 | Tampering | updateCheckpointCardUI | low | mitigate | Allowlist valid checkpoint status values ('pending', 'passed', 'failed') to prevent DOM class injection |
| T-11-03 | Information Disclosure | Storage Namespacing | low | mitigate | Ensure separate storage keys (`learnwith_word_state_v1` and `learnwith_ai_state_v1`) so participant progress and notes never leak across courses |
</threat_model>

<verification>
Run the automated test suite:
`cmd /c "set PATH=C:\nvm4w\nodejs;%PATH% && node tests/word-modules.test.js && node tests/multi-course.test.js"`
</verification>

<success_criteria>
- `tests/word-modules.test.js` passes with 100% green assertions for Bab IV and Checkpoint 3.
- StateManager cleanly isolates `learnwith_word_state_v1` with 28 tasks and 3 checkpoints.
- Course 2 checklists (28 items) and checkpoints (3 gates) calculate weighted progress accurately.
</success_criteria>

## Artifacts this phase produces
- `WORD_DEFAULT_STATE.checklists['word-b4-prepare-source']`
- `WORD_DEFAULT_STATE.checklists['word-b4-setup-mailmerge']`
- `WORD_DEFAULT_STATE.checklists['word-b4-insert-fields']`
- `WORD_DEFAULT_STATE.checklists['word-b4-merge-rules']`
- `WORD_DEFAULT_STATE.checklists['word-b4-preview-finish']`
- `WORD_DEFAULT_STATE.checklists['word-b4-track-changes']`
- `WORD_DEFAULT_STATE.checklists['word-b4-comments-resolve']`
- `WORD_DEFAULT_STATE.checklists['word-b4-compare-combine']`
- `WORD_DEFAULT_STATE.checkpoints['word-cp-3']`
- `#sec-word-module-4`
- `#sec-word-cp-3`
- `#card-word-cp-3`
- `#status-card-word-cp3`
- `#badge-nav-word-b4`
- `#status-nav-word-cp3`
