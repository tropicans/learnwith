---
phase: "06"
plan: "02"
title: "Modul 8 Setup Wizard Markup, Checkpoint 6 Gate, StateManager Extension & Tests"
wave: 2
dependencies:
  - "06-01"
requirements:
  - CLASS-03
  - GATE-03
files_modified:
  - index.html
  - assets/js/state.js
  - assets/js/app.js
  - tests/checkpoint-engine.test.js
  - tests/live-class-modules.test.js
  - tests/index.html
autonomous: true
must_haves:
  truths:
    - Modul 8 provides full step-by-step guidance for running `hermes setup`, selecting model, entering OpenAI-compatible base URL `http://localhost:20128/v1`, inserting local 9Router API key, and performing a quick test prompt.
    - Checkpoint 6 card is fully rendered with Pass, Fail, and Reset buttons (`data-checkpoint="cp-6"`).
    - StateManager tracks new checklists for Modul 6, 7, and 8 (`m6-*`, `m7-*`, `m8-*`) and new checkpoints (`cp-4`, `cp-5`, `cp-6`).
    - Checkpoint gates controller in `app.js` updates and renders Checkpoints 4, 5, and 6 seamlessly alongside Checkpoints 1, 2, and 3.
    - Automated tests in `tests/live-class-modules.test.js` verify checklist tracking, checkpoint status transitions, persistence in localStorage, and module progress calculation for live class modules.
  artifacts:
    - path: index.html
      provides: Rich interactive section for #sec-module-8 and #card-cp-6 checkpoint gate
    - path: assets/js/state.js
      provides: Default state tracking for m6, m7, m8 checklist tasks and cp-4, cp-5, cp-6 checkpoints
    - path: assets/js/app.js
      provides: Updated setupCheckpointGates() handling cp-1 through cp-6 and dynamic sidebar badge counters for live modules
    - path: tests/live-class-modules.test.js
      provides: Automated tests for Phase 6 modules, checklists, and checkpoint gates
    - path: tests/index.html
      provides: Test runner harness script inclusion for live-class-modules.test.js
  key_links:
    - from: assets/js/app.js
      to: assets/js/state.js
      via: updateCheckpoint and getModuleProgress calls for live class modules
    - from: tests/live-class-modules.test.js
      to: assets/js/state.js
      via: StateManager testing with cp-4, cp-5, cp-6
---

<objective>
Implement Modul 8 (`hermes setup` wizard) and Checkpoint 6 gate in `index.html`, extend `StateManager` and `app.js` to manage Checkpoints 4–6 and checklists for Modul 6, 7, and 8, and create an automated test suite verifying state management and UI synchronization for Phase 6.

Purpose: Allow participants to connect Hermes Agent to 9Router with verified test responses, and ensure all Phase 6 interactive states are persisted and backed by automated tests.
Output: Production markup in `index.html`, extended `state.js` & `app.js`, new test suite `tests/live-class-modules.test.js`, and harness update in `tests/index.html`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@PANDUAN-PRAKTIK-KELAS.md
@index.html
@assets/js/state.js
@assets/js/app.js
@tests/checkpoint-engine.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Modul 8 markup and Checkpoint 6 gate card</name>
  <files>index.html</files>
  <action>
    1. In `index.html`, locate `#sec-module-8` in `#container-liveclass`.
    2. Replace the placeholder with a full interactive module container featuring:
       - Module header: "Modul 8: Full Setup & Koneksi 9Router", icon ⚙️, target duration (15 Menit).
       - Step 1: Running the setup wizard with 1-click copy:
         `hermes setup`
       - Step 2: Choosing the provider/model configuration matching instructor dry-run.
       - Step 3: Entering the OpenAI-compatible base URL with 1-click copy:
         `http://localhost:20128/v1`
       - Step 4: Entering participant's local 9Router API key (emphasizing keeping it private on local laptop).
       - Step 5: Test response prompt execution inside Hermes CLI to verify connection.
       - Note on re-configuring: Press Enter to keep default/existing values.
       - Interactive checklist items with `data-task-id`:
         - `m8-run-setup`: Menjalankan wizard `hermes setup` di terminal.
         - `m8-enter-endpoint`: Memasukkan endpoint `http://localhost:20128/v1`.
         - `m8-enter-key`: Memasukkan API key 9Router lokal.
         - `m8-test-response`: Menerima respons uji dari Hermes melalui perute model 9Router.
       - Checkpoint 6 Card (`#card-cp-6`):
         - Header with badge icon "6" and status badge `#status-card-cp6` ("Pending").
         - Pass criteria list (wizard selesai, base URL sesuai, Hermes berhasil menjawab respons uji).
         - Action button group: "Lolos Verifikasi" (`data-checkpoint="cp-6" data-status="passed"`), "Ada Kendala (Gagal)" (`data-checkpoint="cp-6" data-status="failed"`), "Reset Status" (`data-checkpoint="cp-6" data-status="pending"`).
  </action>
  <verify>
    Verify element IDs and structure in index.html.
  </verify>
  <done>
    Modul 8 and Checkpoint 6 gate are completely rendered and aligned with instructor class guidelines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend StateManager and app.js for Checkpoints 4-6 and live modules</name>
  <files>assets/js/state.js, assets/js/app.js</files>
  <action>
    1. In `assets/js/state.js`:
       - Add new checklist items to `DEFAULT_STATE.checklists`:
         - Modul 6: `'m6-run-router': false`, `'m6-open-dashboard': false`, `'m6-select-model': false`, `'m6-create-key': false`
         - Modul 7: `'m7-install-cli': false`, `'m7-restart-shell': false`, `'m7-run-doctor': false`
         - Modul 8: `'m8-run-setup': false`, `'m8-enter-endpoint': false`, `'m8-enter-key': false`, `'m8-test-response': false`
       - Add new checkpoints to `DEFAULT_STATE.checkpoints`:
         - `'cp-4': 'pending'`, `'cp-5': 'pending'`, `'cp-6': 'pending'`
       - Add participant info fields to `DEFAULT_STATE.participantInfo`:
         - `routerProvider: ''`, `routerModel: ''`
       - Ensure `calculateReadiness()` only checks Checkpoints 1-3 for pretraining mode, or cleanly isolates pretraining vs live-class readiness.
    2. In `assets/js/app.js`:
       - Update `setupCheckpointGates()` to include `['cp-1', 'cp-2', 'cp-3', 'cp-4', 'cp-5', 'cp-6']`.
       - Update `updateProgressUI()` to update live class sidebar badges (`badge-nav-m6`, `badge-nav-m7`, `badge-nav-m8`) with completed/total tasks.
  </action>
  <verify>
    Run Node check `node --check assets/js/state.js` and `node --check assets/js/app.js`.
  </verify>
  <done>
    StateManager and App controller seamlessly manage Checkpoints 4–6 and Module 6–8 tasks.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build automated tests for Phase 6 modules and integrate with test runner</name>
  <files>tests/live-class-modules.test.js, tests/index.html</files>
  <action>
    1. Create `tests/live-class-modules.test.js` testing:
       - StateManager tracking of Modul 6, 7, 8 checklists (`m6-*`, `m7-*`, `m8-*`).
       - Checkpoint gates 4, 5, and 6 transitions ('pending' -> 'passed' / 'failed').
       - Module progress calculations for `m6-`, `m7-`, and `m8-`.
       - Persistence to and restoration from localStorage for Phase 6 state.
       - Existing pre-training state preservation during live class updates.
    2. Add `<script src="live-class-modules.test.js"></script>` in `tests/index.html`.
    3. Run all test suites (including checkpoint-engine, mobile-accessibility, search-security, troubleshooting-exporter, mode-switcher, and live-class-modules) to ensure 100% pass rate.
  </action>
  <verify>
    Run all test suites using Node CLI and confirm 0 failures across all suites.
  </verify>
  <done>
    All Phase 6 requirements (CLASS-01, CLASS-02, CLASS-03, GATE-01, GATE-02, GATE-03) are verified by automated tests with 100% success rate.
  </done>
</task>

</tasks>

<threat_model>
- Regression risk: Pre-training progress and Checkpoints 1–3 calculations must remain unaffected by the addition of Checkpoints 4–6.
- LocalStorage schema migration: Default state merging must safely handle existing participant state without dropping previously completed tasks.
</threat_model>
