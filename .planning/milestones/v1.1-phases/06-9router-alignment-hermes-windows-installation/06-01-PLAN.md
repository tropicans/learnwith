---
phase: "06"
plan: "01"
title: "Interactive Guide Cards & Checkpoints 4-5 Markup for Modul 6 & 7"
wave: 1
dependencies: []
requirements:
  - CLASS-01
  - CLASS-02
  - GATE-01
  - GATE-02
files_modified:
  - index.html
  - assets/css/components.css
autonomous: true
must_haves:
  truths:
    - Modul 6 provides step-by-step guidance for running 9Router locally, choosing the designated model/provider, generating an API key privately in dashboard, and recording values in participant quick inputs.
    - Checkpoint 4 card is fully rendered inside or directly adjacent to Modul 6 with Pass, Fail, and Reset buttons (`data-checkpoint="cp-4"`).
    - Modul 7 provides 1-click copy for the official Windows native installer `iex (irm https://hermes-agent.nousresearch.com/install.ps1)` and diagnostic command `hermes doctor`, with explanation of terminal restart requirement.
    - Checkpoint 5 card is fully rendered with Pass, Fail, and Reset buttons (`data-checkpoint="cp-5"`).
    - All code snippets in Modul 6 and 7 use `.code-container` with `.code-copy-btn` for seamless 1-click copying.
    - Checklists in Modul 6 and 7 use standard `.checklist-checkbox` with unique `data-task-id` attributes (`m6-run-router`, `m6-select-model`, `m6-create-key`, `m6-record-config`, `m7-install-cli`, `m7-restart-shell`, `m7-run-doctor`).
  artifacts:
    - path: index.html
      provides: Rich interactive sections for #sec-module-6, #sec-module-7, and checkpoint cards #card-cp-4, #card-cp-5
    - path: assets/css/components.css
      provides: Enhanced styling for live workshop module cards, quick notes, and command boxes if needed
  key_links:
    - from: index.html
      to: assets/js/app.js
      via: data-task-id attributes and data-checkpoint attributes
---

<objective>
Author rich, accessible, beginner-friendly HTML content for Modul 6 (9Router Model Alignment) and Modul 7 (Hermes Windows Native Installation & Doctor Check) in `index.html` under `#container-liveclass`, complete with interactive step checklists, 1-click command copy blocks, warning alerts against secret sharing, and Checkpoint Gates 4 & 5.

Purpose: Provide participants with hands-on, unambiguous instructions to align 9Router and install Hermes native CLI on Windows without confusion or credentials leakage.
Output: Production markup in `index.html` and any supporting responsive component styles in `assets/css/components.css`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@PANDUAN-PRAKTIK-KELAS.md
@index.html
@assets/css/components.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Modul 6 markup and Checkpoint 4 gate card</name>
  <files>index.html</files>
  <action>
    1. In `index.html`, locate `#sec-module-6` in `#container-liveclass`.
    2. Replace the placeholder with a full interactive module container featuring:
       - Module header: "Modul 6: Penyelarasan Provider & Model 9Router", icon 🟢, target duration (15 Menit).
       - Context callout: Why 9Router must align with instructor-chosen model, why local OpenAI-compatible endpoint (`http://localhost:20128/v1`) is used.
       - Step 1: Running `9router` in dedicated terminal (with 1-click copy `9router`).
       - Step 2: Opening local dashboard `http://localhost:20128` and choosing the designated class model.
       - Step 3: Generating local API key from dashboard with strict alert box warning against pasting keys into Zoom chat/screen share.
       - Step 4: Quick-note inputs for participants to record their provider name and model name locally (fields: `data-participant-field="routerProvider"`, `data-participant-field="routerModel"`).
       - Interactive checklist items with unique `data-task-id` attributes:
         - `m6-run-router`: Service 9Router aktif di terminal terpisah.
         - `m6-open-dashboard`: Dashboard terbuka di `http://localhost:20128`.
         - `m6-select-model`: Provider dan nama model disamakan sesuai arahan instruktur.
         - `m6-create-key`: API key dibuat secara mandiri di dashboard dan disimpan privat.
       - Checkpoint 4 Card (`#card-cp-4`):
         - Header with badge icon "4" and status badge `#status-card-cp4` ("Pending").
         - Pass criteria list (dashboard aktif, provider/model sesuai, API key tersedia privat).
         - Action button group: "Lolos Verifikasi" (`data-checkpoint="cp-4" data-status="passed"`), "Ada Kendala (Gagal)" (`data-checkpoint="cp-4" data-status="failed"`), "Reset Status" (`data-checkpoint="cp-4" data-status="pending"`).
  </action>
  <verify>
    Verify elements exist in index.html and validate HTML structure.
  </verify>
  <done>
    Modul 6 and Checkpoint 4 are fully rendered with interactive checklists, 1-click copy boxes, participant config inputs, and verification buttons.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Modul 7 markup and Checkpoint 5 gate card</name>
  <files>index.html, assets/css/components.css</files>
  <action>
    1. In `index.html`, locate `#sec-module-7` in `#container-liveclass`.
    2. Replace the placeholder with full interactive module container featuring:
       - Module header: "Modul 7: Instalasi Hermes di Windows Native", icon ⚡, target duration (25 Menit).
       - Callout: Explain Hermes Agent is the autonomous engine, installed via official PowerShell native script.
       - Step 1: Official 1-click copy installation script:
         `iex (irm https://hermes-agent.nousresearch.com/install.ps1)`
       - Step 2: Restart PowerShell note: If `hermes` is not recognized, close all PowerShell windows and open a new one (path refresh).
       - Step 3: Diagnostics command with 1-click copy:
         `hermes doctor`
       - Step 4: Understanding Doctor output (Python runtime, CLI bin path, system environment checks).
       - Interactive checklist items with `data-task-id`:
         - `m7-install-cli`: Script instalasi resmi Hermes dijalankan di PowerShell.
         - `m7-restart-shell`: Jendela PowerShell dibuka ulang agar PATH ter-refresh.
         - `m7-run-doctor`: Perintah `hermes doctor` berhasil dijalankan tanpa kendala kritis.
       - Checkpoint 5 Card (`#card-cp-5`):
         - Header with badge icon "5" and status badge `#status-card-cp5` ("Pending").
         - Pass criteria list (`hermes doctor` berjalan, CLI dikenali, siap menjalankan setup wizard).
         - Action button group: "Lolos Verifikasi" (`data-checkpoint="cp-5" data-status="passed"`), "Ada Kendala (Gagal)" (`data-checkpoint="cp-5" data-status="failed"`), "Reset Status" (`data-checkpoint="cp-5" data-status="pending"`).
    3. If necessary, adjust CSS in `assets/css/components.css` to ensure checkpoint cards and live class elements render cleanly.
  </action>
  <verify>
    Run `node tests/mobile-accessibility.test.js` to ensure CSS custom property validity and zero mobile overflow.
  </verify>
  <done>
    Modul 7 and Checkpoint 5 are fully rendered with 1-click command copy, step checklists, and Pass/Fail verification gates.
  </done>
</task>

</tasks>

<threat_model>
- Secrets leakage: Explicit prominent warning alerts and input sandboxing prevent participants from sharing credentials in Zoom or chat.
- Mobile layout regression: All new interactive elements and button groups maintain >= 44px touch targets and fluid wrapping.
</threat_model>
