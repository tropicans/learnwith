---
phase: "08"
plan: "02"
title: "Live Workshop Completion Status Engine, Form Laporan Hasil Praktik & Test Suite Verification"
wave: 2
dependencies:
  - "08-01"
requirements:
  - GATE-07
  - RPT-04
  - RPT-05
files_modified:
  - index.html
  - assets/js/state.js
  - assets/js/app.js
  - tests/live-class-modules.test.js
autonomous: true
must_haves:
  truths:
    - `StateManager` implements `calculateLiveReadiness()` calculating in-class workshop status based on Checkpoints 4–9 (`BELUM SIAP` when any CP 4-9 is failed or progress < 50%, `DALAM PRAKTIK` when 50%-99% and no failed CP, and `SELESAI (SUKSES)` when all 6 checkpoints are passed).
    - Section `#sec-live-report` in `index.html` hosts the interactive Form Laporan Hasil Praktik Kelas featuring participant name input, model selection input, status indicator badge, live preview workbench, and dual export buttons.
    - Two 1-click export actions are implemented: WhatsApp formatted export (`btn-copy-live-report-wa`) and Telegram Markdown formatted export (`btn-copy-live-report-tg`), both with automatic sensitive token redaction via `sanitizeLogText()`.
    - Automated tests in `tests/live-class-modules.test.js` are extended to thoroughly test `calculateLiveReadiness()`, live report text generation for both WhatsApp and Telegram formats, error log redaction, and in-class troubleshooting category filtering.
    - All automated tests in the project pass with 100% success rate.
  artifacts:
    - path: assets/js/state.js
      provides: calculateLiveReadiness() method and state bindings for Live Class status
    - path: index.html
      provides: Rich interactive Form Laporan Hasil Praktik Kelas in #sec-live-report
    - path: assets/js/app.js
      provides: generateLiveReportText() function, setupLiveReport() controller, and dual export handlers
    - path: tests/live-class-modules.test.js
      provides: Unit and regression test suites for Phase 8 status calculation, export generation, and filtering
  key_links:
    - from: index.html
      to: assets/js/app.js
      via: #btn-copy-live-report-wa and #btn-copy-live-report-tg click events
    - from: assets/js/app.js
      to: assets/js/state.js
      via: calculateLiveReadiness() and calculateProgress('live-class') calls
    - from: tests/live-class-modules.test.js
      to: assets/js/state.js
      via: testing calculateLiveReadiness across pending, clinic, and success states
---

<objective>
Implement the Live Workshop completion status evaluator (`calculateLiveReadiness`), replace the placeholder in `#sec-live-report` with the full Form Laporan Hasil Praktik Kelas with 1-click WhatsApp and Telegram exports, wire up the controllers in `app.js`, and expand `tests/live-class-modules.test.js` to ensure 100% automated test pass rate.

Purpose: Provide participants and instructors with a verified, automated mechanism to evaluate in-class mastery (Checkpoints 4–9) and generate clear, redacted summary reports for course completion.
Output: Updated `state.js`, `app.js`, `index.html`, and new test suites in `tests/live-class-modules.test.js`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-in-class-troubleshooting-hub-completion-status-engine-final-report-exporter/08-CONTEXT.md
@.planning/phases/08-in-class-troubleshooting-hub-completion-status-engine-final-report-exporter/08-01-PLAN.md
@PANDUAN-PRAKTIK-KELAS.md
@index.html
@assets/js/state.js
@assets/js/app.js
@tests/live-class-modules.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add calculateLiveReadiness() method to StateManager in assets/js/state.js</name>
  <files>assets/js/state.js</files>
  <action>
    In `assets/js/state.js`:
    1. Implement `calculateLiveReadiness()` on `StateManager`:
       - Scope checkpoints to `['cp-4', 'cp-5', 'cp-6', 'cp-7', 'cp-8', 'cp-9']`.
       - Retrieve values from `this.state.checkpoints`.
       - Compute progress via `this.calculateProgress('live-class')`.
       - If any checkpoint has status `'failed'` or `progress.percentage < 50`:
         Return `{ status: 'clinic', label: '⚠️ BELUM SIAP / BUTUH BANTUAN', badgeClass: 'badge-danger', color: 'var(--color-danger)', description: 'Terdapat kendala pada satu atau lebih gerbang checkpoint in-class. Silakan periksa Pusat Bantuan Kendala atau tanyakan pada instruktur.' }`.
       - If all 6 checkpoints have status `'passed'` (100% checkpoints passed):
         Return `{ status: 'ready', label: '🎉 SELESAI (SUKSES)', badgeClass: 'badge-success', color: 'var(--color-success)', description: 'Selamat! Seluruh gerbang checkpoint Hari-H Praktik Kelas (CP 4 - CP 9) berhasil diselesaikan. Integrasi Telegram dan Google Calendar Anda telah terverifikasi penuh!' }`.
       - Otherwise:
         Return `{ status: 'in_progress', label: '⏳ DALAM PRAKTIK', badgeClass: 'badge-warning', color: 'var(--color-warning)', description: 'Sesi praktik kelas sedang berlangsung. Lanjutkan modul berikutnya dan selesaikan verifikasi Checkpoint 4 hingga 9.' }`.
    2. Emit event when readiness changes or expose helper for live UI updates.
  </action>
</task>

<task type="auto">
  <name>Task 2: Build Form Laporan Hasil Praktik Kelas in index.html and app.js</name>
  <files>index.html, assets/js/app.js</files>
  <action>
    1. In `index.html`, replace the Phase 8 placeholder in `#sec-live-report`:
       - Title: "Laporan Hasil Praktik Kelas (Hari-H)" with badge indicator `#badge-live-readiness`.
       - Workbench layout (`.report-workbench` / `.report-grid`):
         - Left: Form input pane:
           - Input `#input-live-report-name`: Nama Lengkap Peserta.
           - Input `#input-live-report-model`: Model LLM yang Digunakan (default: `hermes-3-llama-3.1-8b` atau model 9Router pilihan).
           - Select `#select-live-report-os`: Sistem Operasi Laptop.
           - Input `#input-live-report-problem-step`: Kendala / Langkah Terakhir (default: 'Nihil / Sukses Penuh').
           - Textarea `#input-live-report-error-msg`: Pesan Error Terminal (Opsional, otomatis disensor).
         - Right: Preview pane:
           - Readonly preformatted text preview `#live-report-output-preview`.
           - Action toolbar with two buttons:
             - `#btn-copy-live-report-wa` (`.btn-success`): "📋 Salin Format WhatsApp"
             - `#btn-copy-live-report-tg` (`.btn-primary`): "✈️ Salin Format Telegram"
    2. In `assets/js/app.js`:
       - Implement `generateLiveReportText(format = 'whatsapp', overrides = {})`:
         - Extracts participant info and checkpoints `cp-4` through `cp-9`.
         - Marks Checkpoints 4 to 9 with `[X]` (if passed) or `[ ]` (if failed/pending).
         - Integrates `StateManager.calculateLiveReadiness()`.
         - Applies `sanitizeLogText()` on terminal error message.
         - Formats output cleanly:
           - WhatsApp format uses `*` for bold headers and bullet points.
           - Telegram format uses markdown headers, bullet points, and backtick code blocks for error log.
       - Implement `setupLiveReport()`:
         - Event listeners for live inputs (`input`, `change`) to update preview dynamically.
         - Click handlers on `#btn-copy-live-report-wa` and `#btn-copy-live-report-tg` with clipboard copy and toast notification feedback (`showToast`).
         - Update `#badge-live-readiness` dynamically on checkpoint changes.
       - Initialize `setupLiveReport()` on DOM load.
  </action>
</task>

<task type="auto">
  <name>Task 3: Expand test coverage in tests/live-class-modules.test.js</name>
  <files>tests/live-class-modules.test.js</files>
  <action>
    In `tests/live-class-modules.test.js`, add:
    1. `Suite 8: Phase 8 calculateLiveReadiness Status Engine (GATE-07)`:
       - Verify initial state is `in_progress` ('⏳ DALAM PRAKTIK').
       - Verify setting any checkpoint `cp-4..cp-9` to `'failed'` returns `clinic` ('⚠️ BELUM SIAP / BUTUH BANTUAN').
       - Verify setting all 6 checkpoints `cp-4` through `cp-9` to `'passed'` returns `ready` ('🎉 SELESAI (SUKSES)').
    2. `Suite 9: Phase 8 Form Laporan Hasil Praktik Generator & Dual Export (RPT-04, RPT-05)`:
       - Verify `generateLiveReportText('whatsapp')` produces valid WhatsApp format with Checkpoint 4-9 status tags and sanitized error text.
       - Verify `generateLiveReportText('telegram')` produces valid Telegram markdown format.
       - Verify sensitive tokens (bot token, user path, api key) are masked in the generated report.
    3. `Suite 10: Phase 8 Live Troubleshooting Hub Filtering (TRBL-04, TRBL-05)`:
       - Verify 7 error cards match the 4 categories (`9router-live`, `hermes-cli`, `telegram-gateway`, `google-oauth`).
       - Verify search query matching functions across title and solution description.
  </action>
</task>

</tasks>

<verification>
- Run test suite in browser or automated runner to ensure 100% pass rate across all suites.
- Verify `#sec-live-troubleshooting` and `#sec-live-report` render correctly in `index.html`.
- Test WhatsApp and Telegram copy buttons and verify toast feedback.
</verification>
