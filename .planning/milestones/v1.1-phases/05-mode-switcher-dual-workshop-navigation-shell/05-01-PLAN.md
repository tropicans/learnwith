---
phase: "05"
plan: "01"
title: "Mode Switcher Markup & Responsive CSS Styling"
wave: 1
dependencies: []
requirements:
  - MODE-01
files_modified:
  - index.html
  - assets/css/components.css
  - assets/css/main.css
autonomous: true
must_haves:
  truths:
    - Header contains a segmented tab switcher allowing users to choose between "Pra-Training (Persiapan)" and "Hari-H (Praktik Kelas)".
    - Mobile sidebar drawer contains a synchronized segmented mode switcher.
    - Sidebar navigation links are segmented into dedicated containers for pre-training and live-class modes.
    - Main content area is structured with mode container wrappers (#container-pretraining and #container-liveclass) with proper ARIA accessibility roles.
    - Switcher tabs adhere to the design system tokens, maintain minimum 44px touch targets on mobile, and support keyboard accessibility.
  artifacts:
    - path: index.html
      provides: DOM structure for mode switcher tabs, grouped sidebar links, and mode content containers
    - path: assets/css/components.css
      provides: Segmented control styles, glassmorphic active pill, and focus states
    - path: assets/css/main.css
      provides: Responsive header/sidebar layout adjustments for mode switcher positioning
  key_links:
    - from: index.html
      to: assets/css/components.css
      via: .mode-switcher, .mode-tab, .mode-tab.active, and .sidebar-mode-switcher classes
---

<objective>
Construct the accessible HTML markup and responsive CSS styling for the dual-mode switcher in the application header and mobile sidebar drawer.

Purpose: Provide participants and instructors with an intuitive visual switch to transition between pre-training preparation and live in-class workshop materials without cluttering navigation.
Output: Accessible tab components, grouped sidebar navigation links, and container containers in `index.html` styled with responsive CSS in `assets/css/components.css` and `assets/css/main.css`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@index.html
@assets/css/main.css
@assets/css/components.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mode switcher markup and container separation to index.html</name>
  <files>index.html</files>
  <action>
    1. In `index.html` within `<header class="app-header">`, add a segmented mode switcher `<div class="header-mode-switcher" role="tablist" aria-label="Pilihan Sesi Workshop">` containing:
       - `<button type="button" class="mode-tab active" data-mode="pretraining" role="tab" aria-selected="true" aria-controls="container-pretraining" id="tab-pretraining-header"><span class="mode-tab-icon">📋</span> <span class="mode-tab-label">Pra-Training</span></button>`
       - `<button type="button" class="mode-tab" data-mode="live-class" role="tab" aria-selected="false" aria-controls="container-liveclass" id="tab-liveclass-header"><span class="mode-tab-icon">🚀</span> <span class="mode-tab-label">Hari-H Kelas</span></button>`
    2. Inside `<aside id="app-sidebar" class="app-sidebar">`, add a matching mode switcher card at the top of the navigation drawer so mobile and sidebar users can also toggle modes easily.
    3. Group existing sidebar navigation items (`#sec-target`, `#sec-prereq`, modules 1-4, checkpoints, troubleshooting, redaction, and report) inside `<div id="nav-group-pretraining" class="sidebar-nav-group active">`.
    4. Add `<div id="nav-group-liveclass" class="sidebar-nav-group" style="display: none;">` containing planned in-class navigation anchor links:
       - Pengantar Hari-H (#sec-live-intro)
       - Modul 6: Penyelarasan 9Router (#sec-module-6)
       - Modul 7: Instalasi Hermes (#sec-module-7)
       - Modul 8: Full Setup Hermes (#sec-module-8)
       - Modul 9: Telegram Allowlist (#sec-module-9)
       - Modul 10: Google Calendar OAuth (#sec-module-10)
       - Modul 11: Uji End-to-End (#sec-module-11)
       - Checkpoint Gates 4–9 (#sec-live-checkpoints)
       - Troubleshooting Hari-H (#sec-live-troubleshooting)
       - Laporan Akhir (#sec-live-report)
    5. Wrap the existing main content sections inside `<div id="container-pretraining" class="mode-container active" role="tabpanel" aria-labelledby="tab-pretraining-header">`.
    6. Add `<div id="container-liveclass" class="mode-container" role="tabpanel" aria-labelledby="tab-liveclass-header" style="display: none;">` with an introductory hero banner for the live workshop session ("Sesi Live Workshop: Hermes Agent + 9Router (Google Calendar via Telegram)") and placeholders for Phases 6–8.
  </action>
  <verify>
    Inspect `index.html` structure. Validate with HTML syntax check and verify that all IDs and ARIA controls match.
  </verify>
  <done>
    `index.html` contains header and sidebar mode switchers, partitioned navigation groups, and mode content containers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement responsive CSS styling for mode switcher tabs and navigation groups</name>
  <files>assets/css/components.css, assets/css/main.css</files>
  <action>
    1. In `assets/css/components.css`, create the `.mode-switcher` component styles:
       - Container with subtle border, background `var(--bg-surface-subtle)`, pill radius `var(--radius-full)`, and flex alignment.
       - `.mode-tab` with seamless transitions, padding, font-weight semibold, color `var(--text-secondary)`, and transparent background.
       - `.mode-tab.active` with elevated background `var(--bg-surface)`, active border, brand accent color `var(--accent-primary)`, shadow `var(--shadow-xs)`, and bold text.
       - Hover and keyboard `:focus-visible` styles with distinct outline conforming to accessibility standards.
       - Mobile touch target minimum 44px height.
    2. In `assets/css/main.css`, update the `.app-header` grid/flex layout to gracefully accommodate the new mode switcher:
       - On wide screens (desktop > 1024px), position the mode switcher centrally or adjacent to the brand logo.
       - On medium screens (tablet 768px–1024px), adjust flex-shrink and font size so search and progress remain clearly readable.
       - On mobile (< 768px), hide the header switcher if screen width is constrained (< 480px) and prioritize the prominent switcher inside the sidebar drawer, or use an ultra-compact icon-pill version.
    3. Ensure seamless theme support for both Light and Dark modes using CSS variables (`--bg-surface`, `--bg-surface-subtle`, `--text-primary`, `--accent-primary`).
  </action>
  <verify>
    Verify CSS syntax with Node CLI `node -e "require('fs').readFileSync('assets/css/components.css'); require('fs').readFileSync('assets/css/main.css'); console.log('CSS files readable')"` and check that responsive breakpoints are intact.
  </verify>
  <done>
    Mode switcher tabs are cleanly styled, fully responsive, accessible, and theme-adaptive across desktop and mobile screens.
  </done>
</task>

</tasks>

<threat_model>
- Injection: Static template additions with hardcoded IDs; no dynamic HTML interpolation.
- Accessibility: ARIA tablist/tab roles and keyboard focus styles prevent navigation traps for screen-reader and keyboard users.
</threat_model>
