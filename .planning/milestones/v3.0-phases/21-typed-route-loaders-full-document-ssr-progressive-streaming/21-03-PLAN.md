---
phase: 21-typed-route-loaders-full-document-ssr-progressive-streaming
plan: "03"
type: execute
wave: 3
depends_on:
  - "21-02"
files_modified: []
autonomous: true
requirements:
  - SSR-01
  - SSR-02
  - SSR-03
  - SSR-04
must_haves:
  truths:
    - Production build (npm run build) compiles standalone Vinxi server without errors.
    - Production Nitro server starts and listens on Port 3173.
    - HTTP GET requests to /, /course/ai?mode=pretraining, /course/ai?mode=live-class, and /course/word return HTTP 200 OK with server-rendered curriculum content and dynamic metadata.
    - Transferred HTML contains server-rendered title tags and stream markers for Suspense boundaries.
    - All 11 legacy Node.js test suites pass 100% with zero regressions.
  artifacts: []
  key_links:
    - Port 3173 delivers typed route loaders and progressive SSR streaming to clients.
---

<objective>
Verify end-to-end SSR response codes, dynamic head title tags, progressive streaming output, and client island boundaries on Port 3173, followed by a regression test run on the 11 legacy test suites.

Purpose: Validate SSR-01, SSR-02, SSR-03, and SSR-04 in a production runtime environment and ensure zero platform regressions.
Output: Comprehensive verification evidence across all SSR requirements.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-typed-route-loaders-full-document-ssr-progressive-streaming/21-RESEARCH.md
@.planning/phases/21-typed-route-loaders-full-document-ssr-progressive-streaming/21-PATTERNS.md
@.planning/phases/21-typed-route-loaders-full-document-ssr-progressive-streaming/21-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Production Bundle and Probe SSR Streaming Endpoints on Port 3173</name>
  <files></files>
  <read_first>.planning/phases/21-typed-route-loaders-full-document-ssr-progressive-streaming/21-RESEARCH.md</read_first>
  <action>
    1. Compile the production bundle: `npm run build`.
    2. Start the standalone Node server in background: `node .output/server/index.mjs` with PORT=3173.
    3. Probe each route via HTTP GET:
       - `http://localhost:3173/` -> assert HTTP 200, check for `<title>learnwith — Pusat Workshop & Ruang Belajar Terpadu</title>`, assert presence of loaded course list in HTML body.
       - `http://localhost:3173/course/ai?mode=pretraining` -> assert HTTP 200, check for dynamic title containing "Pra-Training", verify server-rendered module titles.
       - `http://localhost:3173/course/ai?mode=live-class` -> assert HTTP 200, check for dynamic title containing "Hari-H", verify server-rendered modules.
       - `http://localhost:3173/course/word` -> assert HTTP 200, check for dynamic title "Pengolahan Kata Tingkat Lanjut (ASN)", verify server-rendered bab outline.
    4. Verify that HTML stream includes Suspense boundary markup and skeleton fallback placeholders.
    5. Terminate the background server process cleanly after testing.
  </action>
  <verify>
    <automated>npm run build</automated>
    <fails_when>Production build failure or endpoint returns non-200 or lacks server-rendered metadata</fails_when>
  </verify>
  <done>Production server serves all routes with HTTP 200, correct dynamic titles, and progressive SSR streaming.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Legacy CommonJS Test Suite Integrity</name>
  <files></files>
  <read_first>package.json</read_first>
  <action>
    1. Run the full legacy test suite: `npm test`.
    2. Confirm all 11 test suites pass with 0 failures:
       - `tests/security-suite.test.js`
       - `tests/integration.test.js`
       - `tests/export.test.js`
       - `tests/diagnostics.test.js`
       - `tests/telegram.test.js`
       - `tests/state.test.js`
       - `tests/checkpoint.test.js`
       - `tests/pretraining.test.js`
       - `tests/troubleshooting-exporter.test.js`
       - `tests/word-modules.test.js`
       - `tests/word-quiz-report.test.js`
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>Any legacy test fails or cancels</fails_when>
  </verify>
  <done>All 11 test suites pass 100% with 0 regressions.</done>
</task>

</tasks>

<threat_model>
| Threat Ref / Vector | Risk Description | Mitigation | Acceptance Gate |
|---|---|---|---|
| T-21-05 SSR Memory Leak / Connection Exhaustion | Long-lived streaming responses or hung promises exhaust Node server file descriptors or memory | Ensure server stream handler handles client disconnects; verify background server cleanly exits after probe | Server cleanly serves requests without unbounded memory growth or open socket leaks |
</threat_model>

<verification>
- [ ] Production build succeeds via `npm run build`.
- [ ] Route probes return HTTP 200 on Port 3173.
- [ ] Rendered HTML contains route-specific `<title>` and OpenGraph metadata.
- [ ] `npm test` passes 11/11 test suites (100%).
</verification>

<success_criteria>
- End-to-end SSR streaming verified with HTTP 200 responses.
- Complete regression freedom across the entire platform.
</success_criteria>

<output>
Create `.planning/phases/21-typed-route-loaders-full-document-ssr-progressive-streaming/21-03-SUMMARY.md` after execution.
</output>
