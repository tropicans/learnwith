---
phase: 22-typed-server-functions-boundary-isolation
plan: "03"
type: execute
wave: 3
depends_on:
  - 22-01
  - 22-02
files_modified:
  - app/components/course/InstructorUnlockModal.tsx
  - app/routes/course.ai.tsx
  - app/routes/course.word.tsx
autonomous: true
requirements:
  - SRV-01
  - SRV-02
  - SRV-03
must_haves:
  truths:
    - InstructorUnlockModal allows instructors to submit passkeys and verifies them via verifyInstructorPasskeyFn.
    - Course workspaces (course.ai.tsx, course.word.tsx) incorporate the unlock modal and update unlock state seamlessly upon successful verification.
    - No plain text passcodes or secret hashes are exposed in client React components or client bundle.
    - Production build (npm run build) compiles cleanly with distinct client and SSR server artifacts.
    - Full test suite (npm test) passes with 100% success and zero regressions.
  artifacts:
    - app/components/course/InstructorUnlockModal.tsx
    - app/routes/course.ai.tsx
    - app/routes/course.word.tsx
  key_links:
    - InstructorUnlockModal calls verifyInstructorPasskeyFn and propagates unlocked state to course components.
    - app/routes/course.ai.tsx and app/routes/course.word.tsx render InstructorUnlockModal when unlock action is triggered.
---

<objective>
Build the interactive client modal component (`app/components/course/InstructorUnlockModal.tsx`), wire typed RPC verification into the Agentic AI and Word ASN course routes (`app/routes/course.ai.tsx`, `app/routes/course.word.tsx`), eliminate client-exposed passcodes, and verify zero build leaks or test regressions.

Purpose: Connect user-facing instructor unlock workflows to the typed server function boundary and validate full end-to-end functionality.
Output: `app/components/course/InstructorUnlockModal.tsx`, updated `app/routes/course.ai.tsx`, and updated `app/routes/course.word.tsx`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-typed-server-functions-boundary-isolation/22-RESEARCH.md
@.planning/phases/22-typed-server-functions-boundary-isolation/22-PATTERNS.md
@.planning/phases/22-typed-server-functions-boundary-isolation/22-VALIDATION.md
@.planning/phases/22-typed-server-functions-boundary-isolation/22-01-PLAN.md
@.planning/phases/22-typed-server-functions-boundary-isolation/22-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InstructorUnlockModal Component Calling Server RPC</name>
  <files>app/components/course/InstructorUnlockModal.tsx</files>
  <read_first>app/components/course/InstructorUnlockModal.tsx, .planning/phases/22-typed-server-functions-boundary-isolation/22-PATTERNS.md, app/server/auth.ts</read_first>
  <acceptance_criteria>
    - app/components/course/InstructorUnlockModal.tsx exports InstructorUnlockModal
    - Component renders modal dialog with password input and submit button
    - Submits data via verifyInstructorPasskeyFn({ data: { courseId, passkey } })
    - Invokes onUnlocked and onClose on successful verification
    - Displays server error message when verification fails
    - npm run typecheck passes with 0 errors
  </acceptance_criteria>
  <action>
    1. Create app/components/course/InstructorUnlockModal.tsx.
    2. Import useState from react.
    3. Import verifyInstructorPasskeyFn from @/server/auth.
    4. Implement component props:
       - courseId: 'ai' | 'word'
       - isOpen: boolean
       - onClose: () => void
       - onUnlocked: (unlockedAt: number) => void
    5. Form state: passkey, errorMessage, isPending.
    6. Form submission handler:
       - Calls verifyInstructorPasskeyFn({ data: { courseId, passkey } }).
       - On success: triggers onUnlocked(result.unlockedAt || Date.now()) and onClose().
       - On failure: displays result.message in error feedback element.
       - Manages loading state and accessible dialog keyboard events (Escape to close).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit code or TypeScript compilation error</fails_when>
  </verify>
  <done>InstructorUnlockModal component created and verified with strict typing.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate InstructorUnlockModal into Course Workspaces</name>
  <files>app/routes/course.ai.tsx, app/routes/course.word.tsx</files>
  <read_first>app/routes/course.ai.tsx, app/routes/course.word.tsx, app/components/course/InstructorUnlockModal.tsx</read_first>
  <acceptance_criteria>
    - app/routes/course.ai.tsx renders InstructorUnlockModal and unlocks live-class modules upon passkey verification
    - app/routes/course.word.tsx renders InstructorUnlockModal and unlocks restricted materials upon passkey verification
    - No plain text passcodes are embedded in either route file
    - npm run typecheck and npm run build pass with 0 errors
  </acceptance_criteria>
  <action>
    1. Update app/routes/course.ai.tsx:
       - Add unlock trigger button/badge in the live-class header.
       - Manage isUnlockModalOpen state.
       - Maintain isUnlocked session state (checking local session / search params).
       - Render <InstructorUnlockModal courseId="ai" isOpen={isUnlockModalOpen} ... />.
       - When unlocked, toggle live-class modules into unlocked view.
    2. Update app/routes/course.word.tsx:
       - Add unlock trigger button/badge for restricted instructor Bab.
       - Manage isUnlockModalOpen state and unlock callbacks.
       - Render <InstructorUnlockModal courseId="word" isOpen={isUnlockModalOpen} ... />.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
    <fails_when>non-zero exit code, TypeScript error, or Vinxi build failure</fails_when>
  </verify>
  <done>Course workspaces integrated with InstructorUnlockModal and typed RPC flow.</done>
</task>

<task type="auto">
  <name>Task 3: Run Full Regression and Boundary Leakage Audit</name>
  <files>tests/server-boundary-audit.test.js, tests/server-functions.test.js</files>
  <read_first>tests/server-boundary-audit.test.js, tests/server-functions.test.js, .planning/phases/22-typed-server-functions-boundary-isolation/22-VALIDATION.md</read_first>
  <acceptance_criteria>
    - npm run typecheck reports 0 errors
    - npm run build produces standalone server and client artifacts cleanly
    - node --test tests/server-boundary-audit.test.js passes with 0 failures
    - node --test tests/server-functions.test.js passes with 0 failures
    - npm test passes all test suites with 0 regressions
  </acceptance_criteria>
  <action>
    1. Run npm run typecheck to verify zero TypeScript errors across the entire application.
    2. Run npm run build to verify production Vinxi client and server bundles build cleanly.
    3. Run node --test tests/server-boundary-audit.test.js to ensure zero secret leakage in client artifacts.
    4. Run node --test tests/server-functions.test.js to verify RPC function correctness.
    5. Run npm test to verify all legacy unit test suites (11/11 legacy + new tests) pass with 100% success.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build && npm test</automated>
    <fails_when>non-zero exit code, compiler error, or test failure</fails_when>
  </verify>
  <done>Full test suite green with zero regressions and proven boundary isolation.</done>
</task>

</tasks>