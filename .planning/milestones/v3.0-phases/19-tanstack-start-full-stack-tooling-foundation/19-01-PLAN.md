---
phase: 19-tanstack-start-full-stack-tooling-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tests/package.json
  - assets/package.json
  - .gitignore
autonomous: true
requirements:
  - FOUND-01
must_haves:
  truths:
    - Root package.json is initialized with type module, unified npm scripts on Port 3173, and exact pinned TanStack Start/React 19 dependencies.
    - CommonJS isolation in tests/package.json and assets/package.json preserves 100% passing execution for all 11 legacy Node unit tests.
    - npm install executes cleanly with zero peer dependency conflicts.
  artifacts:
    - package.json
    - package-lock.json
    - tests/package.json
    - assets/package.json
    - .gitignore
  key_links:
    - npm test runs node --test tests/*.test.js in tests/ CommonJS scope without ESM syntax errors.
    - package.json scripts dev and start bind explicitly to Port 3173.
---

<objective>
Initialize the project root manifest (`package.json`) with exact pinned dependencies for TanStack Start, React 19, Vinxi, and TypeScript 5.4, configure unified npm scripts targeting Port 3173, update `.gitignore`, and establish CommonJS isolation boundaries in `tests/` and `assets/` so all 11 legacy unit tests continue passing 100%.

Purpose: Establish the foundational package management and test isolation infrastructure required for Milestone v3.0 without breaking legacy test suites or encountering peer dependency cascades.
Output: `package.json`, `package-lock.json`, `tests/package.json`, `assets/package.json`, and updated `.gitignore`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-tanstack-start-full-stack-tooling-foundation/19-CONTEXT.md
@.planning/phases/19-tanstack-start-full-stack-tooling-foundation/19-RESEARCH.md
@.planning/phases/19-tanstack-start-full-stack-tooling-foundation/19-PATTERNS.md
@.planning/phases/19-tanstack-start-full-stack-tooling-foundation/19-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Root package.json with Pinned Dependencies, Port 3173 Scripts, and Update .gitignore</name>
  <files>package.json, .gitignore</files>
  <read_first>.planning/phases/19-tanstack-start-full-stack-tooling-foundation/19-RESEARCH.md, .planning/phases/19-tanstack-start-full-stack-tooling-foundation/19-PATTERNS.md, .gitignore</read_first>
  <action>
    1. Create `package.json` honoring locked decisions D-01, D-08, D-09, D-10, and D-11:
       - `"name": "learnwith"`
       - `"version": "3.0.0"`
       - `"private": true`
       - `"type": "module"` (enables ESM for app/ and app.config.ts)
       - Unified `"scripts"`:
         - `"dev": "vinxi dev --port 3173"`
         - `"build": "vinxi build"`
         - `"start": "vinxi start --port 3173"`
         - `"typecheck": "tsc --noEmit"`
         - `"test:node": "node --test tests/*.test.js"`
         - `"test": "npm run test:node"`
       - Exact verified `"dependencies"` per 19-RESEARCH.md:
         - `"@tanstack/react-query": "^5.66.0"`
         - `"@tanstack/react-router": "1.120.20"`
         - `"@tanstack/react-start": "1.120.20"`
         - `"@tanstack/router-plugin": "1.120.20"`
         - `"@tanstack/start": "1.120.20"`
         - `"react": "^19.0.0"`
         - `"react-dom": "^19.0.0"`
         - `"vinxi": "0.5.3"`
         - `"vite": "^6.1.0"`
         - `"vite-tsconfig-paths": "^5.1.4"`
         - `"zod": "^3.24.2"`
       - Verified `"devDependencies"`:
         - `"@types/node": "^22.5.4"`
         - `"@types/react": "^19.0.8"`
         - `"@types/react-dom": "^19.0.3"`
         - `"typescript": "^5.4.5"`
    2. Update `.gitignore` to include Vinxi build outputs per D-11:
       - Append `.output/` and `.vinxi/`.
    3. Execute `npm install` to resolve dependencies, generate `package-lock.json`, and populate `node_modules`.
  </action>
  <verify>
    <automated>npm ls --depth=0</automated>
    <fails_when>non-zero exit code or missing/unresolved top-level dependencies</fails_when>
  </verify>
  <done>`package.json` created with exact pinned versions, `.gitignore` updated with `.output/` and `.vinxi/`, and dependencies installed cleanly without peer conflicts.</done>
</task>

<task type="auto">
  <name>Task 2: Establish CommonJS Isolation in tests/ and assets/ to Preserve 11 Legacy Tests</name>
  <files>tests/package.json, assets/package.json</files>
  <read_first>.planning/phases/19-tanstack-start-full-stack-tooling-foundation/19-RESEARCH.md, .planning/phases/19-tanstack-start-full-stack-tooling-foundation/19-PATTERNS.md</read_first>
  <action>
    1. Setting `"type": "module"` in root `package.json` causes Node.js to interpret all `.js` files as ES modules by default. The 11 legacy test suites in `tests/*.test.js` and client assets in `assets/js/*.js` rely on CommonJS `require()` and `module.exports`.
    2. Create `tests/package.json` honoring D-05 and D-14:
       ```json
       {
         "type": "commonjs"
       }
       ```
    3. Create `assets/package.json` honoring D-05 and D-14:
       ```json
       {
         "type": "commonjs"
       }
       ```
    4. Run `npm test` (`node --test tests/*.test.js`) to verify all 11 unit test suites pass 100% with 0 failures under the new CommonJS isolation boundary.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit code or any test failure among the 11 legacy test suites</fails_when>
  </verify>
  <done>`tests/package.json` and `assets/package.json` provide CommonJS scoping, and `npm test` executes all 11 legacy test files successfully with 0 failures.</done>
</task>

</tasks>

<threat_model>
| Threat Ref / Vector | Risk Description | Mitigation | Acceptance Gate |
|---|---|---|---|
| Dependency Tree Pollution | Unpinned carets trigger peer conflicts with Vite 7 or unreleased TanStack router builds | Strict version pinning in `package.json` (`1.120.20`, `vinxi@0.5.3`) per 19-RESEARCH.md | `npm ls --depth=0` exits 0 with zero unresolvable peer warnings |
| Legacy Test Regression | Root `"type": "module"` causes `ReferenceError: require is not defined in ES module scope` across 11 test suites | Nearest ancestor `package.json` with `{"type": "commonjs"}` in `tests/` and `assets/` | `npm test` passes all 11 test suites (100% green) |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Verify `package.json` contains `"type": "module"` and scripts for `dev`, `build`, `start`, `typecheck`, `test:node`, and `test`.
- [ ] Verify Port 3173 is explicitly assigned in `dev` and `start` scripts.
- [ ] Verify `package-lock.json` is generated.
- [ ] Verify `.output/` and `.vinxi/` are present in `.gitignore`.
- [ ] Verify `tests/package.json` and `assets/package.json` exist with `{"type": "commonjs"}`.
- [ ] Run `npm test` and confirm all 11 test files pass cleanly.
</verification>

<success_criteria>
- All tasks completed successfully.
- Package manifest initialized with exact verified dependencies per FOUND-01.
- All 11 legacy test suites pass without code modification.
</success_criteria>

<output>
After completion, create `.planning/phases/19-tanstack-start-full-stack-tooling-foundation/19-01-SUMMARY.md`
</output>
