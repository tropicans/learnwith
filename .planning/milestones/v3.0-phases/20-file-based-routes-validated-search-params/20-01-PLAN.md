---
phase: 20-file-based-routes-validated-search-params
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/schemas/searchParams.ts
  - app/components/layout/Header.tsx
  - app/components/layout/CourseSwitcher.tsx
  - app/routes/__root.tsx
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-05
must_haves:
  truths:
    - Centralized Zod search params schema (homeSearchSchema, courseAiSearchSchema, courseWordSearchSchema) defined with safe .catch() defaults.
    - Global HTML root document shell (__root.tsx) loads fonts, stylesheets (main.css, components.css), and NotebookLM platform header with breadcrumb.
    - TypeScript strict type checking passes with zero errors.
  artifacts:
    - app/schemas/searchParams.ts
    - app/components/layout/Header.tsx
    - app/components/layout/CourseSwitcher.tsx
    - app/routes/__root.tsx
  key_links:
    - app/routes/__root.tsx renders the Header component containing brand home link and CourseSwitcher dropdown.
    - app/schemas/searchParams.ts exports type-safe schemas consumed by route parameter validators.
---

<objective>
Implement the type-safe search parameter validation schemas (`app/schemas/searchParams.ts`), the NotebookLM platform header layout components (`Header.tsx` and `CourseSwitcher.tsx`), and the root document shell (`app/routes/__root.tsx`) with full asset pipeline integration.

Purpose: Provide the global application frame, styles, and parameter validation foundation required for ROUTE-01 and ROUTE-05.
Output: `app/schemas/searchParams.ts`, `app/components/layout/Header.tsx`, `app/components/layout/CourseSwitcher.tsx`, and updated `app/routes/__root.tsx`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-file-based-routes-validated-search-params/20-RESEARCH.md
@.planning/phases/20-file-based-routes-validated-search-params/20-PATTERNS.md
@.planning/phases/20-file-based-routes-validated-search-params/20-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Centralized Zod Search Parameter Validation Schemas</name>
  <files>app/schemas/searchParams.ts</files>
  <read_first>.planning/phases/20-file-based-routes-validated-search-params/20-PATTERNS.md</read_first>
  <action>
    1. Create `app/schemas/searchParams.ts`.
    2. Export `homeSearchSchema` validating `filter` (`'all' | 'ai' | 'word'`) with `.catch('all')`.
    3. Export `courseAiSearchSchema` validating `mode` (`'pretraining' | 'live-class'`) with `.catch('pretraining')`, plus optional `step` and `cp`.
    4. Export `courseWordSearchSchema` validating optional `tab`, `bab`, and `unlocked`.
    5. Export corresponding TypeScript types inferred via `z.infer`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>TypeScript type error or syntax error</fails_when>
  </verify>
  <done>Zod validation schemas defined and strictly typechecked.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Platform Header Components and Complete Document Route Shell</name>
  <files>app/components/layout/Header.tsx, app/components/layout/CourseSwitcher.tsx, app/routes/__root.tsx</files>
  <read_first>.planning/phases/20-file-based-routes-validated-search-params/20-PATTERNS.md, index.html</read_first>
  <action>
    1. Create `app/components/layout/CourseSwitcher.tsx`:
       - NotebookLM pill dropdown with active course indicator.
       - Links to `/`, `/course/ai`, and `/course/word`.
       - Accessible dropdown button with aria attributes.
    2. Create `app/components/layout/Header.tsx`:
       - Brand logo and name (`learnwith`).
       - Breadcrumb slash separator `/`.
       - CourseSwitcher integration.
       - Global search input field placeholder.
    3. Update `app/routes/__root.tsx`:
       - Inject `<Meta />` tags and `<Links />` linking Google Fonts (Inter, JetBrains Mono) and stylesheets (`/assets/css/main.css?v=2.2.0`, `/assets/css/components.css?v=2.2.0`).
       - Render `Header` component inside `.app-container`.
       - Render `<Outlet />`, `<ScrollRestoration />`, and `<Scripts />`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <fails_when>TypeScript compilation failure</fails_when>
  </verify>
  <done>Root document shell and Header component tree compile cleanly with zero TypeScript errors.</done>
</task>

</tasks>

<threat_model>
| Threat Ref / Vector | Risk Description | Mitigation | Acceptance Gate |
|---|---|---|---|
| T-20-01 Parameter Injection | Malformed or adversarial query strings crash application or pollute state | Use Zod schemas with strict `.catch()` fallbacks for all query parameters | Invalid parameters fall back safely to defaults |
| T-20-02 SSR FOUC | Unstyled HTML flash occurs if stylesheets are not loaded during initial SSR stream | Include stylesheet `<link>` tags directly in `__root.tsx` `head()` configuration | Stylesheets load in initial HTML head chunk |
</threat_model>

<verification>
- [ ] `app/schemas/searchParams.ts` exports `homeSearchSchema`, `courseAiSearchSchema`, `courseWordSearchSchema`.
- [ ] `app/components/layout/Header.tsx` renders platform logo and breadcrumb course switcher.
- [ ] `app/routes/__root.tsx` incorporates stylesheets and fonts.
- [ ] `npm run typecheck` exits 0.
</verification>

<success_criteria>
- All tasks completed successfully.
- Search schemas and document root shell pass strict typechecking.
</success_criteria>

<output>
Create `.planning/phases/20-file-based-routes-validated-search-params/20-01-SUMMARY.md` after execution.
</output>
