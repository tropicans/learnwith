---
phase: 29-master-admin-authentication-route-protection-google-oauth-ar
plan: "02"
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - app/routes/admin.tsx
  - app/components/admin/AdminLoginGate.tsx
  - app/components/admin/AdminShell.tsx
  - app/components/admin/GoogleSignInButton.tsx
  - assets/css/admin.css
  - public/assets/css/admin.css
autonomous: true
requirements:
  - ADMIN-AUTH-01
  - ADMIN-AUTH-03
  - ADMIN-AUTH-04
  - ADMIN-QA-01
must_haves:
  truths:
    - Navigating to `/admin` loads route with `adminCheckSessionFn()` preloading session state.
    - If unauthenticated, `AdminLoginGate` is displayed with Master Admin Passkey input and Google OAuth readiness component.
    - If authenticated, `AdminShell` is displayed with header navigation, session indicator, and functional Logout button.
    - Responsive, professional dashboard styling in `assets/css/admin.css` seamlessly mirrored to `public/assets/css/admin.css`.
    - Zero regressions against all 116 existing tests, full SSR build passes, and TypeScript check passes cleanly.
  artifacts:
    - app/routes/admin.tsx
    - app/components/admin/AdminLoginGate.tsx
    - app/components/admin/AdminShell.tsx
    - app/components/admin/GoogleSignInButton.tsx
    - assets/css/admin.css
    - public/assets/css/admin.css
  key_links:
    - `app/routes/admin.tsx` consumes `adminLoginFn`, `adminCheckSessionFn`, and `adminLogoutFn` from `app/server/adminAuth.ts`.
    - `app/components/admin/AdminShell.tsx` provides the shared layout frame for subsequent admin phases (Dashboard, Telemetry, Passkeys, Settings).
---

<objective>
Build the client-side `/admin` route and presentation components, including the protected route loader, the Master Admin Passkey login gate, the Google OAuth readiness UI, the authenticated Admin Command Center shell, and dedicated stylesheets.

Purpose: Deliver a seamless, secure, and professional admin interface that allows administrators to log in with the master passkey, inspect Google OAuth status, view the admin workspace, and log out safely.
Output: Admin file-based route, login gate component, Google sign-in button, admin shell layout, and CSS stylesheet.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-master-admin-authentication-route-protection-google-oauth-ar/29-RESEARCH.md
@.planning/phases/29-master-admin-authentication-route-protection-google-oauth-ar/29-01-PLAN.md
@app/server/adminAuth.ts
@app/routes/__root.tsx
</context>

<tasks>

<task type="auto" id="29-02-01">
<title>Create Admin Stylesheet</title>
<instruction>
Create `assets/css/admin.css`:
- Define styles for `.admin-canvas`, `.admin-login-card`, `.admin-header`, `.admin-nav-tabs`, `.admin-session-badge`, `.btn-admin-logout`, `.google-signin-btn`.
- Use existing design tokens (`--bg-surface`, `--bg-surface-subtle`, `--text-primary`, `--text-secondary`, `--accent-primary`, `--border-subtle`, `--radius-xl`, etc.) to match the Editorial Studio & GovTech aesthetic.
- Include responsive styles for mobile, tablet, and desktop.
- Mirror file to `public/assets/css/admin.css` with authoritative source comment header.
- Add `<link rel="stylesheet" href="/assets/css/admin.css" />` to `app/routes/__root.tsx` or inject contextually.
</instruction>
<files>assets/css/admin.css, public/assets/css/admin.css, app/routes/__root.tsx</files>
</task>

<task type="auto" id="29-02-02">
<title>Build Google Sign-In Readiness Component</title>
<instruction>
Create `app/components/admin/GoogleSignInButton.tsx`:
- Render Google vector logo with standard Google Sign-In styling.
- Accept props for `isConfigured: boolean`, `authUrl?: string`, `onGoogleClick?: () => void`.
- If Google OAuth Client ID is not configured in env, show a subtle information badge: "Mode Passkey Aktif (Google OAuth Siap Dikonfigurasi)".
- If configured, provide interactive sign-in click handler directing to Google OAuth endpoint.
</instruction>
<files>app/components/admin/GoogleSignInButton.tsx</files>
</task>

<task type="auto" id="29-02-03">
<title>Build Admin Login Gate Component</title>
<instruction>
Create `app/components/admin/AdminLoginGate.tsx`:
- Centered, focused card with LearnWith Monogram & "Command Center Instruktur / Admin".
- Form with Master Admin Passkey input (`type="password"`, placeholder "Masukkan Master Passkey Admin..."), submit button, and loading state.
- Call `adminLoginFn({ data: { passkey } })` on submit.
- On error, display clear alert without exposing internal hashes.
- On success, trigger route reload or state update to render `AdminShell`.
- Include `GoogleSignInButton` below a clean divider ("atau masuk dengan").
</instruction>
<files>app/components/admin/AdminLoginGate.tsx</files>
</task>

<task type="auto" id="29-02-04">
<title>Build Admin Shell Layout Component</title>
<instruction>
Create `app/components/admin/AdminShell.tsx`:
- Top administrative bar with "LearnWith Admin", active environment indicator, and administrator badge.
- Navigation tabs matching upcoming roadmap capabilities:
  - 📊 Dashboard & Monitoring (Active / Default)
  - 👥 Peserta & Telemetri
  - 🔑 Passkey Modul Dinas
  - 🛠️ Log Kendala & Troubleshooting
  - ⚙️ Pengaturan Platform
- Active session display with timestamp.
- "Keluar / Logout" button calling `adminLogoutFn()`, triggering view reset to `AdminLoginGate`.
- Child container slot rendering the active view / placeholder for subsequent phases.
</instruction>
<files>app/components/admin/AdminShell.tsx</files>
</task>

<task type="auto" id="29-02-05">
<title>Create Route app/routes/admin.tsx & Integrate Authentication Gate</title>
<instruction>
Create `app/routes/admin.tsx`:
- Define createFileRoute('/admin') with loader calling `adminCheckSessionFn()` and `adminGetAuthConfigFn()`.
- Client component renders `AdminLoginGate` when session is unauthenticated.
- Client component renders `AdminShell` when session is authenticated.
- Provide seamless client-side state transitions on login and logout without full page reloads.
- Verify production build (`npx vinxi build`) and run all automated test suites (`npm test`).
</instruction>
<files>app/routes/admin.tsx</files>
</task>

</tasks>

<verification>
Automated:
- `npm test` passes 100% across all suites (existing 116 tests + new admin tests).
- `npx tsc --noEmit` validates strict TypeScript compilation with 0 errors.
- `npx vinxi build` succeeds and produces SSR and client bundles for `/admin`.
Manual:
- Visit `http://localhost:3173/admin`:
  - Login screen is presented by default.
  - Invalid passkey produces error message.
  - Correct passkey (`admin-learnwith`) logs in and reveals `AdminShell`.
  - Logout returns to login screen.
</verification>
