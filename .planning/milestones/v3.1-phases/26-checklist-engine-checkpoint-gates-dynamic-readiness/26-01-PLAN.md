---
phase: 26-checklist-engine-checkpoint-gates-dynamic-readiness
plan: "01"
wave: 1
depends_on: []
files_modified:
  - app/hooks/usePretrainingState.ts
  - app/data/pretrainingModules.ts
  - tests/pretraining-modules.test.js
  - app/components/course/pretraining/CheckpointGateCard.tsx
  - app/components/course/pretraining/PretrainingCheckpointsSection.tsx
  - app/components/course/pretraining/PretrainingReadinessSection.tsx
  - app/components/course/pretraining/ResetProgressModal.tsx
autonomous: true
requirements:
  - PRE-CHK-01
  - PRE-CHK-02
  - PRE-CHK-03
  - PRE-CHK-04
must_haves:
  truths:
    - "`usePretrainingState.ts` provides a reactive, SSR-safe hook managing exactly 13 checklist items, 3 checkpoint gates, participant metadata, progress calculation (60% tasks + 40% checkpoints), dynamic readiness rules, and safe reset with zero cross-course localStorage pollution."
    - "`app/data/pretrainingModules.ts` Step D in Modul 3 is normalized so only the 13 official checklist tasks across Modules 1–4 are emitted with task IDs, and `tests/pretraining-modules.test.js` is updated to maintain 100% green tests."
    - "`CheckpointGateCard.tsx` and `PretrainingCheckpointsSection.tsx` implement Checkpoints 1, 2, and 3 (`#sec-checkpoint-1..3`) with 3-state action groups (`passed`, `failed`, `pending`), verification criteria, and live input validation (Node version, Bot username `/(bot|_bot)$/i`, and numeric-only User ID `/^\\d+$/`)."
    - "`PretrainingReadinessSection.tsx` renders `#sec-readiness-summary` and `#card-readiness-status` with reactive readiness badges and dynamic border coloring."
    - "`ResetProgressModal.tsx` renders accessible confirmation dialog `#modal-reset-confirm` with keyboard navigation, focus management, cancel/confirm actions, and toast notification dispatch."
  artifacts:
    - app/hooks/usePretrainingState.ts
    - app/components/course/pretraining/CheckpointGateCard.tsx
    - app/components/course/pretraining/PretrainingCheckpointsSection.tsx
    - app/components/course/pretraining/PretrainingReadinessSection.tsx
    - app/components/course/pretraining/ResetProgressModal.tsx
  key_links:
    - "`usePretrainingState.ts` acts as the single source of truth for all checklist and checkpoint interactions, persisting to `learnwith_ai_state_v1` and `learnwith_ai_checklist`."
    - "`PretrainingCheckpointsSection.tsx` and `PretrainingReadinessSection.tsx` consume `usePretrainingState` to visualize participant qualification."
    - "`ResetProgressModal.tsx` triggers `resetState()` on confirmation while strictly isolating Course 2 keys (`learnwith_word_state_v1`)."
---

<objective>
Implement the reactive state and verification foundation for Phase 26: create `app/hooks/usePretrainingState.ts`, normalize `app/data/pretrainingModules.ts` to the official 13 checklist items, build Checkpoint Gate cards (`CheckpointGateCard.tsx`, `PretrainingCheckpointsSection.tsx`), build the Dynamic Readiness summary (`PretrainingReadinessSection.tsx`), and construct the Safe Progress Reset modal (`ResetProgressModal.tsx`).

Purpose: Establish the self-verification state engine and presentation components required for PRE-CHK-01 through PRE-CHK-04 before wiring interactive module checkboxes and route integration in Wave 2.
Output: State management hook, 4 modular pretraining verification components, and normalized module dataset.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-checklist-engine-checkpoint-gates-dynamic-readiness/26-RESEARCH.md
@.planning/phases/26-checklist-engine-checkpoint-gates-dynamic-readiness/26-PATTERNS.md
@.planning/phases/26-checklist-engine-checkpoint-gates-dynamic-readiness/26-VALIDATION.md
@index.html
@assets/js/state.js
@assets/js/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement usePretrainingState hook and normalize Modul 3 Step D</name>
  <files>
    app/hooks/usePretrainingState.ts
    app/data/pretrainingModules.ts
    tests/pretraining-modules.test.js
  </files>
  <read_first>
    assets/js/state.js
    app/data/pretrainingModules.ts
    tests/pretraining-modules.test.js
    .planning/phases/26-checklist-engine-checkpoint-gates-dynamic-readiness/26-PATTERNS.md
  </read_first>
  <action>
    1. Normalize `app/data/pretrainingModules.ts`:
       - In Modul 3, step D ("Buka Bot Anda & Tekan Start"): remove `taskId: 'm3-start-chat'` and `checklistLabel`. (In `index.html` line 1630 and `assets/js/state.js`, Step D does not have a checkbox because bot responses are handled during the live workshop).
       - Verify that across all 5 modules, exactly 13 steps possess a `taskId`:
         - Modul 1: `m1-check-node`, `m1-verify-lts`, `m1-check-npm` (3 tasks)
         - Modul 2: `m2-install-pkg`, `m2-start-service`, `m2-open-dashboard`, `m2-verify-local` (4 tasks)
         - Modul 3: `m3-start-botfather`, `m3-create-newbot`, `m3-save-token-secure`, `m3-get-userid` (4 tasks)
         - Modul 4: `m4-open-console`, `m4-verify-login` (2 tasks)
         - Modul 5: 0 checklist tasks.
       - Update `tests/pretraining-modules.test.js` line 110 to assert the 4 official Modul 3 task IDs (`['m3-start-botfather', 'm3-create-newbot', 'm3-save-token-secure', 'm3-get-userid']`) so `pretraining-modules.test.js` remains 100% passing.
    2. Create `app/hooks/usePretrainingState.ts`:
       - Define TypeScript types:
         - `PretrainingState`: `checklists: Record<string, boolean>`, `checkpoints: Record<'cp-1' | 'cp-2' | 'cp-3', 'pending' | 'passed' | 'failed'>`, `participantInfo: { nodeVersion: string; telegramUsername: string; telegramUserId: string }`
         - `ProgressStats`: `totalTasks: number` (13), `completedTasks: number`, `totalCheckpoints: number` (3), `passedCheckpoints: number`, `percentage: number` (0–100)
         - `ReadinessResult`: `status: 'ready' | 'clinic' | 'pending'`, `label: string`, `badgeClass: string`, `description: string`, `color: string`
       - Export constant `PRETRAINING_CHECKLIST_TASK_IDS` containing the 13 official IDs.
       - Export constants `STORAGE_KEY_AI_STATE = 'learnwith_ai_state_v1'`, `STORAGE_KEY_AI_CHECKLIST = 'learnwith_ai_checklist'`, `LEGACY_STORAGE_KEY_AI = 'pretraining_app_state_v1'`.
       - Enforce multi-course isolation: never touch `learnwith_word_state_v1` or `learnwith_word_unlocked`.
       - Implement mathematical progress formula:
         `taskPercent = (completedTasks / 13) * 60`
         `cpPercent = (passedCheckpoints / 3) * 40`
         `percentage = Math.min(100, Math.round(taskPercent + cpPercent))`
       - Implement readiness logic:
         - Any checkpoint `failed` -> status `'clinic'`, label `'⚠️ PERLU TECHNICAL CLINIC'`, color `'var(--color-danger, #ef4444)'`
         - All 3 checkpoints `passed` AND progress `percentage >= 80` -> status `'ready'`, label `'🎉 SIAP MENGIKUTI WORKSHOP'`, color `'var(--color-success, #10b981)'`
         - Otherwise -> status `'pending'`, label `'⏳ MENUNGGU PENYELESAIAN LANGKAH'`, color `'var(--color-warning, #f59e0b)'`
       - Implement SSR-safe external store with memory fallback and window event dispatching (`pretraining:stateChange`), providing:
         - `toggleChecklist(taskId: string, value?: boolean)`
         - `setCheckpoint(checkpointId: 'cp-1' | 'cp-2' | 'cp-3', status: 'pending' | 'passed' | 'failed')`
         - `setParticipantInfo(field: 'nodeVersion' | 'telegramUsername' | 'telegramUserId', value: string)`
         - `resetState()`
         - `progress`, `readiness`, and `isLoaded`.
  </action>
  <verify>
    <automated>pwsh -Command "& 'C:\nvm4w\nodejs\node.exe' --test tests/pretraining-modules.test.js; if ($LASTEXITCODE -eq 0 -and (Test-Path app/hooks/usePretrainingState.ts)) { exit 0 } else { exit 1 }"</automated>
    <fails_when>non-zero exit or file missing</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Build CheckpointGateCard and PretrainingCheckpointsSection</name>
  <files>
    app/components/course/pretraining/CheckpointGateCard.tsx
    app/components/course/pretraining/PretrainingCheckpointsSection.tsx
  </files>
  <read_first>
    index.html
    public/assets/css/components.css
    app/hooks/usePretrainingState.ts
    .planning/phases/26-checklist-engine-checkpoint-gates-dynamic-readiness/26-PATTERNS.md
  </read_first>
  <action>
    1. Create `app/components/course/pretraining/CheckpointGateCard.tsx`:
       - Props: `id: 'cp-1' | 'cp-2' | 'cp-3'`, `num: number`, `title: string`, `subtitle: string`, `criteriaTitle: string`, `criteria: string[]`, `status: 'pending' | 'passed' | 'failed'`, `onStatusChange: (status: 'pending' | 'passed' | 'failed') => void`, `children?: React.ReactNode`.
       - Render `.checkpoint-gate-card` with status modifier (`passed` or `failed`) and ID `card-${id}`.
       - Header row: `.checkpoint-title-wrap`, `.checkpoint-badge-icon` with checkpoint number, title (`h4`), subtitle (`span`), and status badge pill (`#status-card-${id}`) displaying `✓ Lolos` (badge-success), `✕ Kendala` (badge-danger), or `Pending` (badge-warning).
       - Render criteria section with checkmark icons and criteria text.
       - Render optional `children` (form inputs).
       - Render action button group `.cp-action-btn-group`:
         - Button `btn-success btn-cp-action`: "✓ Lolos Verifikasi"
         - Button `btn-outline-danger btn-cp-action`: "✕ Ada Kendala (Gagal)"
         - Button `btn-secondary btn-cp-action btn-sm`: "↺ Reset Status"
    2. Create `app/components/course/pretraining/PretrainingCheckpointsSection.tsx`:
       - Consume `usePretrainingState()` (or accept state props).
       - Render container `<div id="pretraining-checkpoints-container">` with section title and description:
         - Section 1: Checkpoint 1 (`#sec-checkpoint-1`, `#card-cp-1`): Node.js & npm Siap
           - Criteria: `node --version` output and `npm --version` output.
           - Input field: `#input-node-version` ("Catat Nomor Versi Node.js yang Tampil (Opsional)").
         - Section 2: Checkpoint 2 (`#sec-checkpoint-2`, `#card-cp-2`): 9Router Dashboard Berjalan
           - Criteria: Jendela terminal tetap aktif, `http://localhost:20128` terbuka di browser, berhasil login dengan sandi default `123456`.
         - Section 3: Checkpoint 3 (`#sec-checkpoint-3`, `#card-cp-3`): Bot Telegram & User ID Terverifikasi
           - Criteria: Bot dibuat via @BotFather dan Start ditekan, token tersimpan aman, User ID (hanya angka) didapat dari @userinfobot.
           - Input fields with live regex validation:
             - `#input-telegram-username`: regex `/(bot|_bot)$/i`. Shows green validation text if valid, red alert if invalid.
             - `#input-telegram-userid`: regex `/^\d+$/`. Shows green validation text if purely numeric, red warning if contains letters/spaces/symbols.
  </action>
  <verify>
    <automated>pwsh -Command "Test-Path app/components/course/pretraining/CheckpointGateCard.tsx, app/components/course/pretraining/PretrainingCheckpointsSection.tsx"</automated>
    <fails_when>returns False</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Build PretrainingReadinessSection and ResetProgressModal</name>
  <files>
    app/components/course/pretraining/PretrainingReadinessSection.tsx
    app/components/course/pretraining/ResetProgressModal.tsx
  </files>
  <read_first>
    index.html
    public/assets/css/components.css
    app/hooks/usePretrainingState.ts
    app/components/ui/Toast.tsx
    .planning/phases/26-checklist-engine-checkpoint-gates-dynamic-readiness/26-PATTERNS.md
  </read_first>
  <action>
    1. Create `app/components/course/pretraining/PretrainingReadinessSection.tsx`:
       - Props: `readiness: ReadinessResult`, `onOpenResetModal: () => void`.
       - Render section `#sec-readiness-summary` with header badge 🏆, section title "Status Kelayakan Pre-Training Peserta", and description.
       - Render status card `#card-readiness-status` (`.card.card-glass.readiness-status-card`) with dynamic `borderColor: readiness.color`.
       - Render icon 🎯, title "Hasil Evaluasi Kesiapan Mandiri".
       - Render `#readiness-status-badge` with classes `.readiness-badge` and `readiness.badgeClass` displaying `readiness.label`.
       - Render `#readiness-status-desc` with `readiness.description`.
       - Render action buttons:
         - Link to `#sec-readiness-report`: "📋 Buka Form Laporan Kesiapan" (`.btn.btn-primary`)
         - Button `#btn-open-reset-modal`: "🔄 Reset Semua Progres" (`.btn.btn-outline-danger`, calls `onOpenResetModal`)
    2. Create `app/components/course/pretraining/ResetProgressModal.tsx`:
       - Props: `isOpen: boolean`, `onClose: () => void`, `onConfirm: () => void`.
       - When `isOpen` is true:
         - Render `#modal-reset-confirm` (`.modal-backdrop.open`, `role="dialog"`, `aria-modal="true"`, `aria-labelledby="modal-reset-title"`).
         - Implement keyboard accessibility: `Escape` key triggers `onClose()`.
         - Backdrop click triggers `onClose()`.
         - Header `#modal-reset-title`: "⚠️ Atur Ulang Semua Progres?".
         - Body: detailed bullet points explaining that all 13 checklist steps, checkpoints 1–3, and participant inputs will be wiped, plus danger alert box.
         - Footer:
           - Button `#btn-cancel-reset`: "Batal" (`.btn.btn-secondary`, triggers `onClose()`)
           - Button `#btn-confirm-reset`: "Ya, Reset Semua Progres" (`.btn.btn-danger`, triggers `onConfirm()`, autoFocus)
  </action>
  <verify>
    <automated>pwsh -Command "Test-Path app/components/course/pretraining/PretrainingReadinessSection.tsx, app/components/course/pretraining/ResetProgressModal.tsx"</automated>
    <fails_when>returns False</fails_when>
  </verify>
</task>

</tasks>
