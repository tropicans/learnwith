---
phase: 26-checklist-engine-checkpoint-gates-dynamic-readiness
plan: "02"
wave: 2
depends_on:
  - "26-01"
files_modified:
  - app/components/course/pretraining/PretrainingModuleCard.tsx
  - app/components/course/pretraining/PretrainingModulesSection.tsx
  - app/components/course/pretraining/PretrainingHero.tsx
  - app/routes/course.ai.tsx
  - tests/pretraining-checklist.test.js
autonomous: true
requirements:
  - PRE-CHK-01
  - PRE-CHK-02
  - PRE-CHK-03
  - PRE-CHK-04
must_haves:
  truths:
    - "Learners can toggle any of the 13 step checkboxes in Modules 1–4, with state persisting across reloads into `learnwith_ai_state_v1` and `learnwith_ai_checklist`."
    - "The 3 Checkpoint Gates (Node.js, 9Router, Bot Telegram) and the Dynamic Readiness Summary card are mounted and fully operational inside `/course/ai?mode=pretraining`."
    - "The pre-training Hero stat card 4 dynamically updates its completion counter (`X/13 Langkah Selesai`) in real time as checkboxes are toggled."
    - "The Safe Progress Reset modal `#modal-reset-confirm` opens on demand, allows cancelling without state loss, and resets all 13 steps and 3 checkpoints upon confirmation with toast feedback."
    - "All changes maintain absolute multi-course isolation with zero pollution to Course 2 keys (`learnwith_word_state_v1`)."
    - "Automated test suite `tests/pretraining-checklist.test.js` validates PRE-CHK-01 through PRE-CHK-04 with 100% pass rate."
    - "Full test suite (`tests/*.test.js`) and TypeScript verification (`tsc --noEmit`) complete with zero regressions and zero errors."
  artifacts:
    - app/components/course/pretraining/PretrainingModuleCard.tsx
    - app/components/course/pretraining/PretrainingModulesSection.tsx
    - app/components/course/pretraining/PretrainingHero.tsx
    - app/routes/course.ai.tsx
    - tests/pretraining-checklist.test.js
  key_links:
    - "`PretrainingModuleCard.tsx` connects step checkboxes to `usePretrainingState`, reflecting active checklist progress."
    - "`PretrainingHero.tsx` reads real-time progress to display live completed step counts."
    - "`app/routes/course.ai.tsx` orchestrates `<PretrainingCheckpointsSection />`, `<PretrainingReadinessSection />`, and `<ResetProgressModal />` within `#container-pretraining`."
    - "`tests/pretraining-checklist.test.js` exercises state transitions, formula weighting, gate validation, and reset safety."
---

<objective>
Wire interactive checklist checkboxes into `PretrainingModuleCard.tsx`, synchronize live completion counters in `PretrainingHero.tsx`, mount Checkpoint Gates and Readiness Summary in `app/routes/course.ai.tsx`, author comprehensive automated test suite `tests/pretraining-checklist.test.js`, and verify zero regressions across the full application.

Purpose: Complete requirements PRE-CHK-01, PRE-CHK-02, PRE-CHK-03, and PRE-CHK-04 with end-to-end interactivity and automated validation.
Output: Fully wired interactive checklist UI, route integration in course.ai, and green test suite.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-checklist-engine-checkpoint-gates-dynamic-readiness/26-01-PLAN.md
@.planning/phases/26-checklist-engine-checkpoint-gates-dynamic-readiness/26-RESEARCH.md
@.planning/phases/26-checklist-engine-checkpoint-gates-dynamic-readiness/26-PATTERNS.md
@.planning/phases/26-checklist-engine-checkpoint-gates-dynamic-readiness/26-VALIDATION.md
@app/routes/course.ai.tsx
@app/hooks/usePretrainingState.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire interactive checkboxes, sync hero stats, and mount sections in course.ai.tsx</name>
  <files>
    app/components/course/pretraining/PretrainingModuleCard.tsx
    app/components/course/pretraining/PretrainingModulesSection.tsx
    app/components/course/pretraining/PretrainingHero.tsx
    app/routes/course.ai.tsx
  </files>
  <read_first>
    app/hooks/usePretrainingState.ts
    app/components/course/pretraining/PretrainingModuleCard.tsx
    app/components/course/pretraining/PretrainingModulesSection.tsx
    app/components/course/pretraining/PretrainingHero.tsx
    app/components/course/pretraining/PretrainingCheckpointsSection.tsx
    app/components/course/pretraining/PretrainingReadinessSection.tsx
    app/components/course/pretraining/ResetProgressModal.tsx
    app/routes/course.ai.tsx
  </read_first>
  <action>
    1. Update `PretrainingModuleCard.tsx`:
       - Accept props `checklists: Record<string, boolean>` and `onToggleChecklist: (taskId: string) => void`.
       - In step rendering, replace `readOnly` input:
         ```tsx
         {step.taskId && step.checklistLabel && (
           <label className={`step-checklist-action ${checklists[step.taskId] ? 'completed' : ''}`}>
             <input
               type="checkbox"
               className="checklist-checkbox"
               data-task-id={step.taskId}
               checked={!!checklists[step.taskId]}
               onChange={() => onToggleChecklist(step.taskId)}
             />
             <span className="checklist-label">{step.checklistLabel}</span>
           </label>
         )}
         ```
    2. Update `PretrainingModulesSection.tsx`:
       - Import `usePretrainingState` from `@/hooks/usePretrainingState`.
       - Pass `checklists={state.checklists}` and `onToggleChecklist={toggleChecklist}` to each `<PretrainingModuleCard />`.
    3. Update `PretrainingHero.tsx`:
       - Import `usePretrainingState` from `@/hooks/usePretrainingState`.
       - Dynamically replace stat card 4 (`0/13`) value with `${progress.completedTasks}/${progress.totalTasks}` and label "Langkah Selesai" while preserving other hero cards.
    4. Update `app/routes/course.ai.tsx`:
       - Import `PretrainingCheckpointsSection`, `PretrainingReadinessSection`, `ResetProgressModal` from `@/components/course/pretraining/...`.
       - Import `usePretrainingState` from `@/hooks/usePretrainingState`.
       - Import `showToast` from `@/components/ui/Toast`.
       - Manage `isResetModalOpen` boolean state in `course.ai.tsx`.
       - Inside `mode === 'pretraining'` container `#container-pretraining`:
         - Mount `<PretrainingCheckpointsSection />` immediately following `<PretrainingModulesSection />`.
         - Mount `<PretrainingReadinessSection readiness={readiness} onOpenResetModal={() => setIsResetModalOpen(true)} />` immediately following `<PretrainingCheckpointsSection />`.
         - Mount `<ResetProgressModal isOpen={isResetModalOpen} onClose={() => setIsResetModalOpen(false)} onConfirm={() => { resetState(); setIsResetModalOpen(false); showToast('Semua progres dan verifikasi berhasil diatur ulang 🔄', 'info', 2500); }} />`.
  </action>
  <verify>
    <automated>pwsh -Command "& 'C:\nvm4w\nodejs\node.exe' ./node_modules/typescript/bin/tsc --noEmit"</automated>
    <fails_when>non-zero exit or TypeScript compilation error</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Create automated unit and integration test suite</name>
  <files>tests/pretraining-checklist.test.js</files>
  <read_first>
    tests/checkpoint-engine.test.js
    tests/pretraining-modules.test.js
    app/hooks/usePretrainingState.ts
    .planning/phases/26-checklist-engine-checkpoint-gates-dynamic-readiness/26-PATTERNS.md
  </read_first>
  <action>
    Create `tests/pretraining-checklist.test.js` using Node.js native test runner (`node:test`, `node:assert`):
    1. Ensure mock `localStorage` is available for Node environment:
       ```javascript
       if (typeof localStorage === 'undefined' || !localStorage.getItem) {
         global.localStorage = {
           _data: {},
           getItem(k) { return Object.prototype.hasOwnProperty.call(this._data, k) ? this._data[k] : null },
           setItem(k, v) { this._data[k] = String(v) },
           removeItem(k) { delete this._data[k] },
           clear() { this._data = {} }
         }
       }
       ```
    2. Import or instantiate `usePretrainingState` logic (`calculatePretrainingProgress`, `calculatePretrainingReadiness`, `PRETRAINING_CHECKLIST_TASK_IDS`, `DEFAULT_PRETRAINING_STATE`, `STORAGE_KEY_AI_STATE`, `STORAGE_KEY_AI_CHECKLIST`).
    3. **Suite 1: 13-Step Checklist Engine (PRE-CHK-01)**:
       - Verify `PRETRAINING_CHECKLIST_TASK_IDS` contains exactly 13 tasks.
       - Verify task distribution: Modul 1 has 3 (`m1-check-node`, `m1-verify-lts`, `m1-check-npm`), Modul 2 has 4 (`m2-install-pkg`, `m2-start-service`, `m2-open-dashboard`, `m2-verify-local`), Modul 3 has 4 (`m3-start-botfather`, `m3-create-newbot`, `m3-save-token-secure`, `m3-get-userid`), Modul 4 has 2 (`m4-open-console`, `m4-verify-login`).
       - Verify updating checklist tasks correctly updates `completedTasks` and recalculates percentage.
       - Verify multi-course isolation: mutating AI checklist never alters `learnwith_word_state_v1` or `learnwith_word_unlocked`.
    4. **Suite 2: Checkpoint Verification Gates (PRE-CHK-02)**:
       - Verify `cp-1`, `cp-2`, `cp-3` initialize with status `'pending'`.
       - Verify status transitions to `'passed'` and `'failed'`.
       - Verify Telegram bot username validation regex `/(bot|_bot)$/i`:
         - Passes: `jadwal_bot`, `my_calendar_bot`, `HermesBot`.
         - Fails: `jadwalbotku`, `asisten_jadwal`, `testbot123`.
       - Verify Telegram User ID validation regex `/^\d+$/`:
         - Passes: `123456789`, `987654321`.
         - Fails: `@user123`, `id-12345`, `123 456`.
    5. **Suite 3: Dynamic Readiness Calculation (PRE-CHK-03)**:
       - Verify initial state results in status `'pending'` ("⏳ MENUNGGU PENYELESAIAN LANGKAH").
       - Verify any failed checkpoint (`failed`) immediately triggers status `'clinic'` ("⚠️ PERLU TECHNICAL CLINIC").
       - Verify weighted progress formula: 60% tasks, 40% checkpoints.
         - 13/13 tasks (60%) + 0/3 checkpoints (0%) = 60%.
         - 13/13 tasks (60%) + 3/3 checkpoints (40%) = 100%.
       - Verify all 3 checkpoints passed + progress >= 80% yields status `'ready'` ("🎉 SIAP MENGIKUTI WORKSHOP").
    6. **Suite 4: Safe Reset Confirmation Engine (PRE-CHK-04)**:
       - Verify `resetState()` clears all 13 checklist items to `false`.
       - Verify checkpoints revert to `pending`.
       - Verify participantInfo fields are cleared to empty strings.
       - Verify Course 2 storage keys remain intact and untouched.
  </action>
  <verify>
    <automated>& 'C:\nvm4w\nodejs\node.exe' --test tests/pretraining-checklist.test.js</automated>
    <fails_when>non-zero exit or test assertion failure</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Run full suite zero-regression and TypeScript verification</name>
  <files>tests/pretraining-checklist.test.js</files>
  <read_first>
    package.json
  </read_first>
  <action>
    1. Run the entire automated test suite (`tests/*.test.js`) to ensure zero regressions across Course 1 (Agentic AI), Course 2 (Advanced Word), live class guides, foundation sections, and interactive modules.
    2. Run the TypeScript type-checker (`tsc --noEmit`) to verify 100% clean type compliance across the entire codebase.
  </action>
  <verify>
    <automated>& 'C:\nvm4w\nodejs\node.exe' --test tests/*.test.js; if ($LASTEXITCODE -eq 0) { & 'C:\nvm4w\nodejs\node.exe' ./node_modules/typescript/bin/tsc --noEmit } else { exit 1 }</automated>
    <fails_when>non-zero exit or test/compile failure</fails_when>
  </verify>
</task>

</tasks>
