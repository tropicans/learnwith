---
phase: 25-interactive-modules-1-5-1-click-copy-engine
plan: "01"
wave: 1
depends_on: []
files_modified:
  - app/data/pretrainingModules.ts
  - app/components/course/pretraining/CopyableCodeBlock.tsx
  - app/components/ui/Toast.tsx
  - app/components/course/pretraining/CheckpointPreviewCard.tsx
  - app/components/course/pretraining/ModuleArchitectureFlow.tsx
  - app/components/course/pretraining/PretrainingModuleCard.tsx
autonomous: true
requirements:
  - PRE-MOD-01
  - PRE-MOD-02
  - PRE-MOD-03
  - PRE-MOD-04
  - PRE-MOD-05
  - PRE-MOD-06
must_haves:
  truths:
    - Strongly-typed dataset `app/data/pretrainingModules.ts` captures all educational content, commands, output badges, caution alerts, external links, and checkpoint previews for Modules 1 through 5 with 100% fidelity to `index.html`.
    - `CopyableCodeBlock.tsx` delivers a 1-click copy engine with terminal chrome, modern `navigator.clipboard` write with legacy textarea fallback, visual feedback "✓ Tersalin!" for 2000ms, and preserves multi-line whitespace.
    - `Toast.tsx` provides a lightweight, non-blocking notification dispatcher (`showToast`) and container adhering to existing design tokens.
    - `PretrainingModuleCard.tsx` renders accessible module headers with `role="button"`, `aria-expanded`, `aria-controls`, keyboard navigation (`Enter` / `Space`), step nodes, checklist items (`data-task-id`), and previews.
    - `ModuleArchitectureFlow.tsx` renders the interactive 4-node system flow (Telegram Ponsel -> Hermes Agent -> 9Router -> Model AI Cloud) for Modul 5.
    - `CheckpointPreviewCard.tsx` renders the preview gates for Checkpoints 1, 2, and 3.
  artifacts:
    - app/data/pretrainingModules.ts
    - app/components/course/pretraining/CopyableCodeBlock.tsx
    - app/components/ui/Toast.tsx
    - app/components/course/pretraining/CheckpointPreviewCard.tsx
    - app/components/course/pretraining/ModuleArchitectureFlow.tsx
    - app/components/course/pretraining/PretrainingModuleCard.tsx
  key_links:
    - `app/data/pretrainingModules.ts` serves as the single source of truth for Modules 1–5 text, commands, and metadata.
    - `CopyableCodeBlock.tsx` and `Toast.tsx` empower learners to copy CLI commands with instant visual and toast feedback.
    - `PretrainingModuleCard.tsx` consumes module data and composes code blocks, alerts, and checkpoint cards into accessible collapsible sections.
---

<objective>
Extract curriculum content for Modules 1 through 5 into a strongly typed TypeScript dataset (`app/data/pretrainingModules.ts`), implement the 1-Click Copy Engine with toast notifications (`CopyableCodeBlock.tsx` and `Toast.tsx`), and construct accessible modular UI components (`PretrainingModuleCard.tsx`, `ModuleArchitectureFlow.tsx`, `CheckpointPreviewCard.tsx`).

Purpose: Deliver the foundational data and interactive building blocks required for PRE-MOD-01 through PRE-MOD-06 before assembling the master accordion and route integration in Wave 2.
Output: Type-safe data module and 5 modular presentation components.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-interactive-modules-1-5-1-click-copy-engine/25-RESEARCH.md
@.planning/phases/25-interactive-modules-1-5-1-click-copy-engine/25-PATTERNS.md
@.planning/phases/25-interactive-modules-1-5-1-click-copy-engine/25-VALIDATION.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract and define typed dataset for Modules 1–5</name>
  <files>app/data/pretrainingModules.ts</files>
  <read_first>
    index.html
    app/data/pretrainingFoundation.ts
    .planning/phases/25-interactive-modules-1-5-1-click-copy-engine/25-PATTERNS.md
  </read_first>
  <action>
    Create `app/data/pretrainingModules.ts` extracting the complete content of Modules 1–5 from `index.html` (lines 1099–1960):
    1. Define TypeScript interfaces:
       - `AlertType`: `'info' | 'warning' | 'danger' | 'success'`
       - `StepAlert`: `type`, `icon`, `title`, `content`, `bullets?`, `note?`
       - `StepLink`: `url`, `label`, `isExternal`, `note?`
       - `ModuleStep`: `nodeId` (e.g. 'A', 'B', '1'), `title`, `badgeLabel`, `badgeClass?`, `description`, `command?`, `commandLanguage?`, `outputBadge?` (`label`, `value`), `instructions?`, `externalLink?`, `alerts?`, `taskId` (`data-task-id`), `checklistLabel`
       - `CheckpointPreview`: `checkpointNum`, `title`, `badge`, `description`, `requirements`, `note?`
       - `ArchFlowNode`: `id`, `icon`, `title`, `desc`
       - `ArchFlowArrow`: `label`
       - `PretrainingModule`: `id` (`sec-module-1` .. `sec-module-5`), `num` (1..5), `title`, `subtitle`, `icon`, `badgeLabel`, `badgeClass`, `steps`, `checkpoint?`, `hasArchFlow?`
    2. Populate `PRETRAINING_MODULES`:
       - **Modul 1 (Node.js)**: 3 steps (`m1-check-node` with `node --version`, `m1-verify-lts` with nodejs.org link & 10 steps, `m1-check-npm` with `npm --version`), Checkpoint 1 preview (Node.js Siap).
       - **Modul 2 (9Router)**: 4 steps (`m2-install-pkg` with `npm install -g 9router`, `m2-start-service` with `9router` & `http://localhost:20128`, `m2-open-dashboard` with password `123456`, `m2-verify-local` with `Ctrl + C` & `curl http://localhost:20128/v1/models`), Checkpoint 2 preview (Dashboard 9Router Terbuka).
       - **Modul 3 (Bot Telegram & User ID)**: 5 steps (`m3-start-botfather` with `https://t.me/BotFather`, `m3-create-newbot` with `/newbot`, `m3-save-token-secure` with emergency `/revoke`, `m3-start-chat`, `m3-get-userid` with `https://t.me/userinfobot` & ID comparison grid), Checkpoint 3 preview (Bot Telegram & User ID Siap).
       - **Modul 4 (Google Cloud & Hermes Readiness)**: 2 steps (`m4-open-console` with `https://console.cloud.google.com`, `m4-verify-login` with pre-class boundaries & Hermes CLI preview `hermes -v`).
       - **Modul 5 (Catatan & Batasan Penting Sebelum Kelas)**: 3 steps/sections (Batasan 1: Provider 9Router, Batasan 2: Hermes Agent installation, Batasan 3: 4-Node Architecture flow).
    3. Ensure all text, warning boxes, and command strings preserve exact naming and parameter rules from `index.html`.
  </action>
  <verify>
    <automated>pwsh -Command "& 'C:\nvm4w\nodejs\node.exe' -e \"import('./app/data/pretrainingModules.ts').catch(() => { require('esbuild').buildSync({ entryPoints: ['app/data/pretrainingModules.ts'], write: false }); console.log('Modules data compiled successfully'); })\""</automated>
    <fails_when>non-zero exit or syntax/type compile error</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Build 1-Click Copy Engine and Toast Notification System</name>
  <files>
    app/components/course/pretraining/CopyableCodeBlock.tsx
    app/components/ui/Toast.tsx
  </files>
  <read_first>
    index.html
    public/assets/css/components.css
    .planning/phases/25-interactive-modules-1-5-1-click-copy-engine/25-RESEARCH.md
    .planning/phases/25-interactive-modules-1-5-1-click-copy-engine/25-PATTERNS.md
  </read_first>
  <action>
    1. Create `app/components/ui/Toast.tsx`:
       - Export helper function `showToast(message: string, type?: 'info' | 'success' | 'warning' | 'danger', duration?: number): void`.
       - Support window CustomEvent (`app:toast`) dispatcher and listener so any component can safely trigger notifications without deep prop drilling.
       - Export `ToastContainer` component that mounts once in the layout/section, renders active toasts into `.toast-container`, attaches appropriate classes (`toast`, `toast-{type}`, `show`), and auto-dismisses after the specified duration.
    2. Create `app/components/course/pretraining/CopyableCodeBlock.tsx`:
       - Props: `code: string`, `language?: string`, `ariaLabel?: string`, `showDots?: boolean`, `className?: string`.
       - Render terminal container (`.code-container`) with `.code-header` hosting:
         - Three dots (`.code-dots > .code-dot.red, .yellow, .green`)
         - Language badge (`.code-label`, default 'PowerShell')
         - Copy button (`.code-copy-btn` with `.copied` class when active)
       - Implement copy handler:
         - Modern `navigator.clipboard.writeText(cleanCode)` inside `window.isSecureContext`.
         - Fallback using hidden `<textarea>` + `document.execCommand('copy')` for non-secure contexts or legacy browsers.
         - Strictly preserve all newlines and indentation (no `.replace(/\s+/g, ' ')`).
         - Set `isCopied = true` for 2000ms, then revert.
         - Display `✓ Tersalin!` when copied, `📋 Salin Perintah` when normal.
         - Call `showToast('Perintah berhasil disalin ke clipboard 📋', 'success', 2000)`.
       - Render `<pre className="code-content"><code>{code}</code></pre>`.
  </action>
  <verify>
    <automated>pwsh -Command "Test-Path app/components/course/pretraining/CopyableCodeBlock.tsx, app/components/ui/Toast.tsx"</automated>
    <fails_when>returns False</fails_when>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Build PretrainingModuleCard, ModuleArchitectureFlow, and CheckpointPreviewCard</name>
  <files>
    app/components/course/pretraining/CheckpointPreviewCard.tsx
    app/components/course/pretraining/ModuleArchitectureFlow.tsx
    app/components/course/pretraining/PretrainingModuleCard.tsx
  </files>
  <read_first>
    index.html
    app/data/pretrainingModules.ts
    app/components/course/pretraining/CopyableCodeBlock.tsx
    .planning/phases/25-interactive-modules-1-5-1-click-copy-engine/25-PATTERNS.md
  </read_first>
  <action>
    1. Create `app/components/course/pretraining/CheckpointPreviewCard.tsx`:
       - Props: `checkpoint: CheckpointPreview`.
       - Render `.card.card-glass` with `borderLeft: '4px solid var(--color-success)'`.
       - Display title `📍 Gerbang Checkpoint {num}: {title}` in green, badge `Target Tahap {num}`.
       - Render bulleted checklist of passing criteria and troubleshooting note.
    2. Create `app/components/course/pretraining/ModuleArchitectureFlow.tsx`:
       - Render `.arch-flow-container` matching `index.html` lines 1906–1953:
         - Node 1: `📱` Telegram Ponsel ("Anda mengetik instruksi jadwal meeting ke Bot Telegram")
         - Arrow 1: "Kirim Pesan" `──▶`
         - Node 2: `🤖` Hermes Agent ("Berjalan di laptop Anda sebagai asisten cerdas")
         - Arrow 2: "OpenAI Endpoint :20128/v1" `──▶`
         - Node 3: `🚀` 9Router ("Perute lokal menghubungkan agen ke model AI")
         - Arrow 3: "Proses Bahasa" `──▶`
         - Node 4: `🧠` Model AI Cloud ("Menganalisis jadwal & mengeksekusi Google Calendar")
       - Render completion alert box: `.alert-box.alert-success` ("✨ Pre-Training Anda Siap!").
    3. Create `app/components/course/pretraining/PretrainingModuleCard.tsx`:
       - Props: `module: PretrainingModule`, `isExpanded: boolean`, `onToggle: () => void`.
       - Container: `.module-card`.
       - Header: `.module-header` with:
         - `role="button"`
         - `tabIndex={0}`
         - `aria-expanded={isExpanded}`
         - `aria-controls={`module-body-${module.num}`}`
         - `onClick={onToggle}`
         - `onKeyDown={(e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); onToggle(); } }}`
         - Icon badge (`.module-icon-badge`), title (`h3`), subtitle (`p`), step count badge (`.badge-pill`), and `.module-chevron` with rotation transform.
       - Body: `.module-body` rendered with ID `module-body-{module.num}` (or hidden when collapsed).
       - Step rendering:
         - `.step-section` with node badge (`.step-section-node`), title, and badge.
         - Render `CopyableCodeBlock` when `step.command` exists.
         - Render `.command-output-badge` when `step.outputBadge` exists.
         - Render numbered instruction lists and external links (`target="_blank" rel="noopener noreferrer"`).
         - Render alert boxes (`.alert-box.alert-info`, `.alert-warning`, `.alert-danger`).
         - Render checklist action `<label className="step-checklist-action"><input type="checkbox" className="checklist-checkbox" data-task-id={step.taskId} readOnly /><span className="checklist-label">{step.checklistLabel}</span></label>`.
       - Render `<ModuleArchitectureFlow />` if `module.hasArchFlow` is true.
       - Render `<CheckpointPreviewCard checkpoint={module.checkpoint} />` if `module.checkpoint` is defined.
  </action>
  <verify>
    <automated>pwsh -Command "Test-Path app/components/course/pretraining/PretrainingModuleCard.tsx, app/components/course/pretraining/ModuleArchitectureFlow.tsx, app/components/course/pretraining/CheckpointPreviewCard.tsx"</automated>
    <fails_when>returns False</fails_when>
  </verify>
</task>

</tasks>
